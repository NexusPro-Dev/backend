# Requerimientos y Trazabilidad — NEXUS

| Campo | Valor |
|---|---|
| Proyecto | NEXUS — Renovación de plataforma |
| Empresa | FACTECH GROUP SAS |
| Documento | `requirements.md` |
| Versión | 0.79.0 |
| Estado | Borrador |
| Responsable técnico | Bonilla Diaz William Steven |
| Fecha de creación | 20-08-2026 |
| Última actualización | 28-08-2026 |
| Documento superior | `constitution.md` v0.7.0 |
| Documentos relacionados | `modules.md` v0.12.0 |

---

## 1. Propósito

Este documento es el **índice de los requerimientos** del sistema y su **matriz de trazabilidad**. Responde en qué estado está cada requerimiento y qué elementos técnicos lo implementan.

**No es el inventario de módulos.** Ese vive en [`modules.md`](modules.md), que es su autoridad única. Aquí se registran los requerimientos **de** esos módulos.

---

## 2. Documentos de requerimientos por módulo

Cada módulo del inventario tiene su documento en `docs/requirements/`, redactado con la plantilla de requerimientos por módulo.

| Módulo | Documento | Estado |
|---|---|---|
| `SP` — Sistema Principal | [`requirements/sp.md`](requirements/sp.md) | **Aprobado** |
| `PM` — Productos y Mercadeo | [`requirements/pm.md`](requirements/pm.md) | Borrador |
| `CM` — Comisiones | [`requirements/cm.md`](requirements/cm.md) | Borrador |


Para agregar un módulo nuevo, seguir el procedimiento de [`modules.md` §8](modules.md#8-como-se-incorpora-un-modulo).

---

## 3. Nomenclatura de identificadores

### 3.1 Formatos

| Tipo | Formato | Ejemplo |
|---|---|---|
| Requerimiento funcional | `RF-[MÓDULO]-NNN` | `RF-SP-001` |
| Requerimiento no funcional | `RNF-[CATEGORÍA]-NNN` | `RNF-SEG-001` |
| Regla de negocio | `RN-[MÓDULO]-NNN` | `RN-SP-003` |
| Criterio de aceptación | `CA-[MÓDULO]-NNN` | `CA-SP-001` |
| Validación | `VAL-NNN` | `VAL-001` |
| Prueba | `T-[MÓDULO]-NNN` | `T-SP-001` |

Reglas:

- La numeración es correlativa **dentro de cada módulo** y **nunca se reutiliza**, ni siquiera si el requerimiento se descarta. Un identificador retirado se marca como `Descartado` en la matriz y su número queda consumido.
- Los submódulos **no** intervienen en el identificador ([`modules.md` §2.2](modules.md#22-por-que-los-submodulos-no-llevan-codigo-propio)): mover una funcionalidad entre submódulos no debe cambiar su identificador.
- El código de módulo, una vez usado en un identificador, **no se cambia jamás**.
- **Las reglas de negocio admiten dos espacios.** `RN-[MÓDULO]-NNN` para las reglas propias de un módulo, y `RN-SEG-NNN` para las **reglas transversales de seguridad**, que gobiernan la autorización en todo el sistema y alcanzan a varios módulos a la vez. `SEG` es aquí un espacio de reglas transversales, no un código de módulo; el prefijo `RN-` frente a `RNF-` lo desambigua de la categoría de requerimiento no funcional.
- **Las historias de usuario quedan fuera de la trazabilidad.** El documento de historias sirve para levantar requerimientos, pero la cadena trazable es `RF` → tripleta → Pull Request → código → prueba (Art. III.1). Una historia no equivale a un requerimiento funcional: suele originar varios.
- **No existe un identificador de especificación.** Al corresponder cada tripleta a exactamente un requerimiento (Art. I.2), un `SPEC-SP-001` solo podría referirse a `RF-SP-001`: serían dos identificadores para la misma cosa, y el segundo acabaría desincronizándose. La tripleta se referencia por su ruta: `docs/specs/sp/001-registrar-rol/`.

### 3.2 Categorías de requerimientos no funcionales

Las categorías **no son módulos**: comparten el espacio de nombres pero clasifican atributos de calidad según ISO/IEC 25010.

| Categoría | Atributo |
|---|---|
| `SEG` | Seguridad |
| `PERF` | Rendimiento |
| `USA` | Usabilidad |
| `MAN` | Mantenibilidad |
| `PORT` | Portabilidad |
| `FIA` | Fiabilidad |
| `COMP` | Compatibilidad |

!!! note "Sobre `SEG`"

    `SEG` se usa como categoría de requerimiento no funcional (`RNF-SEG-001`) y también como prefijo de las reglas de negocio de seguridad en `security.md` (`RN-SEG-003`). Son espacios distintos y el prefijo `RNF-` o `RN-` los desambigua, pero conviene tenerlo presente al buscar.

---

## 4. Matriz de trazabilidad

Implementa el Art. III.1. Se actualiza **como parte del cambio**, no después (Art. III.6).

| ID | Requerimiento | Módulo | Tripleta | Issue | PR | Pruebas | Estado |
|---|---|---|---|---|---|---|---|
| `RF-SP-001` | Registrar rol | `SP` | [`specs/sp/001-registrar-rol/`](specs/sp/001-registrar-rol/tasks.md) | Pendiente de crear | `feature/registrar-rol` | 32 unitarias y 60 de integración en verde (`./mvnw clean verify`, 22-08-2026) | **En desarrollo** |
| `RF-SP-002` | Consultar roles | `SP` | [`specs/sp/002-consultar-roles/`](specs/sp/002-consultar-roles/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#20) | 10 unitarias y 22 de integración en verde (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-003` | Consultar detalle de un rol | `SP` | [`specs/sp/003-consultar-detalle-rol/`](specs/sp/003-consultar-detalle-rol/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#20) | 10 de integración en verde (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-004` | Editar rol | `SP` | [`specs/sp/004-editar-rol/`](specs/sp/004-editar-rol/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | 25 de integración en verde, compartidas con las otras tres operaciones sobre el rol (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-005` | Asignar permisos a un rol | `SP` | [`specs/sp/005-asignar-permisos/`](specs/sp/005-asignar-permisos/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | 12 de integración en verde, compartidas con `RF-SP-006` (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-006` | Revocar permisos de un rol | `SP` | [`specs/sp/006-revocar-permisos/`](specs/sp/006-revocar-permisos/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de permisos de rol (25-08-2026) | **En desarrollo** |
| `RF-SP-007` | Cambiar el estado de un rol | `SP` | [`specs/sp/007-cambiar-estado-rol/`](specs/sp/007-cambiar-estado-rol/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de administración de roles (25-08-2026) | **En desarrollo** |
| `RF-SP-008` | Cambiar el rol padre de un rol | `SP` | [`specs/sp/008-cambiar-rol-padre/`](specs/sp/008-cambiar-rol-padre/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de administración de roles. **`CA-SP-161` ya tiene prueba** (`RoleParentConcurrencyIT`, 27-08-2026), y al escribirla se vio que **el bloqueo global que el plan exige no existía**: se construyó ese día (`RoleHierarchyLockIT`) | **En desarrollo** |
| `RF-SP-009` | Eliminar rol | `SP` | [`specs/sp/009-eliminar-rol/`](specs/sp/009-eliminar-rol/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de administración de roles (25-08-2026) | **En desarrollo** |
| `RF-SP-010` | Consultar catálogo de permisos | `SP` | [`specs/sp/010-consultar-permisos/`](specs/sp/010-consultar-permisos/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 14 unitarias y 38 de integración en verde (`./mvnw clean verify`, 22-08-2026); el endpoint existe y está documentado en el contrato, **sin las pruebas de API** de `CA-SP-073` a `CA-SP-077` (`T-10`) ni las de casos límite (`T-11`) | **En desarrollo** |
| `RF-SP-011` | Consultar auditoría de cambios | `SP` | [`specs/sp/011-consultar-auditoria-cambios/`](specs/sp/011-consultar-auditoria-cambios/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | 19 de integración en verde, compartidas con los otros tres registros, más 5 del conteo acotado (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-012` | Consultar auditoría de eliminación | `SP` | [`specs/sp/012-consultar-auditoria-eliminacion/`](specs/sp/012-consultar-auditoria-eliminacion/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de auditoría (25-08-2026) | **En desarrollo** |
| `RF-SP-013` | Consultar auditoría de error | `SP` | [`specs/sp/013-consultar-auditoria-error/`](specs/sp/013-consultar-auditoria-error/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de auditoría (25-08-2026) | **En desarrollo** |
| `RF-SP-014` | Consultar auditoría de seguridad | `SP` | [`specs/sp/014-consultar-auditoria-seguridad/`](specs/sp/014-consultar-auditoria-seguridad/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#33) | Cubierto por la suite de auditoría (25-08-2026) | **En desarrollo** |
| `RF-SP-015` | Consultar detalle de un permiso | `SP` | [`specs/sp/015-consultar-detalle-permiso/`](specs/sp/015-consultar-detalle-permiso/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` (#20) | 5 de integración en verde (`./mvnw clean verify`, 25-08-2026) | **En desarrollo** |
| `RF-SP-016` | Registrar membresía | `SP` | [`specs/sp/016-registrar-membresia/`](specs/sp/016-registrar-membresia/tasks.md) | Pendiente de crear | `feature/registrar-membresia` | 9 unitarias y 27 de integración en verde (`./mvnw clean verify`, 24-08-2026); el color de `RN-SP-024` implementado y probado (26-08-2026) | **En desarrollo** |
| `RF-SP-017` | Consultar membresías | `SP` | [`specs/sp/017-consultar-membresias/`](specs/sp/017-consultar-membresias/tasks.md) | Pendiente de crear | `feature/registrar-membresia` | Cubierto por la suite del submódulo (24-08-2026); el color de `RN-SP-024` implementado y probado (26-08-2026) | **En desarrollo** |
| `RF-SP-018` | Consultar detalle de una membresía | `SP` | [`specs/sp/018-consultar-detalle-membresia/`](specs/sp/018-consultar-detalle-membresia/tasks.md) | Pendiente de crear | `feature/registrar-membresia` | Cubierto por la suite del submódulo (24-08-2026); el color de `RN-SP-024` implementado y probado (26-08-2026) | **En desarrollo** |
| `RF-SP-019` | Consultar monedas | `SP` | [`specs/sp/019-consultar-monedas/`](specs/sp/019-consultar-monedas/tasks.md) | Pendiente de crear | `feature/catalogo-monedas` | 22 de integración en verde (`./mvnw clean verify`, 24-08-2026) | **En desarrollo** |
| `RF-SP-020` | Registrar país | `SP` | [`specs/sp/020-registrar-pais/`](specs/sp/020-registrar-pais/tasks.md) | Pendiente de crear | `feature/catalogo-paises` | 38 de integración en verde en las tres suites de países (`./mvnw -Dtest='Countr*' test`, 28-08-2026), más `OpenApiContractIT`. **`./mvnw verify` completo sin ejecutar** desde el paso a alfa-3 | **En desarrollo** |
| `RF-SP-021` | Consultar países | `SP` | [`specs/sp/021-consultar-paises/`](specs/sp/021-consultar-paises/tasks.md) | Pendiente de crear | `feature/catalogo-paises` | Cubierto por la suite del catálogo (24-08-2026) | **En desarrollo** |
| `RF-SP-022` | Cambiar el estado de un país | `SP` | [`specs/sp/022-cambiar-estado-pais/`](specs/sp/022-cambiar-estado-pais/tasks.md) | Pendiente de crear | `feature/catalogo-paises` | Cubierto por la suite del catálogo (24-08-2026) | **En desarrollo** |
| `RF-SP-023` | Cambiar el estado de una moneda | `SP` | [`specs/sp/023-cambiar-estado-moneda/`](specs/sp/023-cambiar-estado-moneda/tasks.md) | Pendiente de crear | `feature/catalogo-monedas` | Cubierto por la suite del catálogo (24-08-2026) | **En desarrollo** |
| `RF-SP-024` | Registrar usuario | `SP` | [`specs/sp/024-registrar-usuario/`](specs/sp/024-registrar-usuario/tasks.md) | Pendiente de crear | `feature/registrar-usuario` | 19 de integración en verde (`./mvnw clean verify`, 24-08-2026) y las concurrentes en `UserConcurrencyIT` (26-08-2026) | **En desarrollo** |
| `RF-SP-025` | Consultar usuarios | `SP` | [`specs/sp/025-consultar-usuarios/`](specs/sp/025-consultar-usuarios/tasks.md) | Pendiente de crear | `feature/consultas-de-usuario` | 21 de integración en verde (`./mvnw clean verify`, 24-08-2026); sin las pruebas de `EXPLAIN` | **En desarrollo** |
| `RF-SP-026` | Consultar detalle de un usuario | `SP` | [`specs/sp/026-consultar-detalle-usuario/`](specs/sp/026-consultar-detalle-usuario/tasks.md) | Pendiente de crear | `feature/consultas-de-usuario` | Cubierto por la suite de consultas (24-08-2026); sin la prueba de número de sentencias | **En desarrollo** |
| `RF-SP-027` | Editar usuario | `SP` | [`specs/sp/027-editar-usuario/`](specs/sp/027-editar-usuario/tasks.md) | Pendiente de crear | `feature/ciclo-de-vida-de-usuario` | 27 de integración en verde (`./mvnw clean verify`, 24-08-2026) y las concurrentes en `UserConcurrencyIT` (26-08-2026), que destaparon un `500` en la edición hacia un correo duplicado | **En desarrollo** |
| `RF-SP-028` | Cambiar el estado de un usuario | `SP` | [`specs/sp/028-cambiar-estado-usuario/`](specs/sp/028-cambiar-estado-usuario/tasks.md) | Pendiente de crear | `feature/ciclo-de-vida-de-usuario` | Cubierto por la suite del ciclo de vida (24-08-2026) y por `AccessRevocationIT` (26-08-2026): el corte de tokens ya emitidos existe y está verificado, y `UserConcurrencyIT` (26-08-2026) destapó que `RN-SP-001` no se sostenía bajo concurrencia | **En desarrollo** |
| `RF-SP-029` | Eliminar usuario | `SP` | [`specs/sp/029-eliminar-usuario/`](specs/sp/029-eliminar-usuario/tasks.md) | Pendiente de crear | `feature/ciclo-de-vida-de-usuario` | Cubierto por la suite del ciclo de vida (24-08-2026), por `AccessRevocationIT` y por `UserConcurrencyIT` (26-08-2026) | **En desarrollo** |
| `RF-SP-030` | Asignar roles a un usuario | `SP` | [`specs/sp/030-asignar-roles-usuario/`](specs/sp/030-asignar-roles-usuario/tasks.md) | Pendiente de crear | `feature/roles-de-usuario` | 30 de integración y 19 unitarias nuevas en verde (`./mvnw clean verify`, 24-08-2026); sin pruebas con dobles del orden de verificación; **el bloqueo pesimista de `RN-SP-018` corregido y con tres pasadas concurrentes en verde** (26-08-2026) | **En desarrollo** |
| `RF-SP-031` | Retirar roles de un usuario | `SP` | [`specs/sp/031-retirar-roles-usuario/`](specs/sp/031-retirar-roles-usuario/tasks.md) | Pendiente de crear | `feature/roles-de-usuario` | Cubierto por la suite de roles de usuario (24-08-2026); sin prueba del fallo al revocar sesiones; **el bloqueo pesimista de `RN-SP-018` corregido y con tres pasadas concurrentes en verde** (26-08-2026) | **En desarrollo** |
| `RF-SP-032` | Asignar membresía a un usuario | `SP` | [`specs/sp/032-asignar-membresia-usuario/`](specs/sp/032-asignar-membresia-usuario/tasks.md) | Pendiente de crear | `feature/membresia-de-usuario` | 22 de integración y 10 unitarias en verde (`./mvnw clean verify`, 24-08-2026); sin el registro de los rechazos; **el bloqueo pesimista de `RN-SP-018` corregido y con tres pasadas concurrentes en verde** (26-08-2026) | **En desarrollo** |
| `RF-SP-033` | Retirar la membresía de un usuario | `SP` | [`specs/sp/033-retirar-membresia-usuario/`](specs/sp/033-retirar-membresia-usuario/tasks.md) | Pendiente de crear | `feature/membresia-de-usuario` | Cubierto por la suite de membresía (24-08-2026) y por `UserConcurrencyIT` (26-08-2026), con el par en los dos órdenes; **el bloqueo pesimista de `RN-SP-018` corregido y con tres pasadas concurrentes en verde** (26-08-2026) | **En desarrollo** |
| `RF-SP-034` | Iniciar sesión | `SP` | [`specs/sp/034-iniciar-sesion/`](specs/sp/034-iniciar-sesion/tasks.md) | Pendiente de crear | `feature/sesion` | 35 de integración y 8 unitarias en verde (`./mvnw clean verify`, 26-08-2026); el cambio obligatorio ya retiene (`T-12`), el límite de tasa lo aporta `RateLimitFilter` | **En desarrollo** |
| `RF-SP-035` | Refrescar el token de acceso | `SP` | [`specs/sp/035-refrescar-token/`](specs/sp/035-refrescar-token/tasks.md) | Pendiente de crear | `feature/sesion` | Cubierto por la suite de sesión (24-08-2026); el límite de tasa lo aporta `RateLimitFilter` con cota propia (60/min por origen), verificada en `RateLimitIT`; sin prueba concurrente | **En desarrollo** |
| `RF-SP-036` | Cerrar sesión | `SP` | [`specs/sp/036-cerrar-sesion/`](specs/sp/036-cerrar-sesion/tasks.md) | Pendiente de crear | `feature/sesion` | Cubierto por la suite de sesión (24-08-2026); **sin límite de tasa**: `RateLimitFilter` no cubre `/auth/logout` | **En desarrollo** |
| `RF-SP-037` | Cambiar la propia contraseña | `SP` | [`specs/sp/037-cambiar-contrasena/`](specs/sp/037-cambiar-contrasena/tasks.md) | Pendiente de crear | `feature/credenciales-y-perfil-propio` | 21 de integración y 4 unitarias en verde (`./mvnw clean verify`, 24-08-2026), más `MustChangePasswordIT` desde el 26-08-2026: esta ruta es la salida de la cuenta retenida | **En desarrollo** |
| `RF-SP-038` | Restablecer la contraseña de un usuario | `SP` | [`specs/sp/038-restablecer-contrasena/`](specs/sp/038-restablecer-contrasena/tasks.md) | Pendiente de crear | `feature/credenciales-y-perfil-propio` | Cubierto por la suite de credenciales (24-08-2026) y por `AccessRevocationIT` (26-08-2026): el corte de tokens ya emitidos existe y está verificado | **En desarrollo** |
| `RF-SP-039` | Consultar el propio perfil | `SP` | [`specs/sp/039-consultar-perfil-propio/`](specs/sp/039-consultar-perfil-propio/tasks.md) | Pendiente de crear | `feature/credenciales-y-perfil-propio` | Cubierto por la suite de credenciales (24-08-2026) y por `MustChangePasswordIT` (26-08-2026): responde con la marca puesta | **En desarrollo** |
| `RF-SP-040` | Restablecer la propia contraseña olvidada | `SP` | [`specs/sp/040-restablecer-contrasena-olvidada/`](specs/sp/040-restablecer-contrasena-olvidada/tasks.md) | Pendiente de crear | `feature/restablecer-contrasena-olvidada` | 21 de integración en verde (`./mvnw clean verify`, 26-08-2026), incluidas las dos concurrentes y la de temporización; cota de 5/min con 5 de espera, probada con el reloj fijado (26-08-2026) | **En desarrollo** |
| `RF-SP-041` | Asignar o cambiar el superior comercial de un usuario | `SP` | [`specs/sp/041-asignar-superior-comercial/`](specs/sp/041-asignar-superior-comercial/tasks.md) | Pendiente de crear | `feature/estructura-comercial` | 25 de integración y 4 unitarias en verde (`./mvnw clean verify`, 24-08-2026) | **En desarrollo** |
| `RF-SP-042` | Consultar el equipo a cargo de un usuario | `SP` | [`specs/sp/042-consultar-equipo-a-cargo/`](specs/sp/042-consultar-equipo-a-cargo/tasks.md) | Pendiente de crear | `feature/estructura-comercial` | Cubierto por la suite de estructura comercial (24-08-2026); alcance global mientras D-22 siga abierta | **En desarrollo** |
| `RF-PM-001` | Registrar producto | `PM` | [`specs/pm/001-registrar-producto/`](specs/pm/001-registrar-producto/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 16 unitarias y 16 de integración en verde (`./mvnw clean verify`, 27-08-2026), incluidas las dos concurrentes | **En desarrollo** |
| `RF-PM-002` | Consultar productos | `PM` | [`specs/pm/002-consultar-productos/`](specs/pm/002-consultar-productos/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 16 unitarias y 29 de integración en verde (`./mvnw clean verify`, 27-08-2026), incluidas las dos de plan con volumen sembrado | **En desarrollo** |
| `RF-PM-003` | Consultar el detalle de un producto | `PM` | [`specs/pm/003-consultar-detalle-producto/`](specs/pm/003-consultar-detalle-producto/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 5 unitarias y 25 de integración en verde (`./mvnw clean verify`, 27-08-2026), incluidas las de la lectura estrecha de `shared/audit` | **En desarrollo** |
| `RF-PM-004` | Editar producto | `PM` | [`specs/pm/004-editar-producto/`](specs/pm/004-editar-producto/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 11 unitarias y 32 de integración en verde (`./mvnw verify`, 27-08-2026), incluidas dos concurrentes | **En desarrollo** |
| `RF-PM-005` | Cambiar el estado de un producto | `PM` | [`specs/pm/005-cambiar-estado-producto/`](specs/pm/005-cambiar-estado-producto/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 4 unitarias y 27 de integración en verde (`./mvnw verify`, 27-08-2026), incluidas **tres concurrentes** | **En desarrollo** |
| `RF-PM-006` | Eliminar producto | `PM` | [`specs/pm/006-eliminar-producto/`](specs/pm/006-eliminar-producto/tasks.md) | Pendiente de crear | `feature/esqueleto-del-proyecto` | 14 unitarias y 15 de integración en verde (`./mvnw verify`, 27-08-2026), incluidas dos concurrentes | **En desarrollo** |
| `RF-PM-007` | Consultar la oferta disponible para uno mismo | `PM` | [`specs/pm/007-consultar-oferta-propia/`](specs/pm/007-consultar-oferta-propia/tasks.md) | Pendiente de crear | — | — | **Tasks aprobadas** |
| `RF-CM-001` | Registrar una tarifa de comisión | `CM` | [`specs/cm/001-registrar-tarifa-comision/`](specs/cm/001-registrar-tarifa-comision/spec.md) | Pendiente de crear | `feature/modulo-comisiones` | — | **Spec en revisión** |
| `RF-CM-002` | Consultar las tarifas de comisión | `CM` | [`specs/cm/002-consultar-tarifas-comision/`](specs/cm/002-consultar-tarifas-comision/spec.md) | Pendiente de crear | `feature/modulo-comisiones` | — | **Spec en revisión** |
| `RF-CM-003` | Corregir una tarifa: su porcentaje y su fin de vigencia | `CM` | [`specs/cm/003-corregir-tarifa-comision/`](specs/cm/003-corregir-tarifa-comision/spec.md) | Pendiente de crear | `feature/modulo-comisiones` | — | **Spec en revisión** |
| `RF-CM-004` | Retirar una tarifa de comisión | `CM` | [`specs/cm/004-retirar-tarifa-comision/`](specs/cm/004-retirar-tarifa-comision/spec.md) | Pendiente de crear | `feature/modulo-comisiones` | — | **Spec en revisión** |
| `RF-CM-005` | Consultar la comisión efectiva de una persona sobre un producto en una fecha | `CM` | [`specs/cm/005-consultar-comision-efectiva/`](specs/cm/005-consultar-comision-efectiva/spec.md) | Pendiente de crear | `feature/modulo-comisiones` | — | **Spec en revisión** |


**Estados**, que reflejan las tres compuertas del Art. I.6:

| Estado | Significa |
|---|---|
| `Pendiente` | Registrado, sin `spec.md` |
| `Spec en revisión` | `spec.md` escrita, en su Pull Request |
| `Spec aprobada` | Compuerta 1 superada; puede escribirse `plan.md` |
| `Plan aprobado` | Compuerta 2 superada; pueden escribirse las tareas |
| `Tasks aprobadas` | Compuerta 3 superada; puede escribirse código |
| `En desarrollo` | Implementación en curso |
| `Implementado` | Integrado y con la definición de terminado cumplida |
| `Descartado` | Retirado; su número queda consumido |

Un requerimiento solo pasa a `Implementado` cuando cumple **todas** las condiciones de la definición de terminado (§16 de la constitución).

---

## 5. Estado general

| Indicador | Valor |
|---|---|
| Requerimientos registrados | 54 |
| Requerimientos con `spec.md` redactada | 54 |
| Requerimientos con `spec.md` aprobada | 49 |
| Requerimientos con `plan.md` aprobado | 49 |
| Requerimientos con **tripleta completa** —`tasks.md` aprobadas— | 49 |
| Requerimientos con endpoint funcionando | 48 |
| Requerimientos implementados | 0 |

La vista **por módulo** de estos mismos indicadores está en la [portada](index.md#indicadores), que los cruza con el inventario de [`modules.md` §4](modules.md#4-inventario-de-modulos). Aquí se cuenta por compuerta; allí se cuenta por módulo.

**De los 54, cuarenta y dos son de `SP`, siete de `PM` y cinco de `CM`.** Los cinco de `CM` se registraron el 28-08-2026 y sus cinco `spec.md` se redactaron el mismo día, **ninguna aprobada todavía**: por eso la segunda fila sube a 54 y las tres siguientes se quedan en 49. Ese desfase es exactamente el trabajo pendiente del módulo nuevo — la primera compuerta del Art. I.6. Las siete `spec.md` de `PM` se redactaron el 26-08-2026. **Las siete están aprobadas**, y con ellas los 49 requerimientos del sistema tienen `spec.md` aprobada: las veintisiete preguntas abiertas del módulo quedaron resueltas el 26-08-2026. **Ninguno tiene bloqueada la segunda compuerta**: D-25 se cerró el mismo día. Las cuatro filas de la tabla que van de `plan.md` hacia abajo cuentan **solo los de `SP`**. El módulo nace además con **D-25** abierta, que bloquea el `plan.md` de dos de ellos y no sus especificaciones ([`requirements/pm.md` §3](requirements/pm.md)).


**Estado al 26-08-2026.** Los cuarenta y dos requerimientos del módulo tienen **tripleta completa y aprobada** —`spec.md`, `plan.md` y `tasks.md`— y **los cuarenta y dos tienen endpoint funcionando**. El último fue `RF-SP-040`, que entró ese mismo día al cerrarse **D-23**. `./mvnw clean verify` termina en verde con **154 pruebas unitarias y 602 de integración**.

**Ninguno está `Implementado`, y el cero es correcto.** El Art. XVI exige Pull Request aprobado e integrado, y el trabajo vive todavía en ramas `feature/…` sin fusionar: las cuarenta y dos filas de §4 dicen «Issue: pendiente de crear». Lo que separa al módulo de darse por terminado ya no es construirlo, es **cerrarlo** — las tareas abiertas que siguen, los Issues y los Pull Requests.

**Las catorce tareas que siguen abiertas**, verificadas contra el código el 26-08-2026:

| Tarea | Qué falta |
|---|---|
| `RF-SP-010` · `T-10` y `T-11` | Las pruebas de API del catálogo de permisos —`CA-SP-073` a `CA-SP-077` y los casos límite—. El endpoint existe y está en el contrato; lo que no existe es la prueba que lo ejercita a través de HTTP |
| `RF-SP-017` · `T-06` | La prueba de la cadena de membresías rota |
| ~~`RF-SP-021` · `T-11`~~ | ~~El `EXPLAIN` de `ix_countries_busqueda`~~ · **Hecha el 27-08-2026** con una salvedad declarada: no se puede afirmar que el planificador lo **prefiera** con doscientas cincuenta filas, y la prueba afirma que el predicado **se puede resolver** con él |
| `RF-SP-023` · `T-04` | Traducir la violación de `ck_currencies_default_active` **por nombre de restricción** y no por el texto del driver |
| `RF-SP-023` · `T-10` | Actualizar `CA-SP-131`: `/{id}/status` responde `405` y no `404` a los métodos distintos de `PATCH` |
| `RF-SP-024` · `T-13` | La lista real de contraseñas comunes leída del empaquetado |
| `RF-SP-025` · `T-14` | Las pruebas de `EXPLAIN` de `ix_users_busqueda` y del filtro por membresía |
| `RF-SP-026` · ~~`T-12`~~ y `T-13` | ~~La prueba de número de sentencias del detalle~~ · **Hecha el 27-08-2026, y desmiente al plan: son cuatro sentencias y no dos.** Sigue pendiente la regla de ArchUnit que impide que `application` importe el adaptador de caché |
| `RF-SP-027` · `T-13` | La prueba de número de sentencias de la edición |
| `RF-SP-029` · `T-13` | La prueba de que `RF-SP-009` no necesitó cambios al eliminarse una persona |
| `RF-SP-034` · `T-15` | Las dos pruebas de temporización, `CA-SP-293` y `CA-SP-377` |
| `RF-SP-036` · `T-05` | El límite de tasa de `/api/v1/auth/logout`: `RateLimitFilter` cubre login, refresco y las dos rutas de recuperación, y **no** el cierre de sesión |

!!! warning "Los estados de tarea de las tripletas van por detrás"

    Las de arriba son las que sus `tasks.md` declaran `Pendiente`. **Dos se cerraron el 27-08-2026** y quedan tachadas en su sitio en lugar de borradas, porque las dos traen una salvedad que conviene que siga leyéndose. Hay además **ochenta y siete filas en `En curso`** repartidas por veinticinco tripletas cuyo trabajo, a la vista del código y de la suite en verde, está en buena parte hecho —`RF-SP-034` · `T-03` es el ejemplo: `LockoutPolicy` existe, tiene prueba propia y el control de cambios la dio por cerrada el 24-08-2026—. **No se han tocado aquí**: darlas por hechas exige comprobar cada una contra su verificación escrita, y eso es una revisión con su propio pase, no una corrección de matriz. Hasta que se haga, el estado de una tarea **no** es fuente fiable y sí lo son §4 de este documento y la suite.

Los 42 corresponden al módulo `SP`, que absorbió los usuarios, sus roles y su acceso al retirarse `USR` (`modules.md` v0.9.0). **Los cuarenta y dos tienen `spec.md` y los cuarenta y dos han superado la primera compuerta** del Art. I.6: treinta y ocho el 21-08-2026, y `RF-SP-041`, `RF-SP-039`, `RF-SP-042` y `RF-SP-040` el 22-08-2026. No queda ninguna pregunta abierta en el módulo. **Los cuarenta y dos han superado también la segunda compuerta**, y con ella **las cuarenta y dos tripletas del módulo están completas**: `RF-SP-001` a `RF-SP-023` antes del 22-08-2026, `RF-SP-024` a `RF-SP-033` ese mismo día, y `RF-SP-034` a `RF-SP-042` el 24-08-2026. El de `RF-SP-040` se escribió **pese a D-23**: la forma del canal ya estaba decidida en `architecture.md` §15.1, de modo que la decisión pendiente bloquea **una sola tarea** —el adaptador de envío— y no el requerimiento entero. Ese requerimiento no puede darse por terminado hasta cerrarla.

**`RF-SP-040` ya está implementada, y D-23 cerrada.** El 22-08-2026 se decidió la **forma** del canal de envío —infraestructura transversal con puerto publicado, ni submódulo ni módulo aparte (`architecture.md` §15.1)—, y eso bastó para cerrar su especificación; el **mecanismo** —proveedor, desacople, reintentos y rebotes— quedó registrado como **D-23** y bloqueó su `plan.md` hasta el 26-08-2026, cuando se cerró con **Resend por su API HTTP y no por SMTP** (`architecture.md` §15.1, v0.17.0). Fue el último requerimiento del módulo sin endpoint. La previsión de su plan funcionó: la decisión estaba aislada en **una sola tarea** —el adaptador de envío—, de modo que las otras trece se construyeron contra el puerto `NotificationSender` y el adaptador entró al final sin tocar ninguna.

Las diecisiete especificaciones que faltaban se redactaron el 21-08-2026 con **sesenta y cuatro preguntas abiertas**, y las sesenta y cuatro quedaron resueltas. Lo que sigue es lo que esas resoluciones fijaron para todo el módulo, porque ninguna se agota en su propio requerimiento.

**Cambio de estado de un catálogo** (`RF-SP-022`, `RF-SP-023`). Se registra solo en la auditoría de cambios y no en la de seguridad, porque no hay privilegio en juego; no se exige motivo, porque el Art. V.13 lo obliga solo en las eliminaciones; el elemento se desactiva aunque tenga datos que lo referencien o importes expresados en él, porque impedirlo dejaría sin salida justo al caso que más urge retirar; y no se devuelve el conteo de esas referencias. No se crea todavía un requerimiento para cambiar la moneda por defecto: mientras haya una sola no hay nada que elegir.

**Identidad de las personas** (`RF-SP-024`). Cada persona lleva **dos identidades y con cualquiera de las dos inicia sesión**: el correo, que puede corregirse y cuyo cambio emite evento de seguridad por ser una vía de acceso, y el **nombre de usuario**, inmutable, sin el carácter `@` —lo que impide que un valor se confunda con el otro— y que es el que la auditoría referencia. El alta fija la contraseña inicial y deja la cuenta `ACTIVO` y **marcada para cambio obligatorio**; `PENDIENTE` queda declarado y sin usar.

**El rol de consumidor y la membresía son inseparables** (`RN-SP-018`, nacida al aprobar `RF-SP-033`). No existe el estado «consumidor sin nivel»: se conceden juntos —`RF-SP-024` y `RF-SP-030` exigen indicar la membresía al dar el primer rol de consumidor— y se sueltan juntos —`RF-SP-031` la retira al quitar el último—. Resolverlo de otro modo producía un bloqueo mutuo del que nadie podía salir, y obligó a devolver a su compuerta tres especificaciones ya aprobadas (Art. I.7). La membresía admite además **fecha de fin**, cuya vigencia se evalúa al consultarla, sin proceso que la retire.

**Retirar el acceso es inmediato; concederlo puede esperar** (`RF-SP-028`, `RF-SP-030`, `RF-SP-031`). Desactivar, bloquear o retirar un rol revoca las sesiones de la persona y su token de acceso deja de admitirse; asignar un rol acepta la latencia de hasta quince minutos que `security.md` §4.5 declara. `INACTIVO` y `BLOQUEADO` pasan a tener significados separados —organizativo frente a seguridad—, el bloqueo admite origen **manual** sin expiración, y retirar el acceso exige motivo mientras que devolverlo no lo admite.

**Sesiones** (`RF-SP-034` a `RF-SP-038`). Bloqueo a los cinco intentos con progresión y techo, **avisando en cada rechazo cuántos intentos quedan** —y contando también los de un identificador sin cuenta, para que el número no delate cuáles existen (`spec.md` v0.2.0)—; la cuenta bloqueada recibe respuesta distinta, como excepción consciente al mensaje genérico, y **entrega su expiración como dato y no escrita en el mensaje**, para que el cliente pueda descontarla; cada revocación registra su **motivo**, y solo la rotación dispara la alarma de robo; la familia de tokens tiene una **duración máxima de sesión** contada desde el inicio; el cierre de sesión es público y admite cerrar todas; y la credencial provisional que fija un administrador **caduca**.

**Quién está a cargo de quién** (`RF-SP-041`, `RF-SP-042`, registrados el 22-08-2026). La estructura comercial **persona → persona** deja de estar aparcada y pasa a tener tabla propia, `user_supervisors`, con historial. Todo vendedor declara un superior salvo la cúspide, y ese superior debe portar el rol padre inmediato del rol del subordinado (`RN-SP-020`): la jerarquía de personas ya no puede contradecir la de roles. Obligó a devolver a su compuerta **cinco** especificaciones aprobadas (Art. I.7): las tres que ya tocó `RN-SP-018` —`RF-SP-024`, `RF-SP-030` y `RF-SP-031`— más `RF-SP-028` y `RF-SP-029`, porque `RN-SP-022` impide retirar el acceso o eliminar a quien tiene gente a cargo. **Lo que no cambia es el alcance de datos:** registrar la estructura no concede a nadie visibilidad sobre los datos de su equipo, y **D-22 sigue abierta**.

**Lo que la interfaz puede saber de quien mira** (`RF-SP-039`, aprobada el 22-08-2026). El sistema publica por fin los **permisos efectivos del actor** en un endpoint sin permiso exigido y **sin parámetro alguno** —el actor y solo el actor—, que es lo que permite a una interfaz decidir qué mostrar sin duplicar en el navegador una regla que vive en el servidor. Cierra el hallazgo `DF-04` de la documentación del frontend. Fija además tres límites del módulo: el perfil propio devuelve **solo el superior comercial**, nunca el equipo, porque a quién reporta uno es un dato del actor y quiénes dependen de uno es un conjunto de terceros —la distinción que sostiene la reserva de D-22—; el último inicio de sesión se devuelve **como dato informativo y no como señal de intrusión**, porque `RF-SP-034` sobrescribe ese valor al entrar y conservar el anterior habría obligado a reabrirla (Art. I.7); y **la autoedición del propio perfil no existe ni se registra**: quien necesite corregir su correo o su nombre se lo pide a quien administra usuarios (`RF-SP-027`). Este último es un hueco aceptado a conciencia, con su síntoma declarado —tráfico de soporte por correcciones triviales— como condición para reabrirlo.

**Lo que sigue abierto.** El canal de envío ya tiene mecanismo desde el 26-08-2026, y con él dejaron de estar en el aire las tres cosas que colgaban de **D-23**: `RF-SP-040` está implementada, y la verificación del correo al cambiarlo (`RF-SP-027`) y el aviso a quien le restablecen la contraseña (`RF-SP-038`) tienen ya por dónde salir — ninguna de las dos se ha escrito todavía, y siguen anotadas como riesgo con su condición de disparo. `RF-SP-027` y `RF-SP-038` **no se reabrieron** (Art. I.7): se decidió el 22-08-2026 revisarlas al escribir sus planes, cosa que ya ocurrió, de modo que quien quiera esos dos envíos debe registrarlos como cambio y no darlos por incluidos. Del adaptador quedan además **tres restos declarados**: el remitente debe pertenecer a un dominio verificado o el proveedor responde `403` que no se ve hasta producción; sin credencial el envío queda apagado y se avisa **al arrancar**; y **no hay reintento propio**, de modo que un fallo de la llamada pierde ese mensaje y solo deja su registro.

Sigue abierta además **D-22** —el alcance de datos por persona—, que es lo que mantiene fuera de `RF-SP-042` el árbol descendente y la variante «mi equipo», y fuera de `RF-SP-039` cualquier lectura hacia abajo.


**`RF-SP-001` cruza la tercera compuerta y se implementa entero.** Sus `tasks.md` se aprobaron el 22-08-2026 y sus **veintidós tareas están hechas**: las cuatro migraciones (`V4` a `V7`), la infraestructura compartida que todo módulo posterior reutiliza —los cuatro registros de auditoría con su mecánica transaccional, la jerarquía de errores, el generador de identificadores, el contexto de origen de la petición y la publicación del actor autenticado— y el caso de uso completo con sus once criterios de aceptación en verde. **No pasó a `Implementado`** por el bloqueo 3 de sus tareas: `RN-SEG-010` leía los permisos efectivos del `Authentication` y no de la base de datos, porque `users` y `user_roles` los crea `RF-SP-024`. **Ese bloqueo se cerró el 24-08-2026** al implementarse `RF-SP-024`: los permisos efectivos se resuelven ya contra la base en cada petición, de modo que un rol desactivado deja de conceder de inmediato. Lo único que hoy lo separa de `Implementado` es el Pull Request. Al ejecutarlas se resolvió además la contradicción que `architecture.md` §5.1 dejó planteada el 22-08-2026 —`T-11`, `T-12` y `T-20` estaban escritas sobre la disposición de capas anterior— por la vía que ese documento admite, y se corrigió `ck_deletion_reason`, cuyo `OR` con un motivo nulo evaluaba a `NULL` y hacía que un `CHECK` aceptara la fila.

**`RF-SP-010` fue el primero que cruzó la tercera compuerta.** Sus `tasks.md` se aprobaron el 22-08-2026 y su implementación está **completa salvo sus pruebas de API**: el catálogo se sirve en `GET /api/v1/permissions` con `permissions:read` sobre el método y sin ningún manejador de escritura —`RN-SP-004` se cumple **no escribiendo código**, y un `POST` obtiene el `405` de Spring—, y el endpoint está documentado en el contrato. Lo que falta son `T-10` y `T-11`: nadie ejercita `CA-SP-073` a `CA-SP-077` ni los casos límite a través de HTTP; lo que hay son pruebas del caso de uso y del repositorio. Al aprobar sus tareas se enmendó `plan.md` §2 para sembrar **veinticuatro** permisos y no veintitrés: faltaba `users:assign-supervisor`, que `requirements/sp.md` §9 declara desde el 22-08-2026 y que este plan, aprobado el 21, no alcanzó a recoger.

La **segunda** compuerta la cruzaron por bloques: los veintitrés primeros `plan.md` se aprobaron el 21-08-2026 y el de `RF-SP-024` el 22-08-2026 —sus `tasks.md`, que entonces estaban en revisión, quedaron todas aprobadas entre el 22 y el 26-08-2026—. Ese último es el que **crea el sujeto del módulo**: hasta él, `SP` tenía roles, permisos y catálogos, y el `actor_id` de los cuatro registros de auditoría no resolvía a ninguna fila. La matriz arrastraba nueve filas —`RF-SP-014` a `RF-SP-019`, `RF-SP-021`, `RF-SP-022` y `RF-SP-023`— marcadas como «Spec aprobada» cuando sus planes ya estaban aprobados en su propio documento; se corrigió el 22-08-2026, que es lo que el Art. III.6 exige hacer **con** el cambio y no después. El de `RF-SP-020` es el primero que toca `countries`: crea la tabla en `V16`, le añade `updated_at` —que `requirements/sp.md` §10.6 omitía pese al Art. V.7— y declara la unicidad del nombre sobre `f_unaccent(lower(name))` y no sobre el literal, porque `RN-SP-009` no admite edición y un `Panamá`/`Panama` duplicado sería permanente. El de `RF-SP-011` amplió `PageResponse<T>` con `totalIsExact`, porque los cuatro listados de auditoría cuentan hasta un techo configurable en lugar de recorrer una tabla que crece sin límite; los listados de conteo exacto lo devuelven siempre en `true`. La aprobación del plan de `RF-SP-010` corrió la numeración de las migraciones —`V1` pasa a ser las funciones compartidas y todas las demás avanzan un número—, porque ese requerimiento se implementa primero y necesita `f_unaccent` antes que `RF-SP-002`, que era quien la creaba. Enmendó también `security.md` §4.4, cuyo catálogo omitía cuatro bloques de permisos, y fijó que cada módulo siembra los suyos.

La aprobación de los nueve primeros devolvió además cinco especificaciones a su compuerta (Art. I.7): `RF-SP-005`, `RF-SP-008` y `RF-SP-009` ganaron `EX-006` —ninguna declaraba la excepción del rol inexistente, que los planes referenciaban con el código de otra— y `RF-SP-006` ganó `VAL-004`, el límite de cien permisos por petición que su plan aplicaba sin respaldo. Enmendaron también `requirements/sp.md` (`CLIENTE` y la longitud de `description`), `architecture.md` (serie `INT-nnn`) y `development-guide.md` (`422` y convención `f_`).

El inventario y el estado de los módulos se consultan en [`modules.md` §4](modules.md#4-inventario-de-modulos).

---

## 6. Control de cambios

| Versión | Fecha | Cambio | Responsable |
|---|---|---|---|
| 0.76.0 | 27-08-2026 | **El issue #27 queda cerrado: las siete pruebas que faltaban están escritas.** Ninguna era decorativa y tres destaparon algo. (1) `CA-SP-161`: el bloqueo global de la jerarquía **no existía** —ver 0.75.0—. (2) `RF-SP-026` · `T-12`: el detalle de una persona cuesta **cuatro** sentencias y `plan.md` §166 promete dos; lo que sí se cumple es que no crece con los roles, y la cifra se fija para que no suba en silencio. (3) `RF-SP-021` · `T-11`: el `EXPLAIN` **no puede** afirmar que el planificador prefiera el índice —con doscientas cincuenta filas prefiere el recorrido, y acierta—, de modo que la prueba afirma que el predicado **se puede resolver** con él y una segunda deja escrito que el recorrido secuencial no es un defecto. Las otras cuatro —concurrencia de sesión, dobles del orden de verificación, fallo al revocar sesiones y registro de rechazos— confirmaron lo construido. Con ellas la suite pasa de 732 a **1005 pruebas**. Se publican además tres **ADR en propuesta** para las tres decisiones abiertas que quedaban: retención (**D-10**), métricas y alertas (#43) y alcance de datos (**D-22**). | Responsable técnico |
| 0.75.0 | 27-08-2026 | **Dos huecos del issue #27 que resultaron ser más que un hueco de pruebas.** (1) `CA-SP-161` ya tiene prueba concurrente, y al escribirla se descubrió que **el mecanismo que debía protegerlo no existía**: `RF-SP-008` daba por hechas `T-03` y `T-04` —el puerto y el adaptador del bloqueo global de la jerarquía— y no había ninguno de los dos. Lo único que serializaba era el bloqueo pesimista **de fila**, que no cubre este caso porque las dos reubicaciones que cierran un ciclo mueven roles **distintos** y no comparten fila. Contra el código tal como estaba, la primera ronda de la prueba devolvió `200` a las dos peticiones y dejó el ciclo escrito en la base de datos. Se construyeron las tres piezas y se dejó declarada una contradicción **abierta** del plan sobre la severidad del rechazo por bloqueo. (2) **La mitad de código de D-21**: `ClientIpResolver` admite ahora **rangos CIDR**, que es lo que [`ADR-002`](architecture/ADR-002-plataforma-de-despliegue-railway.md) dejó como única salida para el Art. V.15 en Railway —no hay dirección de borde fija que declarar, pero la red de la que sale sí—. Con ello D-21 vuelve a ser una decisión de configuración y no de diseño. El resolvedor gana además dos propiedades por seguridad: **no resuelve nombres** desde `X-Forwarded-For` y **no arranca con una lista que no entiende**. | Responsable técnico |
| 0.1.0 | 20-08-2026 | Creación inicial con catálogo de módulos y matriz de trazabilidad. | Responsable técnico |
| 0.2.0 | 20-08-2026 | El inventario de módulos se traslada a `modules.md`, que pasa a ser su autoridad única. Este documento conserva nomenclatura y trazabilidad. | Responsable técnico |
| 0.3.0 | 20-08-2026 | Se retira el identificador `SPEC-[MÓDULO]-NNN` por redundante con el `RF`. La matriz refleja las tres compuertas de aprobación de la tripleta. | Responsable técnico |
| 0.4.0 | 20-08-2026 | §3.1 documenta el espacio de reglas transversales `RN-SEG` y deja las historias de usuario fuera de la cadena de trazabilidad. | Responsable técnico |
| 0.5.0 | 20-08-2026 | Se registran en la matriz los 21 requerimientos del módulo `SP`, cuyo documento queda aprobado. | Responsable técnico |
| 0.6.0 | 20-08-2026 | Los 21 requerimientos de `SP` pasan a estado «Spec en revisión»: su especificación está redactada y pendiente de la primera compuerta. | Responsable técnico |
| 0.7.0 | 21-08-2026 | De `RF-SP-010` a `RF-SP-021` superan la primera compuerta: sus 35 preguntas abiertas quedan resueltas y sus especificaciones aprobadas. Se registran `RF-SP-022` y `RF-SP-023`, derivados de esas resoluciones. | Responsable técnico |
| 0.8.0 | 21-08-2026 | Se registran los quince requerimientos que `SP` absorbe al desaparecer `USR`. | Responsable técnico |
| 0.9.0 | 21-08-2026 | Se redactan las diecisiete especificaciones que faltaban, de `RF-SP-022` a `RF-SP-038`, y pasan a estado «Spec en revisión» con 64 preguntas abiertas. Todo requerimiento registrado tiene ya su `spec.md`. | Responsable técnico |
| 0.10.0 | 21-08-2026 | `RF-SP-022` y `RF-SP-023` superan la primera compuerta. Entre ambas fijan el criterio del módulo para el cambio de estado de un catálogo: solo auditoría de cambios, sin motivo, sin conteo de referencias y sin rechazar el elemento ya referenciado. Se descarta por ahora un requerimiento para cambiar la moneda por defecto. | Responsable técnico |
| 0.11.0 | 21-08-2026 | `RF-SP-024` supera la primera compuerta y fija la identidad de las personas: nombre de usuario y correo, ambos válidos para iniciar sesión, el primero inmutable y sin `@`. El alta fija la contraseña inicial y marca la cuenta para cambio obligatorio. Enmienda `RN-SP-016`, que se contradecía a sí misma (`requirements/sp.md` v1.4.0). | Responsable técnico |
| 0.12.0 | 21-08-2026 | `RF-SP-025` y `RF-SP-026` superan la primera compuerta. El listado devuelve los roles completos de cada persona y admite búsqueda por fragmento de correo; el detalle resuelve los permisos efectivos y no expone el contador de intentos fallidos. Se registra `RF-SP-039`, consultar el propio perfil, derivado de esa segunda aprobación. | Responsable técnico |
| 0.13.0 | 21-08-2026 | `RF-SP-027` supera la primera compuerta. El correo es editable y el anterior queda **liberado** —la reserva permanente alcanza solo a la eliminación—, su cambio emite evento de seguridad por ser una vía de acceso, y la verificación del correo queda como riesgo con condición de disparo. Enmienda `RN-SP-016` y el catálogo cerrado de `security.md` §8.1. | Responsable técnico |
| 0.14.0 | 21-08-2026 | `RF-SP-028` supera la primera compuerta. Se admite el **bloqueo manual** de una cuenta, sin expiración, y `INACTIVO` y `BLOQUEADO` pasan a tener significados separados —organizativo frente a seguridad—. Retirar el acceso exige motivo; devolverlo no lo admite. Enmienda `RN-SP-001`, que se mide sobre usuarios **activos**, y el catálogo cerrado de `security.md` §8.1. | Responsable técnico |
| 0.15.0 | 21-08-2026 | `RF-SP-029` supera la primera compuerta. Eliminar a una persona **retira sus roles y su membresía**, con lo que un rol que solo portaba alguien eliminado vuelve a poder borrarse sin que `RF-SP-009` necesite enmienda. La auditoría de eliminación pasa a ser la única fuente de qué tenía esa persona, y captura el estado **antes** de retirar las asignaciones. La anonimización queda como riesgo por decidir en `docs/security/`. | Responsable técnico |
| 0.16.0 | 21-08-2026 | `RF-SP-030` y `RF-SP-031` superan la primera compuerta. `RN-SEG-010` se comprueba **comparando permisos** y alcanza también al retiro. Asignar no revoca sesiones y retirar sí: asimetría deliberada que enmienda la tabla de latencias de `security.md` §4.5. Sin tope de roles por persona; el riesgo del tamaño del token se anota en el plan. | Responsable técnico |
| 0.17.0 | 21-08-2026 | `RF-SP-032` y `RF-SP-033` superan la primera compuerta. La membresía admite **fecha de fin**, cuya vigencia se evalúa al consultarla sin proceso que la retire. Nace `RN-SP-018` —todo consumidor tiene membresía—, que devuelve a su compuerta `RF-SP-024`, `RF-SP-030` y `RF-SP-031` \(Art. I.7\). `RF-SP-033` queda como operación correctiva. | Responsable técnico |
| 0.18.0 | 21-08-2026 | `RF-SP-034` supera la primera compuerta. Se entra con nombre de usuario **o** correo; bloqueo a los cinco intentos con progresión y techo; la cuenta bloqueada recibe respuesta distinta \(excepción consciente\); la contraseña con cambio obligatorio **autentica y advierte**; sin tope de sesiones simultáneas. | Responsable técnico |
| 0.19.0 | 21-08-2026 | `RF-SP-036`, `RF-SP-037` y `RF-SP-038` superan la primera compuerta, y con ellas **las treinta y ocho especificaciones del módulo quedan aprobadas**: no queda ninguna pregunta abierta. El cierre de sesión pasa a público y admite cerrar todas; los intentos fallidos al cambiar la contraseña cuentan para el bloqueo; la credencial provisional caduca. Se registra `RF-SP-040`, restablecer la propia contraseña olvidada. | Responsable técnico |
| 0.20.0 | 22-08-2026 | Se registran `RF-SP-041` y `RF-SP-042`: la **estructura comercial persona → persona** deja de estar aparcada y pasa a `user_supervisors`, con su historial y sus reglas `RN-SP-019` a `RN-SP-022`. Devuelve a su compuerta cinco specs aprobadas \(Art. I.7\): `RF-SP-024`, `RF-SP-028`, `RF-SP-029`, `RF-SP-030` y `RF-SP-031`. **Corrección de la matriz:** nueve filas —`RF-SP-014` a `RF-SP-019`, `RF-SP-021` a `RF-SP-023`— figuraban en «Spec aprobada» con sus planes ya aprobados; pasan a «Plan aprobado», y §5 incorpora el indicador de planes aprobados para que el desfase vuelva a ser visible de un vistazo. D-22 sigue abierta: la estructura se registra, no concede alcance. | Responsable técnico |
| 0.21.0 | 22-08-2026 | `RF-SP-041` supera la primera compuerta, y con ella son **treinta y nueve** las specs aprobadas. El cambio de superior exige **motivo**, **no admite fecha declarada** —rige desde que se ejecuta— y **no emite evento de seguridad**, con condición de disparo anotada para cuando D-22 haga depender el alcance de datos de esta relación. `RN-SP-017` se amplía al cambio de superior: es la tercera operación en que el actor tendría interés directo sobre su propia cuenta. | Responsable técnico |
| 0.22.0 | 22-08-2026 | Se redactan las tres especificaciones que faltaban —`RF-SP-039`, `RF-SP-040` y `RF-SP-042`—, con lo que **los cuarenta y dos requerimientos del módulo tienen `spec.md`**. Las tres quedan **en revisión**, con doce preguntas abiertas entre ellas. `RF-SP-040` se redacta sabiendo que **no puede aprobarse**: depende del canal de envío, que no existe en ningún requerimiento y cuya forma de incorporación —RNF, submódulo de `SP` o módulo aparte— es su pregunta 1. | Responsable técnico |
| 0.23.0 | 22-08-2026 | `RF-SP-039` supera la primera compuerta, y con ella son **cuarenta** las specs aprobadas. El perfil propio publica los **permisos efectivos del actor** sin exigir permiso y sin admitir parámetro, con lo que la interfaz deja de tener que deducirlos: cierra el hallazgo `DF-04` del frontend. Devuelve **solo el superior comercial**, nunca el equipo —la distinción que sostiene la reserva de D-22—; el último inicio de sesión queda como dato **informativo**, no como señal de intrusión, al comprobarse que `RF-SP-034` lo sobrescribe en cada entrada y que conservar el anterior habría obligado a reabrirla \(Art. I.7\); y la **autoedición del propio perfil no existe ni se registra**, hueco aceptado a conciencia con su síntoma declarado. | Responsable técnico |
| 0.24.0 | 22-08-2026 | `RF-SP-042` supera la primera compuerta, y con ella son **cuarenta y una** las specs aprobadas: solo `RF-SP-040` queda por aprobar. Las cuatro resoluciones fueron restrictivas y en la misma dirección: **solo lo vigente** —sin historial de superiores—, **sin conteo de la rama indirecta** ni siquiera como número, **sin filtros** sobre el equipo directo, y la persona eliminada se trata como inexistente. El criterio común es no dejar que esta consulta se convierta en el sustituto informal del modelo de alcance que falta (**D-22**). Queda un hueco declarado: el historial que `RN-SP-021` conserva **no tiene ninguna vía de lectura** hasta que exista la auditoría del reparto de comisiones. | Responsable técnico |
| 0.25.0 | 22-08-2026 | `RF-SP-040` supera la primera compuerta y con ella **las cuarenta y dos especificaciones del módulo quedan aprobadas**: no queda ninguna pregunta abierta en todo `SP`. La resolución que lo desbloqueó no vive aquí: el **canal de envío** queda decidido como **infraestructura transversal con puerto publicado** \(`architecture.md` §15.1\), ni submódulo ni módulo propio, y cada módulo declara qué envía y cuándo. El envío es **desacoplado de la respuesta** \(`RNF-FIA-001`\) por seguridad, no por rendimiento. La vigencia del permiso temporal se declara en `security.md` §3.2. El restablecimiento **avisa al titular**, con lo que se cierra la condición de disparo de `RF-SP-038`. **Queda D-23**: el mecanismo concreto de envío, sin el cual no puede escribirse el `plan.md` de este requerimiento. | Responsable técnico |
| 0.26.0 | 22-08-2026 | **`RF-SP-024` supera la segunda compuerta**: es el primer plan del bloque de usuarios y el primero que crea el sujeto del módulo. Cinco migraciones, `V18` a `V22`: `users`, `user_roles`, `user_memberships`, `user_supervisors` y la **semilla del superadministrador**, cuya credencial entra por marcador de posición de Flyway —sin él la migración falla, en lugar de arrancar con una contraseña conocida—. Decisiones que alcanzan al módulo entero: la unicidad de nombre de usuario y correo es **total y no parcial**, porque `RN-SP-016` no libera nada al eliminar; el nombre de usuario **conserva su caja** y su unicidad va sobre `lower(username)`, lo que obliga a `RF-SP-034` a comparar sin distinguir mayúsculas; `RN-SEG-010` se **extrae a un componente único** compartido con `RF-SP-005` y `RF-SP-030`; y la política de contraseña gana la prohibición de contener el nombre de usuario o el correo \(`security.md` §3.2\). Enmienda `security.md` §8.1 —**el alta de un usuario entra en el catálogo cerrado de eventos**— y `requirements/sp.md` §10. **Corrige dos planes aprobados**: `user_roles` la crea este requerimiento y no `RF-SP-030`, porque el alta ya escribe asignaciones. | Responsable técnico |
| 0.27.0 | 22-08-2026 | **Revisión de los seis planes reabiertos** —`RF-SP-004` a `RF-SP-009`— y su reaprobación. La corrección que motivó la reapertura era real y queda verificada: `ck_audit_error_log_status` \(`RF-SP-013` §2\) prohíbe `400`, `401`, `403` y `404` en `audit_error_log`, y los seis mandaban ahí el `403` de `RN-SEG-011` y el `404` del rol inexistente; el `403` va a `audit_security_log` con severidad **Alta** y lo emite el caso de uso, no la capa de seguridad. **Un defecto nuevo en `RF-SP-005`:** su §2 afirmaba que la clave primaria compuesta absorbía la asignación concurrente «porque la segunda encuentra la fila ya presente». No es cierto —PostgreSQL lanza `23505` al confirmar la primera, y el caso salía como `500`—; se resuelve con `INSERT … ON CONFLICT DO NOTHING` en sentencia nativa, y `RF-SP-005` y `RF-SP-006` ganan la prueba de concurrencia que sus specs exigían en §13 y sus planes no tenían. **`RF-SP-014` se reabre y se reaprueba**: su `CHECK` de `event_type` era un dominio cerrado de dieciséis códigos que no incluía `USER_CREATED` —que `RF-SP-024` empieza a emitir— ni `EMAIL_CHANGED` —declarado en `security.md` §8.1 desde `RF-SP-027`—; con dieciocho, el evento del alta deja de ser una violación de integridad dentro de la transacción de auditoría. | Responsable técnico |
| 0.28.0 | 22-08-2026 | **Cinco planes nuevos y las tareas de `RF-SP-024`**: el submódulo de **Usuarios** queda con su tripleta completa \(`RF-SP-024` a `RF-SP-029`\). Cuatro decisiones alcanzan al módulo entero. **`deleted_at` nace con `users`, no con `RF-SP-029`**: `architecture.md` §6.4 la declara columna obligatoria de toda tabla de negocio y **diez requerimientos la leen antes de que alguien la escriba** —`RF-SP-003` y `RF-SP-009` ya la daban por existente—; corrige el `plan.md` de `RF-SP-024` \(Art. I.7\). **Las tres columnas de control de acceso son de `RF-SP-034`**, no repartidas con `RF-SP-028`, que solo las lee y las limpia. **`RF-SP-034` precede a `RF-SP-026`, `RF-SP-028` y `RF-SP-029`**: sin `refresh_tokens` no hay sesiones que revocar y tres criterios no son verificables; §6.1 de `requirements/sp.md` gana las precedencias del bloque. Y **`USER_DELETED` entra en el catálogo cerrado** de `security.md` §8.1 y en el `CHECK` de `V4`, que pasa a diecinueve literales: la baja de una persona no estaba en el catálogo pese a que la de un rol sí, y `RF-SP-014` §2 la había resuelto reutilizando `USER_STATUS_CHANGED`. Otras decisiones: el detalle de una persona resuelve sus permisos efectivos **con el mismo componente que autoriza**, para que la pantalla no pueda describir un sistema distinto del que atiende las peticiones; el corte inmediato del token de acceso se resuelve con un **registro de invalidación por usuario** sembrado al arrancar, no consultando el estado en cada petición; y `RN-SP-001` se serializa **bloqueando el conjunto de portadores activos del rol raíz**, no la fila del afectado, que era el caso que `RF-SP-028` §13 anticipaba. | Responsable técnico |
| 0.29.0 | 22-08-2026 | **El bloqueo del entorno se cierra y `RF-SP-010` avanza ocho tareas.** La estación de trabajo cumple ya `development-guide.md` §2.1 —JDK 21.0.12, Docker 29.7.2 y el **envoltorio de Maven** 3.9.9—, con lo que decae el bloqueo 3 de sus `tasks.md`, abierto por no haber JDK ni Maven instalados. `./mvnw clean verify` termina en verde: **14 pruebas unitarias y 38 de integración** sobre Testcontainers, incluidas las veinticuatro filas sembradas y las ocho de recurso `users`. `T-01` a `T-04` y `T-06` a `T-08` pasan a **Hecha**. Dos correcciones en esas tareas: la verificación de `T-08` decía «prueba de API» y lo que existe es una **prueba de serialización sobre el DTO** —el controlador no puede integrarse hasta `RF-SP-001`, bloqueo 5—, y `T-13` figuraba «En curso» dependiendo de `T-10`, que está **bloqueada**; pasa a **Bloqueada**. Se versionan además `mvnw`, `mvnw.cmd` y `.mvn/`, que estaban sin rastrear pese a ser la vía por la que el proyecto se construye, y nace `.gitattributes` para que `mvnw` salga siempre con finales de línea LF. | Responsable técnico |
| 0.30.0 | 22-08-2026 | **Cuatro planes nuevos con sus tareas** —`RF-SP-030` a `RF-SP-033`—: el submódulo de asignación de **roles y membresías** queda con su tripleta completa, y con él **treinta y tres** de los cuarenta y dos requerimientos han superado la segunda compuerta. Cuatro decisiones alcanzan más allá de su tripleta. **`RF-SP-031` cambia de verbo**: `DELETE` con la lista en el cuerpo pasa a `POST …/roles/revocations`, la misma corrección que `RF-SP-006` hizo el 21-08-2026 y por el mismo motivo — RFC 9110 no le define semántica al cuerpo de un `DELETE`, y un intermediario que lo descarte produce un retiro **sin roles**, en una operación que además revoca sesiones. `RF-SP-033` conserva el suyo: no lleva cuerpo. **Tres componentes de dominio quedan declarados compartidos** y ninguno puede escribirse dos veces: `RoleGrantPolicy` \(`RN-SEG-010`\), `RootRoleGuard` \(`RN-SP-001`, con el bloqueo sobre el conjunto de portadores activos dentro\) y `CommercialRank`, que resuelve el rol vendedor de **mayor rango** y es lo único que distingue un ascenso de una asignación lateral. **La definición de «vigente» de una membresía pasa a vivir en un solo componente de dominio**, probado sobre sus tres bordes, en lugar de repetirse como `WHERE` en las consultas de `RF-SP-026` y `RF-SP-031`. Y se enmienda `spec.md` de `RF-SP-030` §11 \(Art. I.7\): los cuatro casos condicionales de membresía y superior responden **`422` y no `400`**, porque ninguno se decide mirando solo el cuerpo — un `400` no debe poder salir del caso de uso, que es la frontera que hace legible el manejador global. `requirements/sp.md` sube a **1.18.0** con la corrección de §9. | Responsable técnico |
| 0.31.0 | 22-08-2026 | **`RF-SP-001` cruza la tercera compuerta y queda implementado**: sus `tasks.md` se aprueban y sus **veintidós tareas pasan a `Hecha`**. Entra con él la infraestructura que todo módulo posterior reutiliza —las migraciones `V4` a `V7`, los cuatro registros de auditoría con su mecánica transaccional, la jerarquía de errores con el manejador global, el generador UUID v7, el contexto de origen de la petición y la publicación del actor autenticado—. `./mvnw clean verify` en verde con **46 pruebas unitarias y 98 de integración**. **No pasa a `Implementado`**: `RN-SEG-010` lee los permisos efectivos del `Authentication` y no de la base de datos, porque `users` y `user_roles` los crea `RF-SP-024`; queda como bloqueo 3 de sus tareas. Se resuelve además la contradicción que `architecture.md` §5.1 dejó planteada —`T-11`, `T-12` y `T-20` estaban escritas sobre la disposición de capas anterior— por la vía que ese mismo documento admite: no hay `RoleEntity` ni `RoleJpaMapper`, el agregado es el modelo persistente y la regla de ArchUnit se acota a lo que §5.1 permite. **Y se corrige `ck_deletion_reason`**: tal como lo transcribe `architecture.md` §6.6.3, un motivo nulo daba `FALSE OR NULL` = `NULL` y el `CHECK` aceptaba la fila, de modo que la obligación del Art. V.13 no existía; `V4` la declara con `reason IS NOT NULL` y una prueba la ejercita. Queda anotado un hueco: **el contexto de origen de la petición no tiene tarea en ninguna tripleta** pese a que las cuatro tablas de auditoría lo exigen. **Y se descubre que el contrato OpenAPI no se generaba**: `/v3/api-docs` devolvía `500` por una incompatibilidad de springdoc 2.6.0 con Spring Framework 6.2, latente desde que se fijó la versión y visible solo al existir el primer `@ControllerAdvice`. Se sube a 2.8.9 y se añade `OpenApiContractIT` para que la ausencia de un endpoint en el contrato vuelva a ser un fallo y no un silencio (Art. VIII.2). | Responsable técnico |
| 0.31.0 | 24-08-2026 | **Tres planes nuevos con sus tareas** —`RF-SP-034` a `RF-SP-036`—: el submódulo de **autenticación y sesión** queda con su tripleta completa, y con él **treinta y seis** de los cuarenta y dos han superado la segunda compuerta. `RF-SP-034` es quien desbloquea el módulo entero: hasta que exista, `CurrentActor` devuelve siempre vacío y **ninguna prueba de API de ningún otro requerimiento puede ejercitar su camino feliz**. Crea `V26` —las tres columnas de control de acceso sobre `users`— y `V27` —`refresh_tokens`—, y con ella el `SessionRevoker` que `RF-SP-028`, `RF-SP-029`, `RF-SP-031` y `RF-SP-037` esperaban. Cuatro decisiones alcanzan más allá de su tripleta. **El token de acceso gana el claim `mcp`** \(`security.md` §5.2, enmendada\): aplicar el cambio obligatorio de contraseña sin él exige leer la base de datos **en cada petición**, que es la consulta que D-08 existe para evitar. **`revoked_reason` es obligatorio en el esquema**, con `CHECK` y dominio cerrado de seis literales: es lo único que separa un robo de un cierre de sesión, y sin él `RF-SP-035` llenaría el registro de seguridad de incidentes falsos hasta volverlo inútil. **La verificación de contraseña se ejecuta también contra cuentas inexistentes**, contra un hash de descarte: saltarla no produce ningún fallo visible y abre la enumeración de cuentas por temporización, de modo que `CA-SP-293` y `CA-SP-377` se escriben como pruebas de tiempo con medianas sobre repeticiones — y no se desactivan aunque resulten incómodas. Y se enmienda `spec.md` de `RF-SP-036` §10 \(Art. I.7\): un token no reconocido devuelve **`204`** y no un rechazo, porque `EX-001` y `FA-001` juntos permitían comprobar en dos peticiones si una cadena de texto era un token real. Queda declarado un **hueco del módulo**: la purga de tokens que `security.md` §5.5 exige **no tiene requerimiento**. | Responsable técnico |
| 0.32.0 | 24-08-2026 | **Seis planes nuevos con sus tareas** —`RF-SP-037` a `RF-SP-042`—: contraseñas, perfil propio y estructura comercial. Con ellos **las cuarenta y dos tripletas del módulo quedan completas** y los cuarenta y dos requerimientos han superado la segunda compuerta. **`RF-SP-040` se planifica pese a D-23**: `architecture.md` §15.1 ya decidió la forma del canal, de modo que la decisión pendiente bloquea **una sola tarea** —el adaptador de envío— y no el requerimiento; queda aislada, con `CA-SP-474` como el único criterio del módulo que ninguna tarea ejecutable cubre. Decisiones que alcanzan más allá de su tripleta: la **credencial provisional caduca** \(`security.md` §3.2, `requirements/sp.md` §10.10\), y quien la comprueba es `RF-SP-034`, no quien la fija; `RF-SP-040` gana una **segunda ruta** en §9, anticipada desde la v0.14.0; y **el catálogo cerrado de eventos no cambia** pese a los seis requerimientos, porque `outcome` y `detail` separan lo que de otro modo habrían sido literales nuevos. Se corrigen además los planes de `RF-SP-030` y `RF-SP-031`, que **bautizaban de nuevo tres componentes ya declarados** por `RF-SP-024` y `RF-SP-028` —`PrivilegeContainment`, `CommercialStructure` y `RootAdministratorPresence`—: es exactamente el modo de fallo que esos planes advertían, y que no falla, concede. Quedan **cuatro huecos declarados sin requerimiento**: la purga de tokens y de permisos temporales, la verificación del correo de la cuenta —que `RF-SP-040` agrava al convertirlo en la llave—, la lectura del historial de superiores, y la autoedición del propio perfil. | Responsable técnico |
| 0.32.0 | 24-08-2026 | **El submódulo de membresías cruza la tercera compuerta**: `RF-SP-016`, `RF-SP-017` y `RF-SP-018` aprueban sus `tasks.md` y quedan implementados sobre la migración `V13`. Entra con ellos la primera **restricción diferida** del sistema: `uq_memberships_parent` con `NULLS NOT DISTINCT`, que en una sola cláusula garantiza que la cadena no se bifurque ni por arriba —una sola cima— ni por abajo —una sola hija por eslabón—, y que `GlobalExceptionHandler` traduce a `EX-003` cuando salta en el `COMMIT`. `./mvnw clean verify` en verde con **55 unitarias y 131 de integración**. **Ninguno pasa a `Implementado`**: falta la prueba de concurrencia real de `RF-SP-016` · `T-13`, la de la cadena rota de `RF-SP-017` · `T-06`, y `RF-SP-018` · `T-08` deja un hueco declarado —un UUID **no canónico** se acepta y devuelve `404` en lugar de `400`, porque `UUID.fromString` del JDK es laxo y no se logró desplazar el convertidor por omisión—. **Dos defectos ajenos corregidos por el camino**: un identificador no convertible en una variable de ruta devolvía `500` en **cualquier** endpoint, y `PermissionsSeedIT` tenía una aserción que dependía del orden de ejecución de la suite. Enmienda `requirements/sp.md` §10.8 (v1.20.0) con las cinco restricciones de `memberships` que el documento omitía. **Numeración de migraciones:** `V8` a `V12` siguen reservadas por `RF-SP-002` y los cuatro listados de auditoría; una base que ya tenga `V13` rechazará la `V8` cuando llegue, y habrá que recrearla. | Responsable técnico |
| 0.33.0 | 24-08-2026 | **El catálogo de monedas cruza la tercera compuerta**: `RF-SP-019` y `RF-SP-023` aprueban sus `tasks.md` y quedan implementados sobre `V14` y `V15`. La siembra —`USD`, con dos decimales— **sí emite su fila de auditoría**, a diferencia de la de permisos: una moneda tiene línea de tiempo porque `RF-SP-023` puede desactivarla. `ck_currencies_default_active` impide dejar inactiva la moneda por defecto por sus **dos** caminos de escritura —la API y una migración—, y una comprobación de arranque **falla el arranque** si no hay exactamente una moneda por defecto activa. `./mvnw clean verify` en verde con **55 unitarias y 153 de integración**. **Dos defectos compartidos corregidos por el camino**: un verbo no admitido sobre un recurso mapeado devolvía `500` en lugar de `405`, y una dirección no mapeada devolvía `500` en lugar de `404`. Lo primero importa especialmente porque `RN-SP-004` y `RN-SP-010` se implementan **no escribiendo código**, y la verificación de esa ausencia es justamente un `405`. **Ninguno pasa a `Implementado`**: de `RF-SP-023` quedan abiertas la traducción de la restricción por nombre (`T-04`), la actualización de `CA-SP-131` (`T-10`) y la prueba concurrente (`T-12`). Enmienda `requirements/sp.md` §10.5 y §10.8 (v1.21.0): `currencies` gana `updated_at` y se incorporan las cinco restricciones que el documento omitía. | Responsable técnico |
| 0.34.0 | 24-08-2026 | **El catálogo de países cruza la tercera compuerta**, y con él quedan implementadas las **tres familias de catálogo con tabla independiente**: `RF-SP-020`, `RF-SP-021` y `RF-SP-022` aprueban sus `tasks.md` sobre `V16` y `V17`. Dos decisiones que no tienen equivalente en el resto del módulo. La primera: **la intercalación `es-x-icu` se declara en la columna** y no en la consulta, porque la API de criterios no puede expresar `COLLATE` —tampoco Hibernate 6— y con la intercalación por bytes «Panamá» iría detrás de «Perú»; una prueba lo fija. La segunda: **la unicidad del nombre va sobre `f_unaccent(lower(name))`** y no sobre el literal, al revés que en `roles`, porque `RN-SP-009` no admite edición y `Panamá` junto a `Panama` serían dos opciones indistinguibles **para siempre**; el mensaje del `409` incluye por eso el nombre enviado, o el actor vería rechazado un valor que no encuentra en el catálogo. El código se **normaliza** a mayúsculas, también al revés que en `roles`: allí lo inventa el actor y conviene que vea cuál quedó, aquí lo fija ISO 3166-1. `./mvnw clean verify` en verde con **55 unitarias y 187 de integración**. **Ninguno pasa a `Implementado`**: faltan las pruebas de concurrencia de `RF-SP-020` · `T-12` y `RF-SP-022` · `T-08`, y la del uso efectivo del índice de búsqueda (`RF-SP-021` · `T-11`). | Responsable técnico |
| 0.35.0 | 24-08-2026 | **Arnés de concurrencia y las pruebas que faltaban en las tres familias de catálogo.** `ConcurrencyHarness` aparca los hilos en una barrera y los suelta a la vez, porque un `executor.submit` por tarea no las hace simultáneas: la primera termina antes de que exista la última y la prueba «de concurrencia» acaba comprobando dos operaciones secuenciales, verde incluso con la garantía rota. **El arnés se prueba a sí mismo** —comprueba que las tareas se solapan— justo por eso. Cierran `RF-SP-016` · `T-13`, `RF-SP-020` · `T-12`, `RF-SP-022` · `T-08` y `RF-SP-023` · `T-12`. `./mvnw clean verify` en verde con **65 unitarias y 201 de integración**. **Hallazgo:** `EX-003` de `RF-SP-016` es **inalcanzable desde la API mientras el bloqueo de la cadena funcione** —quien la lee la mantiene bloqueada hasta escribir, de modo que nadie puede dejar obsoleta la posición calculada—; se verificó con seis altas simultáneas. La restricción diferida es literalmente el respaldo que su plan declara, y su garantía y su traducción a `409` se prueban ahora **por separado**, que es lo único posible. **Un defecto corregido por el camino**: el manejador global consultaba la lista de restricciones diferidas con `Set.of(...).contains(...)` sin comprobar el nulo, y `Set.of` lanza `NullPointerException` en lugar de devolver `false` — un fallo al confirmar sin restricción reventaba **dentro del manejador de excepciones**. | Responsable técnico |
| 0.33.0 | 24-08-2026 | §5 gana el indicador **«Requerimientos con endpoint funcionando»**, hoy en **10** —`RF-SP-001`, `RF-SP-010` y `RF-SP-016` a `RF-SP-023`—, verificado contra el contrato que sirve el backend y no contra esta matriz. Existía un salto engañoso entre «42 planes aprobados» y «0 implementados» que escondía todo el trabajo en curso: **implementados** sigue en cero y es correcto, porque el Art. XVI exige Pull Request aprobado e integrado y ninguno lo ha pasado, pero sin una fila intermedia esa cifra se leía como que no había código. | Responsable técnico |
| 0.36.0 | 24-08-2026 | **`RF-SP-024` cruza la tercera compuerta: el módulo tiene por fin sujeto.** Cinco migraciones, `V18` a `V22`: `users`, `user_roles`, `user_memberships`, `user_supervisors` y la semilla del superadministrador, cuya credencial entra por **marcador de posición de Flyway** —un despliegue que no la declare no arranca, en lugar de arrancar con una conocida—. El alta aplica las seis reglas en el orden que fija su plan, incluidas las tres que son **condicionales en los dos sentidos**: consumidor ⟺ membresía, vendedor ⟺ superior, y el superior debe portar el **rol padre inmediato** y no un ancestro. Contraseñas con Argon2id y política compartida en `shared/security`, porque cuatro requerimientos deben verificar lo mismo. `./mvnw clean verify` en verde con **65 unitarias y 220 de integración**. **Cierra en su mayor parte el bloqueo 3 de `RF-SP-001`**: los permisos efectivos del actor se leen ya **de la base**, recorriendo sus roles vigentes, de modo que un rol desactivado deja de conceder de inmediato; queda un respaldo temporal para los actores simulados de las pruebas, que debe retirarse cuando `RF-SP-034` emita tokens. **No pasa a `Implementado`**: faltan las pruebas concurrentes (`T-21`), la lista real de contraseñas comunes (`T-13`) y la extracción de `RN-SEG-010` y de la estructura comercial a componentes propios (`T-08`, `T-09`), que es lo que `RF-SP-030` necesitará para no escribir una tercera copia. | Responsable técnico |
| 0.37.0 | 24-08-2026 | **El bloque de autenticación y sesión cruza la tercera compuerta**: `RF-SP-034`, `RF-SP-035` y `RF-SP-036` aprueban sus `tasks.md` sobre `V26` y `V27`, y con ellos **el sistema autentica de verdad**. La suite deja de simular al actor: `AuthIT` obtiene el token del inicio de sesión y lo presenta en una cabecera `Authorization`, que es lo único que permite comprobar que el token abre puertas y que **retirar un rol las cierra en la petición siguiente** — los permisos se resuelven contra la base en cada petición y no se leen del token. `./mvnw clean verify` en verde con **65 unitarias y 243 de integración**. **Tres defectos que solo una prueba de extremo a extremo podía encontrar**, los tres invisibles a la compilación y a las pruebas unitarias. Los dos primeros son el mismo error: el rechazo **deshacía su propia contabilidad**. El contador de intentos se incrementaba dentro de la transacción y el rechazo se expresaba lanzando una excepción, de modo que la excepción revertía el incremento y **la cuenta no se bloqueaba nunca**; en el refresco, la detección de robo revocaba la familia y a continuación la deshacía, dejando la alarma escrita en la auditoría —que va en transacción propia— mientras los tokens seguían vivos, que es el peor desenlace posible porque el registro afirma que se actuó. Se corrigen con `noRollbackFor`: unas credenciales inválidas **no son un fallo de la operación sino su resultado**, y en el refresco la alternativa de una transacción aparte **se traba** contra el bloqueo pesimista de la propia fila. El tercero es un molde a `OffsetDateTime` sobre lo que una consulta nativa devuelve como `Instant`, inocuo mientras `locked_until` valiera siempre `null` y por eso visible **solo con la cuenta ya bloqueada**: `500` en lugar de `423`, justo en el camino que existe para protegerla. **Ninguno pasa a `Implementado`**: no hay **límite de tasa** en ninguno de los tres endpoints —`EX-004` y el `429` quedan sin implementar y sin documentar—, no hay **pruebas de temporización** para `CA-SP-293` ni `CA-SP-377`, no se aplica el **cambio obligatorio de contraseña** al resto de endpoints —`RF-SP-037` es del bloque C—, y falta la prueba concurrente de `RF-SP-035` · `T-05`. El respaldo temporal de `CurrentActor` sobre las autoridades del token **sigue en pie**: retirarlo obliga a migrar a tokens reales una quincena de clases de prueba, y se hará en bloque. | Responsable técnico |
| 0.38.0 | 24-08-2026 | **Los roles de una persona se asignan y se retiran**: `RF-SP-030` y `RF-SP-031` aprueban sus `tasks.md` sobre `V28`, y con ellos **quedan por fin extraídos los tres componentes compartidos** que `RF-SP-024` debía haber creado el 22-08-2026 y no creó —sus `T-08` y `T-09` quedaron abiertas y la lógica siguió dentro del alta—. Es exactamente el riesgo que aquellos planes declaraban, y llegó el momento en que la segunda copia se iba a escribir: `PrivilegeContainment` (`RN-SEG-010`), `CommercialStructure` (`RN-SP-019`, `RN-SP-020` y la comparación de rango) y `RootAdministratorPresence` (`RN-SP-001`). Una regla de **ArchUnit** ancla la primera: nadie fuera de `users/domain/security` lee los permisos que un rol declara, de modo que la tercera comprobación de `RN-SEG-010` ya no puede escribirse en silencio — y el modo de fallo que evita no es un error, es que **la copia que se quede atrás concede**. `./mvnw clean verify` en verde con **85 unitarias y 275 de integración**. **Tres decisiones alcanzan más allá de estas dos tripletas.** `UserResponse` gana **`membership` y `supervisor`**, que `RF-SP-024` no había definido: sin ellos la respuesta de un retiro no puede mostrar su efecto más importante, que es la **cascada** —quedarse sin rol de consumidor borra la membresía, quedarse sin rol de vendedor cierra la asignación de superior, y cerrar no es borrar porque esa fila dice a quién se atribuía cada resultado—. El puerto **`SessionRevoker`** se declara en `shared/security` con su adaptador en el módulo de sesión, y lo consumirán después `RF-SP-028`, `RF-SP-029` y `RF-SP-037`: retirar un rol sin revocar sesiones deja vivo hasta siete días el acceso que se acaba de quitar, porque el refresh token sobrevive al cambio de permisos. Y **la reserva de números de migración queda muerta**: la migración es `V28` y no la `V25` que su plan reservaba, porque `V26` y `V27` ya se aplicaron y Flyway rechaza un número inferior salvo con `outOfOrder`, que no se habilita —permitirlo convierte el historial en algo que ya no describe cómo llegó el esquema a su estado—. `V8`-`V12` y `V23`-`V25` no volverán a usarse. **Ninguno pasa a `Implementado`**: faltan las pruebas con dobles que fijan el orden de verificación **paso a paso** —hoy alguien puede reordenar los pasos sin que nada falle mientras el resultado coincida—, la de que un fallo al revocar sesiones **revierta el retiro entero**, y la verificación con `EXPLAIN` de que el conteo de equipo a cargo usa su índice. Se deja constancia de que `MembershipConcurrencyIT.dosAltasConElMismoNombre` **falló una vez** durante este bloque con `500` en lugar de `409` y volvió a pasar sin cambio alguno: es una intermitencia real en la traducción del choque de nombre bajo concurrencia, y queda anotada como hallazgo abierto de `RF-SP-016`. | Responsable técnico |
| 0.39.0 | 24-08-2026 | **El bloque C se completa: membresía de una persona y estructura comercial.** `RF-SP-032`, `RF-SP-033`, `RF-SP-041` y `RF-SP-042` aprueban sus `tasks.md`, ninguna con migración propia. `./mvnw clean verify` en verde con **99 unitarias y 326 de integración**. Cuatro decisiones alcanzan más allá de sus tripletas. **La definición de «vigente» queda en un solo sitio** y con su borde fijado por prueba: una fecha exactamente igual al instante consultado ya **no** está vigente — con `>=` habría un instante en que la membresía seguiría concediendo lo que ya expiró—; la comparten `RF-SP-026`, `RF-SP-031` y `RF-SP-032`. **`ConsumerStatus` unifica una condición de una línea con cuatro clientes, dos de los cuales deciden en direcciones opuestas**: `RF-SP-032` exige que la persona SÍ sea consumidor y `RF-SP-033` que NO lo sea, y esa oposición se prueba en las dos direcciones dentro del mismo archivo porque invertir una mitad no falla en la otra. **`ChangeReason` y `SelfOperationGuard` se crean aquí** pese a pertenecer nominalmente a `RF-SP-028`, que no está implementado: es la misma situación que el bloque anterior resolvió con los tres componentes de `RF-SP-024`, y `RF-SP-028` y `RF-SP-029` los consumirán en lugar de escribirlos. Y el sistema **estrena su paginación** (`shared/pagination`), que `architecture.md` §7.4 declaraba uniforme desde el principio y que ningún endpoint había necesitado todavía: una petición que excede el máximo se **rechaza y no se recorta**, porque recortarla en silencio haría que quien pide doscientos elementos reciba cien y crea que solo hay cien. **Un cambio de comportamiento sobre un requerimiento ya implementado, declarado:** el conteo de `RN-SP-022` dejaba fuera a las personas inactivas, y ahora cuenta a todos los no eliminados. Lo obliga `CA-SP-447` —el total del equipo y el número del rechazo tienen que ser **el mismo**—, y además el criterio anterior no se sostenía: una cuenta suspendida sigue teniendo un superior al que hay que reasignarla. La consecuencia es que retirar el rol comercial a quien tiene subordinados suspendidos ahora se rechaza. **Lo que `RF-SP-042` NO devuelve es lo que lo define**, y son cinco pruebas: un solo nivel y nunca el árbol descendente, sin conteo indirecto, sin historial de superiores, sin filtros y sin ser un perfil. Las cinco existen para no adelantar **D-22** sin que nadie la haya decidido. De `RF-SP-041` quedan probadas las tres tareas que su propio plan señalaba como las de mayor riesgo, y las tres fallan sin dar error: el historial **no se sobrescribe** —quedan dos filas, una cerrada y una vigente—, dos reasignaciones simultáneas **se serializan** bajo bloqueo del subordinado, y **el equipo se mueve con el reasignado** en lugar de ser arrastrado al superior nuevo, que es la implementación equivocada que reorganizaría la empresa con cada cambio y pasaría todas las demás pruebas. **Ninguno pasa a `Implementado`**: faltan las pruebas con dobles del orden de verificación, el registro de los rechazos en `audit_error_log`, las concurrencias entre operaciones **distintas** —el par membresía/rol y la consulta durante una reasignación—, y el `EXPLAIN` que demuestre que el conteo de equipo usa su índice. | Responsable técnico |
| 0.40.0 | 24-08-2026 | **Las consultas de personas, y dos huecos que llevaban días declarados.** `RF-SP-025` y `RF-SP-026` aprueban sus `tasks.md` sobre `V29`. `./mvnw clean verify` en verde con **99 unitarias y 351 de integración**, y **tres pasadas completas consecutivas** para dar por cerrado el hallazgo de concurrencia que sigue. **El contrato OpenAPI declara por fin cómo se autentica.** Sin el esquema de seguridad, Swagger UI no muestra el botón «Authorize» y no hay forma de adjuntar la cabecera desde la interfaz: **toda** operación protegida respondía `401` y quien exploraba la API concluía que estaba rota. El contrato describía cada endpoint y cada permiso, y callaba lo único que hacía falta para probarlos; el esquema se declara **global** —la regla del sistema es que todo exige token salvo tres rutas— y los tres endpoints de sesión lo desheredan de forma expresa, porque de otro modo pedirían el token para darlo. Una prueba lo fija. **Se cierra el hueco del UUID no canónico** (`RF-SP-018` · `T-08`), declarado el 24-08-2026 tras dos intentos fallidos. El motivo de que fallaran estaba en `TypeConverterDelegate`: consulta primero el servicio de conversión, pero si el convertidor **lanza**, captura el fallo y **vuelve a intentarlo con el editor permisivo por omisión** — un convertidor que rechaza no rechaza nada, su excepción se descarta y el valor entra igual. Un editor **personalizado** se localiza antes y cortocircuita ese reintento. Ahora `1-1-1-1-1` es `400` `VAL-001` en **todas** las rutas con identificador, y no el `404` de «no existe» ante algo que nunca pudo existir. **Se corrige el defecto de concurrencia de `RF-SP-016`** anotado en la v0.38.0: `SELECT … FOR UPDATE` bloquea **las filas que encuentra**, y sobre una cadena vacía no encuentra ninguna, de modo que dos registros simultáneos de la primera membresía no se serializaban en absoluto y el desenlace dependía de qué restricción mordiera antes. Se añade un **bloqueo consultivo de transacción** sobre una clave fija, que serializa haya filas o no y se suelta solo al confirmar. La prueba había fallado dos veces en la suite completa y nunca en aislamiento, que es la firma exacta de ese defecto. Del listado, tres decisiones merecen registro: **no se puede ordenar por ningún campo de la credencial** —ordenar por la marca de cambio obligatorio produce la lista de quien no ha cambiado su contraseña inicial—, los filtros por rol y por membresía usan **`EXISTS` y no `JOIN`** porque un `JOIN` haría que el total contase asignaciones en lugar de personas en cuanto alguien añadiera un segundo valor, y una **membresía vencida sigue viajando** con `current` en falso: vencer no es lo mismo que no tener. Del detalle, la que lo define: **los permisos efectivos salen del mismo componente que autoriza**, de modo que la respuesta no puede contradecir a lo que el sistema hará con la siguiente petición de esa persona. **Ninguno pasa a `Implementado`**: faltan las pruebas de `EXPLAIN` que demuestren que la búsqueda usa su índice GIN —el síntoma de que no lo use sería lentitud y no un fallo, y `users` sí crece sin límite—, la prueba de número de sentencias del detalle, y las enmiendas de `requirements/sp.md` §6.1, §10.8 y §10.10. | Responsable técnico |
| 0.41.0 | 24-08-2026 | **El ciclo de vida de una persona queda cerrado**: `RF-SP-027`, `RF-SP-028` y `RF-SP-029` aprueban sus `tasks.md`, y con ellos **los diez requerimientos del bloque de usuarios tienen endpoint**. `./mvnw clean verify` en verde con **99 unitarias y 383 de integración**. La pieza que define `RF-SP-027` es el tipo **`Patchable`**, que distingue los **tres** estados de un campo de `PATCH` —ausente, presente y nulo, presente con valor—. Se intentó antes con `Optional` y **falló en silencio**: al deserializar un registro, Jackson entrega `Optional.empty()` tanto para el campo ausente como para el nulo explícito, de modo que los dos estados que había que separar se funden en uno; el síntoma no se parecía a la causa —enviar solo el nombre rechazaba la petición por «apellido vacío»— y la clave está en `getAbsentValue()`, cuya implementación por omisión delega en `getNullValue()`. La distinción importa porque **aquí el nulo explícito se rechaza**, al revés que en la edición de un rol: las tres columnas son `NOT NULL`, y aceptarlo produciría un `500` donde corresponde un `400`. De `RF-SP-028`, lo que da sentido al requerimiento es **`FA-003`**: pasar de bloqueo **automático** a **manual** sí es un cambio aunque el estado no varíe, porque `locked_until` pasa de informado a nulo y con ello el bloqueo deja de levantarse solo; lo decide esa columna y no un campo aparte, lo que hace observable la distinción **sin ampliar el esquema**. De `RF-SP-029`, lo que se hace mal se hace **sin fallar**: la **captura del estado completo va antes de tocar nada** —después de borrar las asignaciones ya no hay qué capturar y el registro quedaría sin decir qué era la persona—, el **estado no se toca** para que ese registro diga en qué situación estaba, y el superior se **cierra con la misma marca de tiempo** que la eliminación, porque si difirieran el historial diría que estuvo a cargo de alguien después de haber dejado de existir. **Se corrige además una migración editada después de aplicarse.** El rol de negocio `CLIENTE` se había añadido dentro de `V7__seed_system_roles.sql`, que ya estaba aplicada: Flyway valida por suma de comprobación, y toda base que ya la hubiera ejecutado fallaría al arrancar con un desajuste que no dice «alguien editó V7» sino «validación fallida». `V7` vuelve a su estado íntegro y el rol pasa a `V30__seed_client_role.sql`, con el código **bien escrito** —estaba como `CLINETE`, y un código de rol es un identificador permanente que se referencia desde configuración y consultas—. **Invierte una decisión declarada y queda escrito**: `V7` afirmaba que `ESTUDIANTE` y `CLIENTE` quedaban fuera de la siembra por ser roles de negocio que crea la API, y `SystemRolesSeedIT` lo verificaba; ahora `CLIENTE` se siembra —clasificado `CONSUMIDOR`, lo que lo ata a una membresía por `RN-SP-018`— y `ESTUDIANTE` sigue fuera. La prueba no se borró: se reescribió para guardar lo que sigue siendo cierto y dejar constancia de qué dejó de serlo. **Ninguno pasa a `Implementado`**, y hay un hueco que conviene tener presente: **el token de acceso ya emitido sigue valiendo hasta quince minutos** tras desactivar o eliminar una cuenta, porque el registro de corte de `RF-SP-028` · `T-09` no existe todavía. Los permisos sí se cortan de inmediato —se resuelven contra la base en cada petición— y el refresh token también, de modo que la ventana está acotada; pero existe. Faltan además las pruebas concurrentes con transacciones reales de los tres, la de número de sentencias, y las enmiendas de `requirements/sp.md` §6.1, §10.8 y §10.10 y de `security.md` §8.1. | Responsable técnico |
| 0.42.0 | 24-08-2026 | **Contraseñas y perfil propio, y con ellos el bloque de usuarios y sesión queda completo.** `RF-SP-037`, `RF-SP-038` y `RF-SP-039` aprueban sus `tasks.md` sobre `V31`. El indicador de endpoints sube a **28**; los catorce requerimientos sin endpoint son los ocho de administración de roles, los cinco listados de auditoría y `RF-SP-040`, este último bloqueado por **D-23**. `./mvnw clean verify` en verde con **103 unitarias y 407 de integración**. La decisión que define `RF-SP-038` es que **la credencial provisional CADUCA**, y su ausencia era un agujero silencioso: sin plazo, una cuenta restablecida y nunca usada conserva indefinidamente una contraseña que otra persona conoce, la cuenta sigue activa, el registro dice que se restableció, y lo que alguien apuntó en un papel sigue abriendo la puerta meses después — **nadie se entera porque no falla nada**. La columna va atada por `CHECK` a la marca de cambio obligatorio, porque las dos describen el mismo hecho y separarlas admitiría dos estados que no significan nada: una caducidad sobre una credencial propia, y una credencial ajena que no caduca. Quien comprueba el plazo es el inicio de sesión, y su rechazo es el **genérico**: decir «su credencial provisional caducó» confirmaría que la cuenta existe y que alguien la restableció. De `RF-SP-037`, lo que importa es **el orden de los cuatro pasos**: la contraseña actual se comprueba **la última** porque es el único paso que consume el contador de intentos, y ponerlo antes haría que cinco peticiones descuidadas de un cliente propio bloquearan la cuenta de su titular. Su rechazo es **`422` y no `401`**, y la diferencia se nota en el cliente: un `401` le diría que su sesión ya no vale y lo mandaría a iniciar sesión cuando lo único que ocurrió es que escribió mal su contraseña. **Se cierra además `RF-SP-034` · `T-03`**, pendiente desde el 24-08-2026: la progresión del bloqueo y su techo se extraen a `LockoutPolicy`, compartida por las dos operaciones que consumen el mismo contador, y el techo tiene por fin prueba propia —incluida la del **desbordamiento**, porque sin tope de duplicaciones el desplazamiento de bits da un factor negativo y con él una fecha en el pasado: la cuenta quedaría desbloqueada justo cuando más fallos acumula—. De `RF-SP-039`, lo que lo define es a quién le devuelve qué: publica los **permisos efectivos del actor** —cierra el hallazgo `DF-04` del frontend, que tenía que deducirlos del listado de roles y se quedaba atrás—, devuelve **solo el superior comercial y nunca el equipo** —la distinción que sostiene la reserva de **D-22**—, y devuelve la marca de cambio obligatorio, que el detalle de terceros **no** lleva: al titular le dice que tiene que actuar, a un tercero solo le diría que esa cuenta arrastra una contraseña ajena. Una prueba recorre **el ciclo entero** —restablecer, entrar, ver que toca cambiarla, cambiarla— que es lo que ninguno de los tres verifica por su cuenta. **Ninguno pasa a `Implementado`**, y quedan dos huecos con nombre. El primero: **el filtro de cambio obligatorio no existe** (`RF-SP-034` · `T-12`), de modo que la marca viaja en el token, se devuelve en el perfil y **no restringe nada todavía**; cuando exista, estas dos rutas son sus dos excepciones. El segundo, ya declarado: el **token de acceso ya emitido sigue valiendo hasta quince minutos** tras un restablecimiento, porque el registro de corte sigue pendiente. Faltan además las pruebas concurrentes de los tres y las enmiendas de `requirements/sp.md` §10.10 y `security.md` §3.2. | Responsable técnico |
| 0.34.0 | 24-08-2026 | **`RN-SP-023` — todo usuario tiene al menos un rol.** Corrección de diseño solicitada el 24-08-2026 sobre un estado que seis especificaciones aprobadas declaraban válido. La cuenta sin roles se autentica y no puede hacer nada: no es un estado de trabajo, es un nombre de usuario y un correo reservados para siempre \(`RN-SP-016`\) a cambio de nada. Se cierra en **las dos puertas** —el alta la exige y el retiro no admite quitar el último— porque una regla aplicada en una sola es un invariante que se incumple desde la creación. Se acota a la **asignación** y no al rol activo, para que desactivar un rol no pueda violarla sobre terceros. Devuelve a su compuerta `RF-SP-024`, `RF-SP-031`, `RF-SP-025`, `RF-SP-026`, `RF-SP-034` y `RF-SP-039` \(Art. I.7\), y obliga a enmendar los `plan.md` de las dos primeras — que son las que la implementan. `requirements/sp.md` sube a **1.20.0**. | Responsable técnico |
| 0.43.0 | 25-08-2026 | **Las tres consultas de lectura que faltaban en roles y permisos.** `RF-SP-002`, `RF-SP-003` y `RF-SP-015` cruzan la tercera compuerta y quedan implementadas: el indicador de endpoints sube a **31**, y los once requerimientos sin endpoint son ahora los **seis** de escritura sobre roles —`RF-SP-004` a `RF-SP-009`—, los **cuatro** listados de auditoría y `RF-SP-040`, este último bloqueado por **D-23**. `./mvnw clean verify` en verde con **123 unitarias y 448 de integración**. Lo que estas tres cierran es el hueco que dejaba a la administración de accesos sin entrada: hasta hoy se podía **crear** un rol y no **verlo**, de modo que la interfaz no tenía de dónde sacar el catálogo para asignarlo a nadie. Cuatro decisiones merecen quedar escritas. La **migración del índice de búsqueda es `V32` y no la `V8`** que sus tareas reservaron: aplicar hoy una `V8` deja una migración fuera de orden y Flyway aborta el arranque en toda base ya migrada, de modo que el número se lee como orden de aplicación y no como identificador del requerimiento. El **atajo del conteo tenía un caso mal descrito**: «omitir el conteo cuando la página no se llena» es correcto salvo en la página **vacía** más allá de la última, donde deducir el total del desplazamiento daría `1980` para la página 99 de un catálogo de doce roles — un total inventado, con la colección vacía y sin ningún error que lo delate; se corrige aquí y **`ListUsersService` (`RF-SP-025`) arrastra el mismo defecto**, anotado para su propio pase. Los **cuatro `400` del listado se devuelven juntos** —paginación, ordenamiento, estado y clasificación—, para que quien escribió mal cuatro parámetros no tenga que corregir la URL cuatro veces. Y el **detalle del rol cuenta personas y no asignaciones**, incluyendo a las bloqueadas y excluyendo a las eliminadas: quien está bloqueado sigue portando el rol y su existencia es lo que impide borrarlo (`RN-SEG-008`). Queda una discrepancia declarada y sin cerrar: `Pagination` emite `VAL-003` donde cuatro specs declaran `VAL-001` y `VAL-002` para página y tamaño; se mantiene la uniformidad del componente compartido y la resuelve quien decida cambiarlo. | Responsable técnico |
| 0.44.0 | 25-08-2026 | **La administración de roles queda completa**: `RF-SP-004` a `RF-SP-009` cruzan la tercera compuerta y se implementan. El indicador de endpoints sube a **37**, y los cinco requerimientos sin endpoint son ya solo los **cuatro listados de auditoría** y `RF-SP-040`, bloqueado por **D-23**. Hasta hoy se podía crear un rol y consultarlo, y nada más: no había forma de corregirle el nombre, ajustarle los permisos, desactivarlo ni retirarlo. Lo que define este bloque es que **las seis operaciones cruzan las mismas tres puertas** —el rol existe y está vigente, no es de sistema (`RN-SEG-012`), y el actor no lo tiene asignado (`RN-SEG-011`)—, y que esas tres viven **en un solo componente**: seis copias de la misma comprobación divergen con el tiempo, y **la que se queda atrás no falla, concede**. La tercera responde `403` y no `409` a propósito, porque no es un dato inválido sino una operación que ese actor no puede ejecutar, y la diferencia se ve en la auditoría — queda como denegación de autorización y no como error de operación. Cinco decisiones más merecen quedar escritas. **El rol raíz se protege aparte de los de sistema** aunque hoy la primera puerta ya lo detenga: un rol raíz inactivo no concede nada y dejaría al sistema sin su última vía de administración, y esa protección no debe depender de que alguien recuerde marcarlo como de sistema. **Reubicar un rol NO retira nada**: si excede al nuevo padre se rechaza entero enumerando los permisos que sobran, porque recortarlos en silencio dejaría al rol concediendo menos de lo que su titular cree. **Retirar un permiso que un rol hijo declara se rechaza sin cascada**, y el mensaje dice qué rol y qué permiso —un hijo **inactivo** lo impide igual que uno activo, porque el invariante vale siempre—. **Eliminar dice qué lo impide**: qué roles cuelgan, o cuántas personas lo portan —incluidas las bloqueadas, que es justo lo que `RN-SEG-008` protege— y sugiere desactivar, que suele ser lo que en realidad se quería. Y **la unicidad del nombre se comprueba antes de tocar el agregado**: con el nombre nuevo ya escrito en la entidad, el propio `SELECT` dispara el vaciado de Hibernate y la violación del índice llega como `500` donde corresponde un `409` — un defecto que solo aparece con el dato ya duplicado. `GlobalExceptionHandler` amplía además su lista de rechazos de **severidad alta** con `RN-SEG-005`, `RN-SEG-006`, `RN-SEG-007`, `RN-SEG-008` y `RN-SEG-013`, que hasta hoy se registraban como media: un ciclo o una segunda raíz corrompen la jerarquía entera, no un rol. **Queda un hueco declarado**: `CA-SP-161` —dos reubicaciones simultáneas que formarían un ciclo— no tiene prueba concurrente; el bloqueo pesimista de fila está puesto y es lo que debe serializarlas, pero nadie lo ha verificado bajo carrera. | Responsable técnico |
| 0.45.0 | 25-08-2026 | **`RF-SP-034` enmienda su tripleta: el rechazo deja de ser mudo.** Cada `401` declara cuántos intentos quedan, y para que ese número no reabra la enumeración que el requerimiento existe para cerrar, el identificador **sin cuenta** pasa a gastar intentos igual —mismo umbral, misma progresión, mismo `423` al agotarlos—. Dos rechazos que se producían con la contraseña correcta pasan a consumir intento por lo mismo. La expiración del bloqueo, que `CA-SP-378` exigía desde el 21-08-2026 y **no estaba implementada**, viaja ahora como dato —`unlockAt` y `retryAfterSeconds`— y **no escrita en el mensaje**: un texto con la duración envejece entre que se compone y se lee. Tres criterios nuevos, `CA-SP-476` a `CA-SP-478`. | Responsable técnico |
| 0.46.0 | 25-08-2026 | **Los cuatro registros de auditoría se pueden leer, y con ellos el bloque `SP` queda a un requerimiento de estar completo.** `RF-SP-011` a `RF-SP-014` cruzan la tercera compuerta: el indicador de endpoints sube a **41**, y el único requerimiento sin endpoint es ya `RF-SP-040`, bloqueado por **D-23**. Hasta hoy el sistema escribía cuatro registros de auditoría que **solo podían leerse con un cliente de base de datos**: la evidencia existía y no era consultable, que es una forma cara de no tenerla. La decisión que define el bloque es **el conteo acotado**: `RF-SP-002` dejó anotado que su `COUNT(*)` exacto no debía heredarse aquí, y no se hereda — el total se cuenta sobre una subconsulta con `LIMIT techo + 1`, de modo que la sentencia **nunca examina más de diez mil filas** tenga la tabla mil o cien millones, y por encima del techo la respuesta **declara** que el total no es exacto en lugar de mentir. El techo es configuración (`nexus.pagination.count-limit`) y no una constante: subirlo recupera el total exacto siempre, y quitarlo devuelve el recorrido completo de la tabla en cada página. `totalIsExact` deja de valer siempre verdadero y empieza a significar algo. Cuatro decisiones más quedan escritas. **El orden no es configurable** —`occurred_at DESC, id DESC`—, porque es parte del significado de un registro cronológico, y el desempate por `id` no es cosmético: dos eventos pueden compartir instante, y al ser `id` un UUID v7 ese desempate sigue siendo orden cronológico. **El rango de fechas es semiabierto**, para que dos rangos consecutivos no devuelvan dos veces el evento de la frontera y quien recorra la línea de tiempo mes a mes no cuente de más. **Solo la consulta de seguridad se audita a sí misma** (`CA-SP-167`): en ese registro el acto de mirar es en sí mismo información de seguridad, y el evento guarda **con qué filtros** se miró, porque «alguien consultó el registro» no dice nada. Y **los cuatro exigen cuatro permisos distintos**: quién editó un rol es información de operación y quién intentó entrar y falló es información de seguridad; un único `audit:read` obligaría a conceder la segunda para poder dar la primera. La migración de índices es **`V33` y no `V9` a `V12`**, por el mismo motivo que `V32`: aquellos números se reservaron antes de que se aplicaran `V13` a `V32`. Se implementan además con **un puerto, un adaptador, un caso de uso y un controlador para los cuatro**, en lugar de cuatro juegos: son la misma consulta cuatro veces, y cuatro copias del predicado de rango serían cuatro ocasiones de que una divergiera —una divergencia que no se ve, porque devuelve datos plausibles—. | Responsable técnico |
| 0.46.0 | 25-08-2026 | **El cambio obligatorio de contraseña pasa a decidirlo `provisional_password_expires_at`, y solo esa columna**: nula, la persona navega; con fecha —vencida o no—, queda obligada a cambiarla. Decisión del responsable del proyecto, tomada sobre la advertencia de sus consecuencias. Alcanza a `RF-SP-034` (`spec.md` v0.3.0, tres criterios nuevos `CA-SP-480` a `CA-SP-482`), a `RF-SP-038` (`spec.md` v0.2.0, `CA-SP-392` **retirado** y sustituido por `CA-SP-479`) y a `security.md` v0.26.0. **Tres consecuencias declaradas:** el alta de `RF-SP-024` no escribe caducidad, de modo que quien se registra —y el superadministrador sembrado— navegan con la contraseña que fijó otra persona; la credencial provisional **deja de expirar**, reabriendo la ventana que `RF-SP-038` había acotado; y `POST /api/v1/users` sigue devolviendo `mustChangePassword: true` para alguien a quien el inicio de sesión le dirá `false`. | Responsable técnico |
| 0.47.0 | 26-08-2026 | **El cambio obligatorio de contraseña deja de ser una advertencia y pasa a retener de verdad.** `RF-SP-034` · `T-12` era la única tarea que faltaba para que el claim `mcp` restringiera algo: desde el 24-08-2026 el token lo llevaba y la respuesta del inicio de sesión lo devolvía, pero **ningún filtro lo hacía cumplir** — quien tenía credencial provisional usaba la API entera con normalidad, que es exactamente la ventana que la marca existe para cerrar. Con ella se cierran de paso `RF-SP-037` · `T-06` y `RF-SP-039` · `T-05`, las dos excepciones que esperaban a que el filtro existiera, y el bloqueo #2 de `RF-SP-034`. `spec.md` de `RF-SP-034` sube a **v0.4.0**: `FA-002` decía «le responden que debe cambiarla» sin decir con qué, de modo que **su punto 3 no era verificable**; ahora declara `403` con un `type` propio **distinto del de la falta de permiso** —comparten estado y lo que el cliente debe hacer con cada uno es opuesto: ante uno oculta la opción, ante el otro lleva a cambiar la contraseña—, más la ruta de salida como miembro de extensión. Cuatro criterios nuevos, `CA-SP-483` a `CA-SP-486`. **Dos hallazgos al implementarla.** Las excepciones resultaron ser **siete y no dos**: las tres rutas públicas de sesión hubo que exceptuarlas porque un cliente adjunta su `Authorization` en toda petición, y sin ellas quien tiene la marca **no podía cerrar su propia sesión** — lo contrario de lo que `RF-SP-036` persigue al hacer público ese endpoint. Y la retención **no se audita**, con el mismo argumento por el que el límite de tasa avisa una vez por ventana: no es un intento de saltarse un permiso, y un evento por rechazo sepultaría `audit_security_log` bajo el ruido de cualquier interfaz que reintente. `security.md` v0.30.0. | Responsable técnico |
| 0.48.0 | 26-08-2026 | **Retirarle el acceso a alguien pasa a tener efecto inmediato de verdad** (`RF-SP-028` · `T-09` y `T-10`). `security.md` §4.5 declaraba desde el principio que desactivar a una persona «revoca sus refresh tokens **y rechaza su token de acceso**», y solo la primera mitad era cierta: el token de acceso es un JWT firmado que se valida sin consultar nada, y seguía abriendo puertas los quince minutos que le quedaran de vida. Quien fuera desactivado, eliminado o le restablecieran la contraseña conservaba un cuarto de hora de acceso, **y ninguna prueba lo decía**. Lo cierra `AccessRevocationRegistry` con su puerto `AccessRevocationPublisher`, consumido por las **cuatro** operaciones que retiran acceso: `RF-SP-028`, `RF-SP-029`, `RF-SP-037` y `RF-SP-038`. Con ello se cierran `RF-SP-028` · `T-09` y `T-10`, `RF-SP-029` · `T-07` y `RF-SP-038` · `T-05`, y se **corrige** `RF-SP-037` · `T-04`, que estaba marcada hecha desde el 24-08-2026 con solo la mitad construida y con una verificación escrita que era falsa. **Lo que desbloqueó la tarea sin resolver lo que la bloqueaba:** llevaba abierta desde el 22-08-2026 esperando a decidir cómo se propaga el corte entre instancias, y esa decisión sigue pendiente — pero va detrás de un puerto, de modo que sustituir el registro en memoria por un canal compartido no toca ningún caso de uso. Hoy bloquea el despliegue de una **segunda instancia** (**D-09**), no el requerimiento. **Tres hallazgos de implementación quedan escritos** en `RF-SP-028` `tasks.md` §4.ter: no hace falta un filtro —es un validador del token, que además corta antes de resolver los permisos contra la base—; **el segundo del corte es ambiguo** porque el `iat` va en segundos enteros, y no se resuelve eligiendo a qué lado caen los empates —cerrar mata tokens legítimos, abrir deja vivos quince minutos los que debían morir— sino **sellando** el token que se emite con el corte, que quita la ambigüedad en lugar de repartirla; y la siembra del arranque **fallaba en silencio** leyendo el tipo con `queryForList`, defecto que solo destapó la prueba del reinicio porque por HTTP todo seguía funcionando. `security.md` v0.31.0. | Responsable técnico |
| 0.49.0 | 26-08-2026 | **`RF-SP-040` deja de ser el único requerimiento del módulo sin endpoint**, y con él **los cuarenta y dos tienen implementación**. Lo que lo tenía parado no era código: era **D-23**, la decisión del mecanismo de envío, abierta desde el 22-08-2026. Se cierra con **Resend** por decisión del responsable del proyecto, y se elige su API HTTP sobre SMTP por lo que cuesta operar cada uno — SMTP obliga a credenciales, puertos salientes que muchas redes bloquean y **una cola propia de reintentos**, mientras que aquí la entrega, los reintentos y los rebotes los lleva el proveedor. **El plan había aislado esa decisión en una sola tarea, y esa previsión funcionó**: las otras trece se construyeron contra el puerto `NotificationSender` y el adaptador entró al final sin tocar ninguna. Dos hallazgos de implementación merecen quedar escritos. **El desacople del envío son dos mitades y solo una es evidente**: `afterCommit` saca el envío de la transacción y **no del hilo de la petición**, de modo que la respuesta seguía esperando al correo y volvía a tardar distinto según existiera la identidad — la fuga exacta que el requerimiento existe para cerrar, invisible leyendo el código y visible con un cronómetro. La destapó `CA-SP-473`, y se corrigió despachando a otro hilo. Y **recuperar la contraseña no puede reutilizar el cambio de contraseña propia**: aquel levanta el bloqueo automático y este no debe (`CA-SP-464`), porque recuperar prueba que se tiene el **correo** y no que alguien decidiera devolver el acceso — con la vía reutilizada, quien tuviera a mano el correo de una cuenta bloqueada podía desbloquearla solo. La migración es **`V37` y no `V29`**: el número se reservó al aprobar el plan y quedó tomado mientras la decisión seguía abierta. `security.md` v0.32.0, `architecture.md` v0.17.0 —donde §15.1 gana el mecanismo y **D-23 sale de las decisiones pendientes**— y `requirements/sp.md` v1.23.0 con §10.13. | Responsable técnico |
| 0.50.0 | 26-08-2026 | **El bloque de usuarios estrena sus pruebas concurrentes, y dos de ellas encontraron defectos reales** (`RF-SP-024` · `T-21`, `RF-SP-027` · `T-11`, `RF-SP-028` · `T-15`, `RF-SP-029` · `T-12`, `RF-SP-033` · `T-09`). El módulo tenía concurrentes para membresías, países y monedas y ninguna para personas — justo donde viven las reglas cuyo incumplimiento cuesta más caro. **`RN-SP-001` no se sostenía**: dos desactivaciones simultáneas de superadministradores distintos devolvían `200` las dos y dejaban el sistema **sin ninguno**. `T-05` estaba marcada Hecha y su verificación se cumplía —la traza mostraba el `SELECT … FOR UPDATE` sobre el conjunto—, pero `FOR UPDATE OF user_roles` solo sirve cuando la operación **borra** la asignación: `RF-SP-028` únicamente cambia `users.status`, la fila bloqueada no cambia, y el `users` del `JOIN` se lee de la instantánea propia. Corregido con una **compuerta** —la fila del rol raíz en `roles`, tomada antes de contar— y el conteo en sentencia aparte, que en `READ COMMITTED` toma instantánea nueva. Se descartó bloquear también las filas de `users`: resuelve la reevaluación y **crea un abrazo mortal**, porque cada transacción ya tiene bloqueada la de su objetivo. **Y la edición hacia un correo duplicado devolvía `500`**: el adaptador traduce la violación de `uq_users_email` dentro de `save(...)`, y la edición no llama a `save` — el `UPDATE` sale en el commit, fuera de todo `try`. Corregido con `flushChanges()`. **Y una tercera, intermitente**: eliminar una cuenta mientras alguien inicia sesión con ella dejaba **una sesión viva sobre una cuenta que ya no existe**. El inicio decide el estado sobre su instantánea; la eliminación barre las sesiones vigentes y confirma; y la sesión que el inicio inserta después nace fuera de ese barrido. Pasó aislada y **falló en la suite completa**, que es donde el reparto de hilos cambia. Corregido releyendo la cuenta con su fila bloqueada antes de emitir la sesión, y **solo en el camino de éxito**: tomar el bloqueo antes de comprobar la contraseña le añadiría al rechazo un tiempo que depende de si la cuenta existe, que es la fuga que `EX-001` cierra. **La lección es sobre la verificación:** las tres tareas afectadas estaban marcadas Hechas con verificaciones que se cumplían y no probaban la regla. Se corrige además la redacción de `T-21` de `RF-SP-024`: «jamás queda un usuario con un rol inactivo» **no es un invariante** —`RF-SP-007` desactiva roles que la gente porta, y `RN-SEG-002` existe para eso—, y se sustituye por la atomicidad del alta, que sí es lo que la carrera puede romper. | Responsable técnico |
| 0.51.0 | 26-08-2026 | **La matriz se sincroniza con el código, y §5 deja de contar una historia que ya no era cierta.** Las cuarenta y dos filas de §4 llevaban tres estados distintos —catorce en «Tasks aprobadas», `RF-SP-010` en «En implementación», que ni siquiera está en el vocabulario, y el resto en «En desarrollo»— cuando los cuarenta y dos tienen ya tripleta aprobada y endpoint funcionando: **pasan las cuarenta y dos a `En desarrollo`**, y `Implementado` sigue en **cero**, que es lo correcto mientras ningún Pull Request esté integrado (Art. XVI). El indicador de endpoints sube de **41 a 42** y §5 gana la fila de **tripleta completa**. Se corrigen tres celdas de pruebas que afirmaban lo contrario de lo que hay: `RF-SP-035` decía «sin límite de tasa» y lo tiene desde el 26-08-2026 con cota propia, `RF-SP-036` sigue sin él —`RateLimitFilter` cubre login, refresco y las dos rutas de recuperación, y **no** el cierre de sesión—, y `RF-SP-010` figuraba sin PR teniendo endpoint desde el 22-08-2026. §5 incorpora el **estado al 26-08-2026** con las **catorce tareas que siguen abiertas**, verificadas contra el código una a una, y retira lo que había dejado de ser verdad: `RF-SP-040` ya no es «aprobada pero no implementable» —**D-23 está cerrada**—, el bloqueo 3 de `RF-SP-001` se cerró el 24-08-2026 con `RF-SP-024`, y las `tasks.md` ya no están «en revisión». **Queda declarado un desfase que esta versión no cierra:** ochenta y siete filas de tarea siguen en `En curso` en veinticinco tripletas con su trabajo en buena parte hecho, y darlas por hechas exige comprobar cada una contra su verificación escrita — hasta entonces, el estado de una tarea no es fuente fiable y sí lo son §4 y la suite. | Responsable técnico |
| 0.52.0 | 26-08-2026 | **Entra el segundo módulo del sistema: `PM` — Productos y Mercadeo**, con sus **siete requerimientos** registrados en la matriz y su documento en [`requirements/pm.md`](requirements/pm.md) v0.1.0. El sistema pasa de 42 a **49 requerimientos**, y por primera vez la matriz mezcla dos módulos: las seis filas de compuerta de §5 siguen contando **solo los de `SP`**, porque los siete nuevos están en `Pendiente` y sin `spec.md`. `PM` es dueño de `products` y trae **dos tipos de producto que no se mezclan** —el upgrade de membresía, que da derecho a pasar al nivel que declara, y el servicio del sistema—, con la condición cruzada que los separa: el destino es obligatorio en uno y **está prohibido** en el otro, y la mitad que se olvida es la segunda, porque no falla — promete un cambio de nivel que nadie va a aplicar. **Lo que el módulo NO hace está escrito y es la mitad del diseño**: no cobra, no entrega y no aplica el upgrade sobre la persona, porque `user_memberships` es de `SP` y `modules.md` §7 prohíbe que otro módulo la escriba. De ahí sale **D-25**, registrada en `architecture.md` §16: `SP` no publica hoy **ninguna interfaz de aplicación** para las tres lecturas que `PM` necesita —una membresía y su nivel, una moneda y sus decimales, la membresía vigente de alguien—, de modo que la dependencia está declarada y **no es consumible todavía**; bloquea los `plan.md` de `RF-PM-001` y `RF-PM-007` y no sus especificaciones. | Responsable técnico |
| 0.53.0 | 26-08-2026 | **La membresía gana su color** (`RN-SP-024`), por decisión del responsable del proyecto: seis dígitos **hexadecimales sin `#`**, obligatorio, único y normalizado a mayúsculas al escribir, con el que el frontend pinta el nivel. Es un cambio de comportamiento sobre **tres requerimientos ya implementados**, de modo que se entrega con su tripleta enmendada (Art. I.7): `RF-SP-016` incorpora el dato de entrada, `VAL-007` a `VAL-009` y `CA-SP-487` a `CA-SP-489`; `RF-SP-017` lo devuelve en el listado (`CA-SP-490`) y `RF-SP-018` en el detalle, **para la consultada, su superior y su hija** (`CA-SP-491`). Enmienda también `requirements/sp.md` v1.25.0 —regla, columna y las dos restricciones— y `modelo-datos.md` v0.11.0. **La columna llega en `V38` y no editando `V13`**, que ya está aplicada y cuya suma de comprobación validaría Flyway al arrancar; el orden dentro de la migración es columna nullable, relleno, `NOT NULL` y restricciones, porque `ADD COLUMN … NOT NULL` sin `DEFAULT` falla con una sola fila y un `CHECK` puesto antes del relleno **no fallaría** —sobre `NULL` evalúa a `NULL` y la fila se acepta—, que es peor. **Hueco declarado y aceptado:** `RN-SP-008` mantiene la membresía inmutable, de modo que un color mal elegido **no se corrige**; `requirements/sp.md` §5.1 fija la condición para abrir `RF-SP-043`. Las tareas abiertas del módulo pasan de **catorce a veintiuna**. | Responsable técnico |
| 0.54.0 | 26-08-2026 | **El color de la membresía queda implementado** (`RN-SP-024`, `RF-SP-016` · `T-19` a `T-23`, `RF-SP-017` · `T-11`, `RF-SP-018` · `T-12`). `./mvnw clean verify` en verde con **149 pruebas unitarias y 600 de integración**, y las tareas abiertas del módulo vuelven de veintiuna a **catorce**. La migración es **`V38`** y el orden de sus cuatro pasos está escrito en el propio SQL, porque es la parte que no se puede deducir leyendo el resultado. **La validación vive en el dominio además de en el DTO**: `MembershipColorTest` la ejercita con doce casos, y existe porque el `@Pattern` del DTO solo atiende a quien llega por HTTP — si alguien retirara la del dominio, la suite de API seguiría en verde y el modelo admitiría colores mal formados por cualquier otro camino. La unicidad se comprueba **dos veces y a propósito**: el caso de uso compara ya normalizado para dar el mensaje preciso, y `uq_memberships_color` se traduce **por nombre de restricción** a `EX-001` sobre el campo `color`. **Seis suites ajenas hubo que tocarlas** porque insertaban membresías sin color; en las dos de membresías el color lo genera un contador atómico, ya que la prueba del empate concurrente lanza dos altas con el mismo código y nombre a la vez y un color compartido haría que el `409` viniera del color en lugar de lo que se prueba. `docs/api/openapi.json` y `.yaml` se regeneran con el campo, `color` entre los requeridos del alta y su patrón publicado. | Responsable técnico |
| 0.55.0 | 26-08-2026 | **`PM` cruza su primera compuerta hacia la revisión**: las **siete `spec.md`** quedan redactadas en `docs/specs/pm/` y sus filas pasan de `Pendiente` a **`Spec en revisión`**, enlazando cada una con su especificación. El indicador de `spec.md` redactada sube de 42 a **49**; los de compuerta aprobada siguen contando solo los de `SP`, porque las siete nuevas traen **veintisiete preguntas abiertas** y el Art. I.6 no admite aprobar una spec con preguntas sin resolver. Ninguna de las siete depende de **D-25**: la decisión bloquea los `plan.md` de `RF-PM-001` y `RF-PM-007`, no lo que esos requerimientos deben hacer. | Responsable técnico |
| 0.56.0 | 26-08-2026 | **La cota de la recuperación de contraseña pasa de 3 por hora a 5 por minuto, con cinco minutos de espera al superarla** (`RF-SP-040` · `T-15`), por decisión del responsable del proyecto tomada sobre la advertencia de sus consecuencias. El límite de tasa **estrena un concepto que no tenía**: hasta hoy superar una cota solo significaba aguardar a que la ventana deslizante dejara sitio, y ahora puede costar una espera fija — `Politica` gana `penalizacion`, nula en las otras tres, que no cambian. Dos decisiones del contador quedan escritas y probadas con el reloj en la mano: **la espera no se renueva al insistir** —se fija en la petición que cruza la cota, porque renovarla convertiría unas peticiones de más en un bloqueo indefinido para quien tenga un cliente que reintenta solo— y **al penalizar se olvida la ventana**, de modo que cumplido el castigo la cota vuelve entera en lugar de encadenarse sola. Enmienda `spec.md` de `RF-SP-040` a v0.2.0 con `CA-SP-492` y `CA-SP-493`, y `security.md` a v0.33.0, cuya tabla §5.5.1 gana la columna «espera al superarla». **Dos consecuencias declaradas y no resueltas:** la cota sube unas **veinte veces**, y lo que se multiplica por veinte es cuántos correos puede provocar un desconocido en la bandeja de una persona real; y **con las dos cotas iguales la de identidad no llega a dispararse**, porque el filtro comprueba primero el origen — queda como red de seguridad para cuando la del origen se afloje detrás de una pasarela corporativa. | Responsable técnico |
| 0.57.0 | 26-08-2026 | **`RF-PM-001` cruza la primera compuerta**: sus cinco preguntas quedan resueltas y la spec pasa a **aprobada**, la primera del módulo `PM`. El indicador de `spec.md` aprobada sube de 42 a **43**, y las preguntas abiertas del módulo bajan de veintisiete a **veintiuna** — dos de las seis resueltas alcanzan a otras specs: `RF-PM-005` recibe la excepción del upgrade activo, que ya no puede darse al registrar, y da por descartado su tercer estado `BORRADOR`. Las tres reglas nuevas del módulo —`RN-PM-012`, `RN-PM-013` y `RN-PM-014`— están en `requirements/pm.md` v0.3.0 con su motivo. **El `plan.md` sigue bloqueado por D-25**: qué debe hacer `RF-PM-001` está decidido; por dónde entran la membresía y la moneda de `SP`, no. | Responsable del proyecto |
| 0.58.0 | 26-08-2026 | **`RF-PM-002` aprobada**, la segunda del módulo `PM`. El indicador de `spec.md` aprobada sube a **44** y las preguntas abiertas de `PM` bajan de veintiuna a **diecisiete**. La resolución del orden alcanzó a `RF-PM-007`, que queda enmendada con el suyo propio: **hay dos formas de consulta y cada una tiene su orden**, porque responden a actores distintos — el administrador ve el catálogo por fecha de alta, y el cliente su oferta agrupada por tipo, con los upgrades por nivel destino. | Responsable del proyecto |
| 0.59.0 | 26-08-2026 | **`RF-PM-003` aprobada**, la tercera del módulo. El indicador de `spec.md` aprobada sube a **45** y las preguntas abiertas de `PM` bajan a **catorce**. Una de sus resoluciones **enmendó una spec aprobada el mismo día** (Art. I.7): `RF-PM-002` decía que el motivo del retiro no viajaba en el catálogo, y `RF-PM-003` resolvió que el detalle sí lo devuelve — la decisión de aquella no cambia, su justificación se reescribió y `CA-PM-077` deja explícito que el listado sigue sin llevarlo. | Responsable del proyecto |
| 0.60.0 | 26-08-2026 | **`RF-PM-004` aprobada**, la cuarta del módulo. El indicador de `spec.md` aprobada sube a **46** y las preguntas abiertas de `PM` bajan a **diez**. Dos de las cuatro **no hubo que decidirlas**: `RN-PM-012` y `RN-PM-013`, aprobadas con `RF-PM-001`, ya las habían cerrado. La resolución del precio deja escrita una **condición sobre un módulo que todavía no existe** —cada compra guardará el importe que se pagó, no lo leerá del producto—, que es la única forma de que corregir el catálogo no reescriba facturas emitidas. | Responsable del proyecto |
| 0.61.0 | 26-08-2026 | **`RF-PM-005` y `RF-PM-006` aprobadas**: seis de las siete specs de `PM` han cruzado la primera compuerta y solo queda `RF-PM-007`, con cinco preguntas. El indicador de `spec.md` aprobada sube a **48**. Tres de las seis preguntas de este par **no hubo que decidirlas**: dos las había cerrado `RF-PM-001` y una no tiene hoy nada que decidir —el carrito no existe—, y se traslada con lo único afirmable: la consulta de oferta no reserva nada. | Responsable del proyecto |
| 0.62.0 | 26-08-2026 | **Las siete `spec.md` de `PM` están aprobadas**, y con ellas **los 49 requerimientos del sistema tienen su especificación aprobada**. Las veintisiete preguntas abiertas del módulo se resolvieron el mismo día: **diecinueve por decisión** del responsable del proyecto y **ocho por consecuencia** —cerradas por decisiones anteriores o sin nada que decidir hoy—, todas conservadas en la §14 de su spec con el motivo, que es lo que impide reabrirlas por olvido. Tres resoluciones alcanzaron a specs ya aprobadas y obligaron a enmendarlas el mismo día (Art. I.7). **Lo que separa a `PM` de la segunda compuerta ya no es ninguna spec: es D-25**, que bloquea los `plan.md` de `RF-PM-001` y `RF-PM-007`; los otros cinco planes se pueden escribir hoy. | Responsable del proyecto |
| 0.63.0 | 26-08-2026 | **D-25 cerrada**, y `PM` queda sin nada que lo detenga en la segunda compuerta: sus siete `plan.md` se pueden escribir. La respuesta quedó escrita como norma para cualquier par de módulos (`architecture.md` §15.2): **el dueño del dato publica interfaces de aplicación de solo lectura y el consumidor las importa**, una por lectura, con modelos de lectura en vez de entidades, la ausencia como valor vacío y una regla de ArchUnit que impide importar repositorios ajenos. Es la primera vez que la prohibición de `modules.md` §7 va acompañada de **por dónde sí**. Las tareas de esos puertos pertenecen a `RF-PM-001` y `RF-PM-007`, y no se abre un requerimiento nuevo en `SP` para alojarlas. | Responsable del proyecto |
| 0.64.0 | 26-08-2026 | **`RN-SP-018` no se sostenía bajo concurrencia, y se corrige.** Retirar la membresía y asignar el rol de consumidor a la vez dejaban a la persona **portando un rol de consumidor sin nivel**, sin que ninguna de las dos operaciones fallara: cada una validaba contra el estado que la otra estaba a punto de cambiar, y en `READ COMMITTED` nada lo impide. Lo destapó `RF-SP-024` · `T-21` de forma **intermitente** —falló en CI, pasó en la ejecución siguiente y volvió a fallar dos veces—, que es la firma del defecto y no de una prueba inestable. **La causa estaba a la vista en una sola línea**: de las nueve operaciones sobre una persona, cinco tomaban el bloqueo pesimista sobre su fila y **cuatro no** — y las cuatro sin bloqueo eran justo las que tocan `user_roles` y `user_memberships`, que son las dos tablas que el invariante abarca. `RF-SP-030`, `RF-SP-031`, `RF-SP-032` y `RF-SP-033` pasan a tomarlo. Bloquean **la misma fila**, de modo que se serializan sin riesgo de abrazo mortal —a diferencia del caso que `RF-SP-028` descartó, donde caía sobre filas de terceros—, y lo toman **antes** de leer roles y membresía, que es la otra mitad de la corrección. La obligación queda escrita en el puerto, donde la encontrará quien añada la décima operación. | Responsable técnico |
| 0.65.0 | 26-08-2026 | **Los siete `plan.md` de `PM` quedan redactados** y en revisión, con lo que el módulo llega entero a su segunda compuerta. El indicador de `plan.md` aprobado sigue en **42**: escribirlos no es aprobarlos. Dos decisiones de los planes alcanzan más allá de su requerimiento y conviene leerlas antes de aprobar. **`RF-PM-003` necesita el motivo del retiro, que no está en `products`** —el Art. V.13 lo manda al registro de eliminación— y propone que **`shared/audit` publique una lectura estrecha**: el motivo de **una** entidad propia, sin actor, sin instantánea y sin poder recorrer el registro. Va ahí y no detrás de un puerto de `SP` porque la auditoría es infraestructura compartida donde **cada módulo escribe**, mientras que consultarla como producto es lo que `SP` posee. La alternativa —una columna `deletion_reason` en `products`— duplicaría un dato que ya existe y crearía dos verdades. **`RF-PM-002` resuelve el destino de cada upgrade con un `JOIN` y no con el puerto de D-25**, porque llamarlo fila a fila es el problema N+1 con otro nombre; la frontera se mantiene donde importa, que es que **ninguna regla se decide con ese `JOIN`**. | Responsable técnico |
| 0.66.0 | 26-08-2026 | **Los siete `plan.md` de `PM` quedan aprobados**, y con ellos **los 49 requerimientos del sistema han cruzado la segunda compuerta**. La aprobación arrastra dos decisiones técnicas que los planes plantearon y que ahora son firmes. **`shared/audit` publicará una lectura estrecha del motivo de eliminación** —el de **una** entidad propia, sin actor, sin instantánea y sin poder recorrer el registro—, que es de donde `RF-PM-003` saca el motivo que su spec exige devolver: no está en `products` porque el Art. V.13 lo manda al registro de eliminación, y leerlo con un `JOIN` desde `PM` sería entrar en un almacén que no gobierna. Va en `shared/audit` y no detrás de un puerto de `SP` porque la auditoría es infraestructura compartida donde **cada módulo escribe**, mientras que consultarla como producto es lo que `SP` posee. **`RF-PM-002` resuelve el destino de cada upgrade con un `JOIN`** y no llamando al puerto de D-25 fila a fila, que es el problema N+1 con otro nombre; la frontera se mantiene donde importa, porque **ninguna regla se decide con ese `JOIN`**. | Responsable del proyecto |
| 0.67.0 | 26-08-2026 | **Las siete `tasks.md` de `PM` quedan aprobadas**, y con ellas **los 49 requerimientos del sistema tienen tripleta completa**: el módulo cruzó sus tres compuertas el mismo día. Son **95 tareas**, y cuatro de ellas están señaladas como lo que este proyecto ya ha pagado varias veces —trabajo que **se hace mal sin fallar**—: los tres estados de un campo con `Patchable`, que ya falló en silencio en `RF-SP-027`; el índice antes que la comprobación previa en `RF-PM-005`, porque escribirlo al revés invita a creer que la comprobación basta; no tocar el estado ni capturar tarde la instantánea en `RF-PM-006`, que producirían un sistema que funciona y un registro que miente; y los tres casos de nivel en `RF-PM-007`, porque la cadena crece hacia abajo y «nivel superior» es **número menor**. Queda declarada una **dependencia invertida**: `RF-PM-003` · `T-01` necesita una eliminación registrada, que escribe `RF-PM-006` · `T-05`, de modo que el orden de implementación no es el de los identificadores sino `001 → 002 → 005 → 006 → 003 → 004 → 007`. Y **tres tareas tocan código fuera de `PM`** —las dos interfaces de `SP`, la tercera de `RF-PM-007` y la lectura de `shared/audit`—: son las de mayor riesgo del módulo, y su definición de terminado exige que la suite de `SP` siga en verde sin cambios. | Responsable del proyecto |
| 0.68.0 | 27-08-2026 | **Los productos ganan vigencia de adquisición en días** (`RN-PM-015`), por decisión del responsable del proyecto, y **las siete tripletas vuelven a su compuerta y se reaprueban** (Art. I.7) — el mismo día en que se habían aprobado, y **sin una línea de código escrita**: el cambio costó una edición donde después de `V39` habría costado una migración. Es **opcional y en los dos tipos**: sin ella, lo adquirido no caduca. Cinco specs enmendadas —`RF-PM-001` la recibe como dato, `RF-PM-002`, `RF-PM-003` y `RF-PM-007` la devuelven, y `RF-PM-004` la hace corregible con el mismo criterio que el precio—, cinco planes y cinco listas de tareas, que pasan de 95 a **100**. Cuatro criterios nuevos, `CA-PM-092` a `CA-PM-095`. **Dos condiciones más sobre la compra futura** quedan escritas en `requirements/pm.md` §1.4: cada compra guardará **la vigencia que compró**, o corregirla reescribiría lo vendido; y **al vencer, la persona se queda sin nivel vigente** —no vuelve al anterior, que habría exigido que la compra lo guardase, ni baja al más bajo, que castigaría a quien ya estaba arriba—. | Responsable del proyecto |
| 0.69.0 | 27-08-2026 | **`RF-PM-001` implementado**: el módulo `PM` estrena código. Sus dieciocho tareas quedan `Hecha` y `./mvnw clean verify` termina en verde con **170 pruebas unitarias y 616 de integración**. Entran `V39` —la tabla con sus seis `CHECK` y tres índices únicos, dos parciales y uno total— y `V40`, que siembra los cuatro permisos **y los asocia a `SUPERADMIN` y `ADMIN` en la misma migración**. **D-25 pasa de decisión a código**: `SP` publica el catálogo de membresías y el de monedas, y una regla de ArchUnit impide que `modules/products` importe nada de su dominio. **Las pruebas encontraron tres defectos, ninguno por casualidad.** El más instructivo: el caso de uso buscaba la membresía destino **antes** de comprobar que el upgrade la trajera, de modo que un upgrade sin destino salía como `422` «la membresía no existe» —sobre un dato que el actor nunca envió— en lugar del `400` que le corresponde; lo destapó exactamente la prueba que el plan señalaba como imprescindible, la de `RN-PM-002` en los dos sentidos. Los otros dos: un choque de nombre de bean con el `MembershipCatalog` interno de usuarios —que dejó al descubierto que **son la misma lectura declarada dos veces**, anotado como deuda— y el crecimiento del catálogo de permisos de 24 a 28, que hizo fallar seis pruebas de `SP` escritas como lista cerrada; se ampliaron en lugar de aflojarse, porque esa fricción es la que impide que aparezca un permiso que nadie revisó. | Responsable técnico |
| 0.70.0 | 27-08-2026 | **`RF-PM-002` construido**: el catálogo se puede ver, no solo crear. Sus quince tareas quedan `Hecha` con **16 pruebas unitarias y 29 de integración** nuevas en verde. Entra `V41` con los dos índices del listado —trigramas sobre el nombre normalizado y `(created_at DESC, id DESC)`—, y deja escrito **lo que no se indexa y por qué**: `type` y `status` son dominios de dos valores y un índice sobre ellos no acota nada que el recorrido no acote igual. Tres decisiones del listado merecen leerse juntas. **El orden es total**: el desempate por identificador no es cosmético, y su prueba recorre las once páginas de veintiún productos del **mismo instante de alta** para comprobar que ninguna fila se repite ni se queda sin salir — sin desempate, el defecto no se manifiesta como error sino como «faltan productos». **El conteo es exacto siempre**, sin el atajo de `RF-SP-002`: omitirlo cuando la página no se llena es correcto salvo en la página vacía más allá de la última, donde deducir el total del desplazamiento devuelve un número inventado con la colección vacía y sin error que lo delate. Y **los `400` salen juntos**: la paginación, el tipo, el estado y el orden se validan y se acumulan, de modo que quien se equivocó en cuatro parámetros corrige una vez. Se estrenan además las **dos primeras pruebas de plan del proyecto** —siembran doscientos productos a propósito, porque con pocas filas el planificador elige recorrido secuencial y la prueba no probaría nada—, que es justo el hueco que `SP` lleva abierto en `RF-SP-021` · `T-11` y `RF-SP-025` · `T-14`. El listado **no lleva el motivo del retiro** (`CA-PM-077`): la proyección ni lo selecciona, que es lo único que hace verificable el criterio. | Responsable técnico |
| 0.71.0 | 27-08-2026 | **`RF-PM-003` construido**: el detalle de un producto, con **doce tareas** y 5 pruebas unitarias y 25 de integración nuevas en verde. Lo que este requerimiento resolvió de verdad no es el endpoint sino **de dónde sale el motivo del retiro**: no está en `products` —el Art. V.13 lo manda al registro de eliminación— y `PM` no puede unir una tabla que no gobierna. Se cierra con una **lectura estrecha publicada por `shared/audit`**, registrada en [`architecture.md` §15.3](architecture.md) como norma general y no como apaño de este módulo: devuelve **una sola columna** y no una fila, pregunta por **una entidad concreta** sin filtros ni paginación, y **el módulo es parte de la clave**, de modo que preguntar por lo ajeno no devuelve nada — con su prueba, que es lo que impide que el puerto se convierta en la puerta trasera de la auditoría. Tres decisiones más quedan escritas: el motivo se pide **solo si el producto está retirado**, y una prueba de número de sentencias lo sujeta —un producto vivo cuesta **una** consulta, no dos—; `deletedAt` y `deletionReason` **no aparecen** en un producto vivo, porque su ausencia *significa* que sigue vivo; y la respuesta **no lleva autoría en ninguna forma** (`CA-PM-081`), comprobado sobre el cuerpo entero y no campo a campo, que es la única manera de demostrar que no está lo que a nadie se le ocurrió buscar. **El bloqueo declarado se cerró por ser menor de lo que parecía**: `T-01` necesitaba una *fila* en el registro, no el caso de uso que la escribe. Lo que sigue faltando —y queda anotado— es el recorrido de extremo a extremo, que llega con `RF-PM-006`. De paso se unificó la escala del precio en un solo componente y se resolvió una contradicción entre dos specs aprobadas: `RF-PM-001` recortaba a la baja lo que no cabe en la moneda y `RF-PM-003` §13 pide devolver lo almacenado — manda la segunda, porque recortar escondería el dato inválido justo donde alguien podría verlo. | Responsable técnico |
| 0.72.0 | 27-08-2026 | **`RF-PM-005` construido**, y con él **`RN-PM-004` deja de ser papel**: trece tareas, 4 pruebas unitarias y 27 de integración —**tres de ellas concurrentes**—. Desde que el producto nace inactivo, `PATCH /api/v1/products/{id}/status` es **la única puerta** por la que un upgrade puede quedar activo, y por tanto el único sitio donde dos precios simultáneos para el mismo nivel podrían entrar. **Se comprobó cuál de las dos piezas sostiene la regla, y no de palabra**: se desactivó la comprobación previa y se volvió a correr la suite. Las pruebas concurrentes **siguieron en verde** —con dos y con tres activaciones simultáneas queda exactamente una, porque quien decide es `uq_products_upgrade_target`— y la que falló fue la del mensaje, en `$.detail`: el `409` seguía siendo correcto pero dejaba de **nombrar** al producto que ocupa el destino. Es el reparto que el plan describía, ahora demostrado: **la restricción decide, la comprobación redacta**, y quitar cualquiera de las dos rompe algo distinto. Tres decisiones más quedan escritas: la petición que **no cambia nada** responde `200`, no escribe la fila y **no registra evento** —quien pulsa dos veces el mismo botón no ha hecho nada malo—; `RN-PM-014` exige descripción **solo al publicar**, porque la regla acota lo que se ofrece y exigirla para desactivar dejaría atrapado en la oferta al producto peor documentado; y **desactivar no comprueba nada del destino**, comprobado contando sentencias y no confiando en que no falle. El bloqueo declarado se cerró: lo que `T-02` debía demostrar es que la regla **no es una puerta cerrada para siempre**, y quién ponga la descripción no cambia lo que se comprueba. | Responsable técnico |
| 0.73.0 | 27-08-2026 | **`RF-PM-006` construido**: trece tareas, 14 pruebas unitarias y 15 de integración —dos concurrentes—. Con él **se cierra el recorrido que `RF-PM-003` dejó abierto**: aquella prueba sembraba las dos mitades del retiro a mano porque este requerimiento no existía; ahora se retira por su endpoint y el detalle lee el motivo por el puerto estrecho de `shared/audit`. Lo que define esta operación **no es lo que hace sino el orden**, y las dos piezas que la definen son las dos que se hacen mal sin fallar: la **instantánea se captura antes de tocar nada** —capturarla después dejaría un registro que dice qué *quedó* del producto y no qué *era*— y **el estado no se toca** —desactivarlo «de paso» haría que todos los registros dijeran «inactivo» y ese dato dejaría de significar nada, con la salvaguarda destruyendo la evidencia que protege—. Las dos tienen prueba que compara contra el estado **anterior**, no contra el resultado. **Se corrigió un defecto del `plan.md` bajo el Art. I.7**: proponía `DELETE` con cuerpo justificándolo en que «es lo que hacen `RF-SP-009` y `RF-SP-029`», y al comprobarlo contra el código resultó **falso** — los dos exponen `POST /{id}/deletion`, porque RFC 9110 no define semántica para el cuerpo de un `DELETE` y un intermediario puede descartarlo. Se corrigió el plan y no el sistema; la `spec.md` no se toca, porque no menciona ningún verbo. Dos decisiones más: **`EX-002` es un `409` distinguible** del `404` —al eliminar una persona los dos casos comparten el `404` para no revelar que existió, y aquí no hay nada que ocultar porque el catálogo devuelve los retirados a cualquiera con `products:read`—, y la **instantánea la arma ahora el agregado**, compartida con el alta, para que el registro de creación y el de eliminación no describan el mismo producto con claves distintas. Una prueba sola cubre las **tres unicidades**: el retiro libera el destino y el nombre, y **nunca el código**. | Responsable técnico |
| 0.74.0 | 27-08-2026 | **`RF-PM-004` construido**, y con él **`PM` tiene sus seis operaciones de catálogo**: quince tareas, 11 pruebas unitarias y 32 de integración —dos concurrentes—. El problema de fondo de esta operación no era la validación sino **distinguir tres estados de un campo** —ausente, presente y nulo, presente con valor—, y de ahí salen dos comportamientos opuestos: la descripción y la vigencia **admiten vaciarse** y el nombre **no**. Se reutilizó `Patchable`, que `RF-SP-027` creó tras el intento fallido con `Optional`, y se escribió un **deserializador genérico** en `shared/patch` para no repetirlo cinco veces: el que existía solo servía para texto y aquí hacen falta los tres estados en un importe, un identificador y un número de días. **La prueba concurrente encontró un defecto que el plan no cubría, y por poco**: advertía del vaciado disparado por el `SELECT` de unicidad, y el hueco real estaba después — dos productos **distintos** corrigiéndose hacia el mismo nombre **no se serializan**, porque cada uno bloquea su propia fila, de modo que los dos pasan la comprobación previa y la violación del índice llega **en el `commit`**, fuera del caso de uso, como `500` en lugar de `409`. Se cierra con un volcado explícito, la misma corrección que `RF-SP-027` aplicó al correo. **Y una prueba destapó otro defecto de raíz compartida**: cambiar **solo** la moneda de un producto se rechazaba por decimales que su precio no tiene, porque el precio leído de la base trae la escala de la columna —`numeric(14,4)`, de modo que `49.99` llega como `49.9900`—. La comparación pasó a hacerse sobre la escala **significativa**, y **el alta usa ahora la misma regla**: el mismo importe no puede ser válido por un endpoint e inválido por otro. Los tres campos inmutables —tipo, código y destino— se **declaran** en el DTO para poder rechazarlos con `VAL-006` y su propio mensaje: sin declararlos, quien intentara cambiar el código leería «propiedad desconocida» y creería haberse equivocado de nombre. | Responsable técnico |
| 0.77.0 | 28-08-2026 | **El código de un país pasa a ISO 3166-1 alfa-3**, por decisión del responsable del proyecto: `COL` donde antes iba `CO`. Toca las dos mitades del sistema. En el backend, `CountryCode` valida `^[A-Z]{3}$`, `Country.code` es `char(3)` y la migración **`V42`** hace el cambio en el esquema — `V16` no se edita, que está aplicada y Flyway valida por suma de comprobación. **La migración no se limita a ensanchar la columna**: `char(2)` a `char(3)` convierte `CO` en `'CO '`, no en `COL`, y esa fila pasaría el `NOT NULL` y el `UNIQUE`; `V42` ensancha, traduce con una tabla de equivalencias explícita, **falla nombrando los códigos que no sabe traducir** —adivinar sería irreversible, porque `RN-SP-009` no admite editar un país— y solo entonces repone el `CHECK`. Verificada sobre datos reales: `CO`, `US` y `PA` quedan `COL`, `USA` y `PAN`, y con un `ZW` sin equivalencia la migración se detiene diciendo `ZW`. Enmienda la tripleta de `RF-SP-020` (Art. I.7) con `T-18` a `T-20`, y `requirements/sp.md`, `modelo-datos.md` y `flujos-por-caso.md` dejan de afirmar dos letras. **El contrato publicado cambia**, de modo que la copia del frontend queda vieja: `docs/api/openapi.json` es la autoridad. | Responsable técnico |
| 0.78.0 | 28-08-2026 | **Entra el tercer módulo del sistema: `CM` — Comisiones**, con sus **cinco requerimientos** registrados y **diez reglas propias**. Es el **primero que depende de dos módulos** —`SP` le da el rol y la persona, `PM` el producto—, y la dependencia sigue siendo acíclica. Nace con las **tarifas y no con el cálculo**, y esa mitad no se aplaza por reparto sino porque **no hay sobre qué calcular**: ninguna tabla de ventas existe. **Se pidió como submódulo de `PM` y se decidió que fuera módulo propio**, por [`modules.md` §2.1](modules.md#21-regla-de-decision): la comisión no opera sobre `products`, opera sobre `roles` y `users`, y el identificador es irreversible. La comisión se declara en **cuatro grados** —rol, rol y producto, persona, persona y producto— donde **la ausencia es la que da el alcance**, **rige durante un periodo con inicio obligatorio y fin opcional**, y admite el **cero** como porcentaje válido, que es la única forma de exceptuar un producto a un rol con tarifa por omisión. Su precedencia (`RN-CM-004`) vive en un solo sitio para que ningún consumidor la reimplemente, y `RN-CM-006` pasa a ser una regla de **no solapamiento** —ningún día cubierto dos veces—, que es una **exclusión** y no una unicidad: la restricción más difícil de declarar del módulo. Deja **tres cosas escritas hacia fuera**: `PM` deberá publicar una interfaz de lectura de productos que hoy no tiene; la liquidación futura deberá guardar el porcentaje que aplicó, o corregir una tarifa reescribiría lo pagado; y `SP` gana **`RN-SP-025`** —una persona no puede portar dos roles de tipo `VENDEDOR`—, que enmienda la tripleta de `RF-SP-030` y todavía no está implementada. **Los cinco requerimientos quedan con alcance global explícito** y en la lista de los que hay que revisar cuando se cierre **D-22**. | Responsable del proyecto |
| 0.79.0 | 28-08-2026 | **Las cinco `spec.md` de `CM` quedan redactadas** y los cinco requerimientos pasan a `Spec en revisión`. **No traen ninguna pregunta abierta**: las cuatro que había —el porcentaje cero, la vigencia, el alcance de la resolución y qué pasa con el producto retirado— las resolvió el responsable del proyecto antes de redactarlas. Lo que sí queda declarado en tres de ellas es un **condicionante que no es una pregunta**: `RF-CM-002` es el requerimiento que **D-22 puede obligar a cambiar**, porque se especifica con alcance global explícito; y `RF-CM-005` **no puede darse por terminado** mientras `RN-SP-025` —un solo rol vendedor por persona— siga sin implementarse, porque de esa regla depende que su resolución sea determinista. Las especificaciones fijan además dos distinciones que sostienen el módulo entero y que es fácil colapsar: **corregir no es cambiar** —corregir reescribe lo que una tarifa dice que rigió; cambiar es cerrar la vigente y registrar otra—, y **retirar no es cerrar la vigencia** —se retira lo que no debió existir, se cierra lo que dejó de regir—. De ahí que `RF-CM-004` **no toque la vigencia al retirar**: hacerlo destruiría el dato de qué periodo cubría lo retirado, que es el mismo criterio con el que `RF-PM-006` no toca el estado de un producto. | Responsable técnico |
