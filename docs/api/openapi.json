{
  "components" : {
    "schemas" : {
      "AssignMembershipRequest" : {
        "properties" : {
          "endsAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "membershipId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "membershipId" ],
        "type" : "object"
      },
      "AssignRolesRequest" : {
        "properties" : {
          "roleIds" : {
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "maxItems" : 100,
            "minItems" : 0,
            "type" : "array"
          },
          "supervisorId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "roleIds" ],
        "type" : "object"
      },
      "AssignSupervisorRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          },
          "supervisorId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "supervisorId" ],
        "type" : "object"
      },
      "AssociatePackageProductRequest" : {
        "properties" : {
          "discountType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "discountValue" : {
            "type" : "number"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "discountType", "discountValue", "productId" ],
        "type" : "object"
      },
      "AuditActor" : {
        "properties" : {
          "fullName" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "Author" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "BrokerAccountHolder" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "BrokerAccountItem" : {
        "properties" : {
          "accountId" : {
            "type" : "string"
          },
          "broker" : {
            "$ref" : "#/components/schemas/BrokerRef"
          },
          "brokerUsername" : {
            "type" : "string"
          },
          "declaredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "status" : {
            "enum" : [ "REGISTER", "FIRST_DEPOSIT" ],
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "BrokerAccountsByBroker" : {
        "properties" : {
          "broker" : {
            "$ref" : "#/components/schemas/BrokerRef"
          },
          "total" : {
            "format" : "int64",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "BrokerAccountsPage" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/TeamBrokerAccountItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "summary" : {
            "$ref" : "#/components/schemas/BrokerAccountsSummary"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "BrokerAccountsResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/BrokerAccountItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "BrokerAccountsSummary" : {
        "properties" : {
          "accounts" : {
            "$ref" : "#/components/schemas/BrokerAccountsTotals"
          },
          "firstDeposit" : {
            "$ref" : "#/components/schemas/BrokerAccountsTotals"
          },
          "register" : {
            "$ref" : "#/components/schemas/BrokerAccountsTotals"
          }
        },
        "type" : "object"
      },
      "BrokerAccountsTotals" : {
        "properties" : {
          "byBroker" : {
            "items" : {
              "$ref" : "#/components/schemas/BrokerAccountsByBroker"
            },
            "type" : "array"
          },
          "total" : {
            "format" : "int64",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "BrokerCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/BrokerItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "BrokerItem" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isActive" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "BrokerRef" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ChangeAuditItem" : {
        "properties" : {
          "action" : {
            "type" : "string"
          },
          "actor" : {
            "$ref" : "#/components/schemas/AuditActor"
          },
          "actorId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "changes" : {
            "$ref" : "#/components/schemas/JsonNode"
          },
          "correlationId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "entity" : {
            "type" : "string"
          },
          "entityId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "ipAddress" : {
            "type" : "string"
          },
          "module" : {
            "type" : "string"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "userAgent" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ChangeCountryStatusRequest" : {
        "properties" : {
          "isActive" : {
            "type" : "boolean"
          }
        },
        "required" : [ "isActive" ],
        "type" : "object"
      },
      "ChangeCurrencyStatusRequest" : {
        "properties" : {
          "isActive" : {
            "type" : "boolean"
          }
        },
        "required" : [ "isActive" ],
        "type" : "object"
      },
      "ChangePackageStatusRequest" : {
        "properties" : {
          "status" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "status" ],
        "type" : "object"
      },
      "ChangePasswordRequest" : {
        "properties" : {
          "currentPassword" : {
            "type" : "string"
          },
          "newPassword" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ChangeProductStatusRequest" : {
        "properties" : {
          "status" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "status" ],
        "type" : "object"
      },
      "ChangeRoleParentRequest" : {
        "properties" : {
          "parentRoleId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "parentRoleId" ],
        "type" : "object"
      },
      "ChangeRoleStatusRequest" : {
        "properties" : {
          "status" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "status" ],
        "type" : "object"
      },
      "ChangeUserStatusRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CommercialStructureResponse" : {
        "properties" : {
          "previousSupervisor" : {
            "$ref" : "#/components/schemas/Person"
          },
          "previousSupervisorEndedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "supervisor" : {
            "$ref" : "#/components/schemas/Person"
          },
          "team" : {
            "$ref" : "#/components/schemas/PageResponsePerson"
          },
          "user" : {
            "$ref" : "#/components/schemas/Person"
          }
        },
        "type" : "object"
      },
      "CommissionRateItem" : {
        "properties" : {
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "fixedAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "percentage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/CommissionRateProduct"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "role" : {
            "$ref" : "#/components/schemas/CommissionRateRole"
          }
        },
        "type" : "object"
      },
      "CommissionRatePageResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/CommissionRateItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "sort" : {
            "type" : "string"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "CommissionRateProduct" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CommissionRateProductCurrency"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "CommissionRateProductCurrency" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "decimalPlaces" : {
            "format" : "int32",
            "type" : "integer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CommissionRateResponse" : {
        "properties" : {
          "fixedAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "percentage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/CommissionRateProduct"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "role" : {
            "$ref" : "#/components/schemas/CommissionRateRole"
          }
        },
        "type" : "object"
      },
      "CommissionRateRole" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ContactRef" : {
        "properties" : {
          "addressLine1" : {
            "type" : "string"
          },
          "addressLine2" : {
            "type" : "string"
          },
          "city" : {
            "type" : "string"
          },
          "companyPhone" : {
            "type" : "string"
          },
          "phone" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CorrectPackageDiscountRequest" : {
        "properties" : {
          "discountType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "discountValue" : {
            "type" : "number"
          }
        },
        "required" : [ "discountType", "discountValue" ],
        "type" : "object"
      },
      "CountryCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/CountryResponse"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "CountryRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CountryResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isActive" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CreateProductCommentRequest" : {
        "properties" : {
          "comment" : {
            "description" : "El texto de la reseña, de 1 a 1000 caracteres sin contar los espacios de los extremos. Se guarda tal cual y NO se sanea: el front debe escaparlo al pintarlo, porque la lista es pública.",
            "maxLength" : 1000,
            "type" : "string"
          },
          "rating" : {
            "description" : "Puntuación entera de 1 a 5. Un decimal se rechaza, no se redondea.",
            "format" : "int32",
            "maximum" : 5,
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "comment", "rating" ],
        "type" : "object"
      },
      "CreateRoleRequest" : {
        "properties" : {
          "code" : {
            "maxLength" : 50,
            "minLength" : 0,
            "pattern" : "^[A-Z][A-Z0-9_]*$",
            "type" : "string"
          },
          "description" : {
            "maxLength" : 500,
            "minLength" : 0,
            "type" : "string"
          },
          "name" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          },
          "parentRoleId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "permissionIds" : {
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "type" : "array"
          },
          "roleType" : {
            "enum" : [ "FUNCIONARIO", "VENDEDOR", "CONSUMIDOR" ],
            "type" : "string"
          }
        },
        "required" : [ "code", "name", "parentRoleId", "roleType" ],
        "type" : "object"
      },
      "CurrencyCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/CurrencyResponse"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "CurrencyRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "decimalPlaces" : {
            "format" : "int32",
            "type" : "integer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "CurrencyResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "decimalPlaces" : {
            "format" : "int32",
            "type" : "integer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isActive" : {
            "type" : "boolean"
          },
          "isDefault" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          },
          "symbol" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeleteCommissionRateRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeletePackageRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeleteProductRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeleteRoleRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeleteUserRequest" : {
        "properties" : {
          "reason" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DeletionAuditItem" : {
        "properties" : {
          "actor" : {
            "$ref" : "#/components/schemas/AuditActor"
          },
          "actorId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "correlationId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "deletionType" : {
            "type" : "string"
          },
          "entity" : {
            "type" : "string"
          },
          "entityId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "ipAddress" : {
            "type" : "string"
          },
          "module" : {
            "type" : "string"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "reason" : {
            "type" : "string"
          },
          "snapshot" : {
            "$ref" : "#/components/schemas/JsonNode"
          },
          "userAgent" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DiscountRef" : {
        "properties" : {
          "type" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "value" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "DocumentRef" : {
        "properties" : {
          "number" : {
            "type" : "string"
          },
          "type" : {
            "$ref" : "#/components/schemas/DocumentTypeRef"
          }
        },
        "type" : "object"
      },
      "DocumentTypeCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/DocumentTypeItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "DocumentTypeItem" : {
        "properties" : {
          "abbreviation" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isActive" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "DocumentTypeRef" : {
        "properties" : {
          "abbreviation" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "EffectiveCommissionResponse" : {
        "properties" : {
          "onDate" : {
            "format" : "date",
            "type" : "string"
          },
          "outcome" : {
            "enum" : [ "RESUELTA", "SIN_TARIFA", "NO_COMISIONA" ],
            "type" : "string"
          },
          "rateId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "roleId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "source" : {
            "enum" : [ "PERSONALIZADA", "ROL" ],
            "type" : "string"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          },
          "value" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "ErrorAuditItem" : {
        "properties" : {
          "actor" : {
            "$ref" : "#/components/schemas/AuditActor"
          },
          "actorId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "correlationId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "entityId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "errorCode" : {
            "type" : "string"
          },
          "errorType" : {
            "type" : "string"
          },
          "httpStatus" : {
            "format" : "int32",
            "type" : "integer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "ipAddress" : {
            "type" : "string"
          },
          "message" : {
            "type" : "string"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "operation" : {
            "type" : "string"
          },
          "resource" : {
            "type" : "string"
          },
          "severity" : {
            "type" : "string"
          },
          "userAgent" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ExchangeRateResponse" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isActive" : {
            "type" : "boolean"
          },
          "price" : {
            "type" : "number"
          },
          "sourceCurrency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "targetCurrency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ExchangeRef" : {
        "properties" : {
          "amount" : {
            "type" : "number"
          },
          "currency" : {
            "$ref" : "#/components/schemas/TargetCurrency"
          },
          "rate" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ExcludedCountry" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "HotlinkCatalogResponse" : {
        "properties" : {
          "services" : {
            "$ref" : "#/components/schemas/Offered"
          },
          "upgrades" : {
            "$ref" : "#/components/schemas/Offered"
          }
        },
        "type" : "object"
      },
      "HotlinkResponse" : {
        "properties" : {
          "product" : {
            "$ref" : "#/components/schemas/ProductRef"
          },
          "seller" : {
            "$ref" : "#/components/schemas/SellerRef"
          }
        },
        "type" : "object"
      },
      "Item" : {
        "properties" : {
          "discount" : {
            "$ref" : "#/components/schemas/DiscountRef"
          },
          "priceInPackage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/ProductRef"
          }
        },
        "type" : "object"
      },
      "JsonNode" : { },
      "Line" : {
        "properties" : {
          "discount" : {
            "$ref" : "#/components/schemas/DiscountRef"
          },
          "priceInPackage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/OfferItem"
          }
        },
        "type" : "object"
      },
      "ListCommissionRatesRequest" : {
        "properties" : {
          "includeDeleted" : {
            "type" : "boolean"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "roleId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "ListCountriesRequest" : {
        "properties" : {
          "includeInactive" : {
            "type" : "boolean"
          },
          "search" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ListCurrenciesRequest" : {
        "properties" : {
          "includeInactive" : {
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ListMembershipsRequest" : {
        "properties" : {
          "search" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ListPermissionsRequest" : {
        "properties" : {
          "action" : {
            "type" : "string"
          },
          "resource" : {
            "type" : "string"
          },
          "search" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ListUserCommissionRatesRequest" : {
        "properties" : {
          "includeDeleted" : {
            "type" : "boolean"
          },
          "onDate" : {
            "format" : "date",
            "type" : "string"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "userId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "LoginRequest" : {
        "properties" : {
          "identifier" : {
            "minLength" : 1,
            "type" : "string"
          },
          "password" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "identifier", "password" ],
        "type" : "object"
      },
      "LogoutRequest" : {
        "properties" : {
          "allSessions" : {
            "type" : "boolean"
          },
          "refreshToken" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "refreshToken" ],
        "type" : "object"
      },
      "MembershipBadge" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "color" : {
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "MembershipChainResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/Item"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "MembershipDetailResponse" : {
        "properties" : {
          "childMembership" : {
            "$ref" : "#/components/schemas/Neighbor"
          },
          "code" : {
            "type" : "string"
          },
          "color" : {
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "level" : {
            "format" : "int32",
            "type" : "integer"
          },
          "name" : {
            "type" : "string"
          },
          "parentMembership" : {
            "$ref" : "#/components/schemas/Neighbor"
          }
        },
        "type" : "object"
      },
      "MembershipRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "color" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "level" : {
            "format" : "int32",
            "type" : "integer"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "MembershipResponse" : {
        "properties" : {
          "childMembershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "code" : {
            "type" : "string"
          },
          "color" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "level" : {
            "format" : "int32",
            "type" : "integer"
          },
          "name" : {
            "type" : "string"
          },
          "parentMembershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "MyMovement" : {
        "description" : "Un movimiento en el que participa quien consulta.",
        "properties" : {
          "client" : {
            "$ref" : "#/components/schemas/MyMovementParty"
          },
          "code" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/MyMovementCurrency"
          },
          "discountAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "payableAmount" : {
            "type" : "number"
          },
          "paymentMethod" : {
            "type" : "string"
          },
          "role" : {
            "description" : "El papel de quien consulta en ESTE movimiento.",
            "enum" : [ "BUYER", "SELLER", "BOTH" ],
            "type" : "string"
          },
          "seller" : {
            "$ref" : "#/components/schemas/MyMovementParty",
            "description" : "A quién se atribuye. NULO cuando quien compró no cuelga de ningún vendedor.",
            "type" : [ "object", "null" ]
          },
          "status" : {
            "type" : "string"
          },
          "totalAmount" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "MyMovementCurrency" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "MyMovementParty" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "Neighbor" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "color" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "level" : {
            "format" : "int32",
            "type" : "integer"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "NetworkIndicators" : {
        "properties" : {
          "accounts" : {
            "format" : "int32",
            "type" : "integer"
          },
          "consumers" : {
            "format" : "int32",
            "type" : "integer"
          },
          "conversion" : {
            "type" : "number"
          },
          "ftd" : {
            "format" : "int32",
            "type" : "integer"
          },
          "pending" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "NetworkIndicatorsResponse" : {
        "properties" : {
          "nodes" : {
            "items" : {
              "$ref" : "#/components/schemas/NetworkNode"
            },
            "type" : "array"
          },
          "totals" : {
            "$ref" : "#/components/schemas/NetworkIndicators"
          },
          "unassigned" : {
            "$ref" : "#/components/schemas/NetworkIndicators"
          }
        },
        "type" : "object"
      },
      "NetworkNode" : {
        "properties" : {
          "children" : {
            "items" : {
              "$ref" : "#/components/schemas/NetworkNode"
            },
            "type" : "array"
          },
          "network" : {
            "$ref" : "#/components/schemas/NetworkIndicators"
          },
          "own" : {
            "$ref" : "#/components/schemas/NetworkIndicators"
          },
          "roleCode" : {
            "type" : "string"
          },
          "user" : {
            "$ref" : "#/components/schemas/NetworkNodeUser"
          }
        },
        "type" : "object"
      },
      "NetworkNodeUser" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "OfferItem" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "icon" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "implementation" : {
            "enum" : [ "AUTOMATICA", "MANUAL" ],
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "rating" : {
            "$ref" : "#/components/schemas/RatingSummary"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "targetMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "OfferPackageItem" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "items" : {
            "items" : {
              "$ref" : "#/components/schemas/Line"
            },
            "type" : "array"
          },
          "listPrice" : {
            "type" : "number"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "savings" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "OfferResponse" : {
        "properties" : {
          "currentMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "packages" : {
            "$ref" : "#/components/schemas/OfferedPackages"
          },
          "services" : {
            "$ref" : "#/components/schemas/Offered"
          },
          "upgrades" : {
            "$ref" : "#/components/schemas/Offered"
          }
        },
        "type" : "object"
      },
      "Offered" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/OfferItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "OfferedPackages" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/OfferPackageItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "OwnProfileResponse" : {
        "properties" : {
          "contact" : {
            "$ref" : "#/components/schemas/ContactRef"
          },
          "country" : {
            "$ref" : "#/components/schemas/CountryRef"
          },
          "document" : {
            "$ref" : "#/components/schemas/DocumentRef"
          },
          "email" : {
            "type" : "string"
          },
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastLoginAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "membership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "mustChangePassword" : {
            "type" : "boolean"
          },
          "permissions" : {
            "items" : {
              "type" : "string"
            },
            "type" : "array"
          },
          "roles" : {
            "items" : {
              "$ref" : "#/components/schemas/RoleRef"
            },
            "type" : "array"
          },
          "status" : {
            "type" : "string"
          },
          "supervisor" : {
            "$ref" : "#/components/schemas/SupervisorRef"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PackageDetailResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "deletionReason" : {
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "items" : {
            "items" : {
              "$ref" : "#/components/schemas/PackageItemResponse"
            },
            "type" : "array"
          },
          "listPrice" : {
            "type" : "number"
          },
          "name" : {
            "type" : "string"
          },
          "offerable" : {
            "type" : "boolean"
          },
          "offerableReason" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "savings" : {
            "type" : "number"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PackageHotlinkResponse" : {
        "properties" : {
          "package" : {
            "$ref" : "#/components/schemas/PackageRef"
          },
          "seller" : {
            "$ref" : "#/components/schemas/SellerRef"
          }
        },
        "type" : "object"
      },
      "PackageItemResponse" : {
        "properties" : {
          "discount" : {
            "$ref" : "#/components/schemas/DiscountRef"
          },
          "priceInPackage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/PackageProductRef"
          }
        },
        "type" : "object"
      },
      "PackageItemSummary" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "itemCount" : {
            "format" : "int32",
            "type" : "integer"
          },
          "listPrice" : {
            "type" : "number"
          },
          "name" : {
            "type" : "string"
          },
          "offerable" : {
            "type" : "boolean"
          },
          "price" : {
            "type" : "number"
          },
          "savings" : {
            "type" : "number"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PackagePageResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/PackageItemSummary"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "sort" : {
            "type" : "string"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PackageProductRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "deleted" : {
            "type" : "boolean"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "purchasePrice" : {
            "type" : "number"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PackageRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "items" : {
            "items" : {
              "$ref" : "#/components/schemas/Item"
            },
            "type" : "array"
          },
          "listPrice" : {
            "type" : "number"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "savings" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "PageResponse" : {
        "properties" : {
          "content" : {
            "items" : { },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseChangeAuditItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/ChangeAuditItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseDeletionAuditItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/DeletionAuditItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseErrorAuditItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/ErrorAuditItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseMyMovement" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/MyMovement"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponsePerson" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/Person"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseProductCommentPublicItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/ProductCommentPublicItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseSecurityAuditItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/SecurityAuditItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PageResponseTeamBrokerAccountItem" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/TeamBrokerAccountItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "PasswordRecoveryConfirmation" : {
        "properties" : {
          "newPassword" : {
            "type" : "string"
          },
          "permit" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PasswordRecoveryRequest" : {
        "properties" : {
          "identifier" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PasswordRecoveryResponse" : {
        "properties" : {
          "message" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PatchableBigDecimal" : { },
      "PatchableCommissionRateType" : { },
      "PatchableInteger" : { },
      "PatchableLocalDate" : { },
      "PatchableObject" : { },
      "PatchableProductImplementation" : { },
      "PatchableProductScope" : { },
      "PatchableString" : { },
      "PatchableUUID" : { },
      "PaymentMethodCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/PaymentMethodResponse"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "PaymentMethodResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "excludedCountries" : {
            "items" : {
              "$ref" : "#/components/schemas/ExcludedCountry"
            },
            "type" : "array"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "PermissionCatalogResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/PermissionResponse"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "PermissionResponse" : {
        "properties" : {
          "action" : {
            "type" : "string"
          },
          "code" : {
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "resource" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "Person" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "roles" : {
            "items" : {
              "$ref" : "#/components/schemas/RoleRef"
            },
            "type" : "array"
          },
          "since" : {
            "format" : "date-time",
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductAssociationItem" : {
        "properties" : {
          "commissionRateId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "fixedAmount" : {
            "type" : "number"
          },
          "percentage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/ProductRef"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "role" : {
            "$ref" : "#/components/schemas/RoleRef"
          }
        },
        "type" : "object"
      },
      "ProductAssociationResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/ProductAssociationItem"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "ProductCommentPublicItem" : {
        "properties" : {
          "author" : {
            "$ref" : "#/components/schemas/Author"
          },
          "comment" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rating" : {
            "format" : "int32",
            "type" : "integer"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductCommentResponse" : {
        "properties" : {
          "comment" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rating" : {
            "format" : "int32",
            "type" : "integer"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductDetailResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "deletionReason" : {
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "icon" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "implementation" : {
            "enum" : [ "AUTOMATICA", "MANUAL" ],
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "purchasePrice" : {
            "type" : "number"
          },
          "rating" : {
            "$ref" : "#/components/schemas/RatingSummary"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "sourceMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          },
          "targetMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductItem" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "icon" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "implementation" : {
            "enum" : [ "AUTOMATICA", "MANUAL" ],
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "purchasePrice" : {
            "type" : "number"
          },
          "rating" : {
            "$ref" : "#/components/schemas/RatingSummary"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "sourceMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          },
          "targetMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductPageResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/ProductItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "sort" : {
            "type" : "string"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "ProductRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "icon" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "membership" : {
            "$ref" : "#/components/schemas/MembershipBadge"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "rating" : {
            "$ref" : "#/components/schemas/RatingSummary"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ProductResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "coverImageUrl" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/CurrencyRef"
          },
          "description" : {
            "type" : "string"
          },
          "exchange" : {
            "$ref" : "#/components/schemas/ExchangeRef"
          },
          "icon" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "implementation" : {
            "enum" : [ "AUTOMATICA", "MANUAL" ],
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number"
          },
          "purchasePrice" : {
            "type" : "number"
          },
          "rating" : {
            "$ref" : "#/components/schemas/RatingSummary"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "sourceMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "status" : {
            "enum" : [ "ACTIVO", "INACTIVO" ],
            "type" : "string"
          },
          "targetMembership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RatingSummary" : {
        "properties" : {
          "average" : {
            "type" : "number"
          },
          "count" : {
            "format" : "int64",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "RefreshRequest" : {
        "properties" : {
          "refreshToken" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "refreshToken" ],
        "type" : "object"
      },
      "RegisterCommissionRateRequest" : {
        "properties" : {
          "fixedAmount" : {
            "minimum" : 0.0,
            "type" : "number"
          },
          "percentage" : {
            "maximum" : 100.0,
            "minimum" : 0.0,
            "type" : "number"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "roleId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "productId", "rateType", "roleId" ],
        "type" : "object"
      },
      "RegisterCountryRequest" : {
        "properties" : {
          "code" : {
            "maxLength" : 10,
            "minLength" : 0,
            "type" : "string"
          },
          "name" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          }
        },
        "required" : [ "code", "name" ],
        "type" : "object"
      },
      "RegisterExchangeRateRequest" : {
        "properties" : {
          "isActive" : {
            "type" : "boolean"
          },
          "price" : {
            "minimum" : 0.0,
            "type" : "number"
          },
          "sourceCurrencyId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "targetCurrencyId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "price", "sourceCurrencyId", "targetCurrencyId", "validFrom" ],
        "type" : "object"
      },
      "RegisterMembershipRequest" : {
        "properties" : {
          "childMembershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "code" : {
            "maxLength" : 50,
            "minLength" : 0,
            "pattern" : "^[A-Z][A-Z0-9_]*$",
            "type" : "string"
          },
          "color" : {
            "minLength" : 1,
            "pattern" : "^[0-9a-fA-F]{6}$",
            "type" : "string"
          },
          "description" : {
            "maxLength" : 500,
            "minLength" : 0,
            "type" : "string"
          },
          "name" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          }
        },
        "required" : [ "code", "color", "name" ],
        "type" : "object"
      },
      "RegisterPackageRequest" : {
        "properties" : {
          "code" : {
            "maxLength" : 50,
            "minLength" : 0,
            "pattern" : "^[A-Za-z][A-Za-z0-9_]*$",
            "type" : "string"
          },
          "currencyId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "description" : {
            "maxLength" : 1000,
            "minLength" : 0,
            "type" : "string"
          },
          "name" : {
            "maxLength" : 150,
            "minLength" : 0,
            "type" : "string"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          }
        },
        "required" : [ "code", "currencyId", "name", "scope" ],
        "type" : "object"
      },
      "RegisterProductRequest" : {
        "properties" : {
          "code" : {
            "maxLength" : 50,
            "minLength" : 0,
            "pattern" : "^[A-Za-z][A-Za-z0-9_]*$",
            "type" : "string"
          },
          "currencyId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "description" : {
            "maxLength" : 1000,
            "minLength" : 0,
            "type" : "string"
          },
          "icon" : {
            "maxLength" : 50,
            "minLength" : 0,
            "type" : "string"
          },
          "implementation" : {
            "enum" : [ "AUTOMATICA", "MANUAL" ],
            "type" : "string"
          },
          "name" : {
            "maxLength" : 150,
            "minLength" : 0,
            "type" : "string"
          },
          "price" : {
            "minimum" : 0.0,
            "type" : "number"
          },
          "purchasePrice" : {
            "minimum" : 0.0,
            "type" : "number"
          },
          "scope" : {
            "enum" : [ "TIENDA", "HOTLINK", "AMBOS", "NINGUNO" ],
            "type" : "string"
          },
          "sourceMembershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "targetMembershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "type" : {
            "enum" : [ "UPGRADE_MEMBRESIA", "BOT" ],
            "type" : "string"
          },
          "validityDays" : {
            "format" : "int32",
            "minimum" : 1,
            "type" : "integer"
          },
          "videoUrl" : {
            "type" : "string"
          }
        },
        "required" : [ "code", "currencyId", "implementation", "name", "price", "scope", "type" ],
        "type" : "object"
      },
      "RegisterSaleRequest" : {
        "properties" : {
          "clientId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lines" : {
            "items" : {
              "$ref" : "#/components/schemas/SaleLineRequest"
            },
            "minItems" : 1,
            "type" : "array"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "paymentMethodId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "clientId", "lines" ],
        "type" : "object"
      },
      "RegisterUserCommissionRateRequest" : {
        "properties" : {
          "fixedAmount" : {
            "minimum" : 0.0,
            "type" : "number"
          },
          "percentage" : {
            "maximum" : 100.0,
            "minimum" : 0.0,
            "type" : "number"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "userId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "productId", "rateType", "userId", "validFrom" ],
        "type" : "object"
      },
      "RegisterUserRequest" : {
        "properties" : {
          "addressLine1" : {
            "maxLength" : 150,
            "minLength" : 0,
            "type" : "string"
          },
          "addressLine2" : {
            "maxLength" : 150,
            "minLength" : 0,
            "type" : "string"
          },
          "city" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          },
          "companyPhone" : {
            "maxLength" : 25,
            "minLength" : 0,
            "type" : "string"
          },
          "countryId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "documentNumber" : {
            "maxLength" : 30,
            "minLength" : 0,
            "type" : "string"
          },
          "documentTypeId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "email" : {
            "minLength" : 1,
            "type" : "string"
          },
          "firstName" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          },
          "lastName" : {
            "maxLength" : 100,
            "minLength" : 0,
            "type" : "string"
          },
          "membershipId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "password" : {
            "minLength" : 1,
            "type" : "string"
          },
          "phone" : {
            "maxLength" : 25,
            "minLength" : 0,
            "type" : "string"
          },
          "roleIds" : {
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "maxItems" : 100,
            "minItems" : 0,
            "type" : "array"
          },
          "supervisorId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "username" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "countryId", "documentNumber", "documentTypeId", "email", "firstName", "lastName", "password", "phone", "roleIds", "username" ],
        "type" : "object"
      },
      "RegistrationBrokerAccount" : {
        "properties" : {
          "accountId" : {
            "type" : "string"
          },
          "brokerId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RegistrationMovement" : {
        "properties" : {
          "movementTypeCode" : {
            "type" : "string"
          },
          "paymentMethodId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "sellerUsername" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "productId", "sellerUsername" ],
        "type" : "object"
      },
      "ResetPasswordRequest" : {
        "properties" : {
          "newPassword" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RevokeRolesRequest" : {
        "properties" : {
          "roleIds" : {
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "maxItems" : 100,
            "minItems" : 0,
            "type" : "array"
          }
        },
        "required" : [ "roleIds" ],
        "type" : "object"
      },
      "RoleDetailResponse" : {
        "properties" : {
          "assignedUserCount" : {
            "format" : "int64",
            "type" : "integer"
          },
          "childRoleCount" : {
            "format" : "int64",
            "type" : "integer"
          },
          "code" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isSystem" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          },
          "parentRole" : {
            "$ref" : "#/components/schemas/RoleSummaryResponse"
          },
          "permissions" : {
            "items" : {
              "$ref" : "#/components/schemas/PermissionResponse"
            },
            "type" : "array"
          },
          "roleType" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RolePermissionsRequest" : {
        "properties" : {
          "permissionIds" : {
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "maxItems" : 100,
            "minItems" : 0,
            "type" : "array",
            "uniqueItems" : true
          }
        },
        "required" : [ "permissionIds" ],
        "type" : "object"
      },
      "RoleRef" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RoleResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "description" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "isSystem" : {
            "type" : "boolean"
          },
          "name" : {
            "type" : "string"
          },
          "parentRole" : {
            "$ref" : "#/components/schemas/RoleSummaryResponse"
          },
          "permissions" : {
            "items" : {
              "$ref" : "#/components/schemas/PermissionResponse"
            },
            "type" : "array"
          },
          "roleType" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "RoleSummaryResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SaleCurrency" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SaleLineRequest" : {
        "properties" : {
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "quantity" : {
            "format" : "int32",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "productId", "quantity" ],
        "type" : "object"
      },
      "SaleLineResponse" : {
        "properties" : {
          "lineAmount" : {
            "type" : "number"
          },
          "productCode" : {
            "type" : "string"
          },
          "productId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "productName" : {
            "type" : "string"
          },
          "quantity" : {
            "format" : "int32",
            "type" : "integer"
          },
          "unitPrice" : {
            "type" : "number"
          },
          "validityDays" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "SaleParty" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SaleResponse" : {
        "properties" : {
          "client" : {
            "$ref" : "#/components/schemas/SaleParty"
          },
          "code" : {
            "type" : "string"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "currency" : {
            "$ref" : "#/components/schemas/SaleCurrency"
          },
          "discountAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lines" : {
            "items" : {
              "$ref" : "#/components/schemas/SaleLineResponse"
            },
            "type" : "array"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "payableAmount" : {
            "type" : "number"
          },
          "paymentMethod" : {
            "type" : "string"
          },
          "seller" : {
            "$ref" : "#/components/schemas/SaleParty",
            "description" : "A quién se atribuye la venta. NULO cuando quien compra no cuelga de ningún vendedor — un agente, o la cúspide de la fuerza comercial. Esa venta NO comisiona a nadie.",
            "type" : [ "object", "null" ]
          },
          "status" : {
            "type" : "string"
          },
          "totalAmount" : {
            "type" : "number"
          }
        },
        "type" : "object"
      },
      "SecurityAuditItem" : {
        "properties" : {
          "actor" : {
            "$ref" : "#/components/schemas/AuditActor"
          },
          "actorId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "correlationId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "detail" : {
            "$ref" : "#/components/schemas/JsonNode"
          },
          "eventType" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "ipAddress" : {
            "type" : "string"
          },
          "occurredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "outcome" : {
            "type" : "string"
          },
          "severity" : {
            "type" : "string"
          },
          "targetUser" : {
            "$ref" : "#/components/schemas/AuditActor"
          },
          "targetUserId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "userAgent" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SelfRegistrationRequest" : {
        "properties" : {
          "addressLine1" : {
            "type" : "string"
          },
          "addressLine2" : {
            "type" : "string"
          },
          "brokerAccounts" : {
            "items" : {
              "$ref" : "#/components/schemas/RegistrationBrokerAccount"
            },
            "type" : "array"
          },
          "city" : {
            "type" : "string"
          },
          "countryCode" : {
            "minLength" : 1,
            "type" : "string"
          },
          "documentNumber" : {
            "minLength" : 1,
            "type" : "string"
          },
          "documentType" : {
            "minLength" : 1,
            "type" : "string"
          },
          "email" : {
            "minLength" : 1,
            "type" : "string"
          },
          "firstName" : {
            "minLength" : 1,
            "type" : "string"
          },
          "lastName" : {
            "minLength" : 1,
            "type" : "string"
          },
          "movement" : {
            "$ref" : "#/components/schemas/RegistrationMovement"
          },
          "password" : {
            "minLength" : 1,
            "type" : "string"
          },
          "phone" : {
            "minLength" : 1,
            "type" : "string"
          },
          "username" : {
            "minLength" : 1,
            "type" : "string"
          }
        },
        "required" : [ "countryCode", "documentNumber", "documentType", "email", "firstName", "lastName", "movement", "password", "phone", "username" ],
        "type" : "object"
      },
      "SelfRegistrationResponse" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "pending" : {
            "type" : "string"
          },
          "sale" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SellerRef" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SessionResponse" : {
        "properties" : {
          "accessToken" : {
            "type" : "string"
          },
          "expiresIn" : {
            "format" : "int64",
            "type" : "integer"
          },
          "mustChangePassword" : {
            "type" : "boolean"
          },
          "refreshToken" : {
            "type" : "string"
          },
          "tokenType" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "SupervisorRef" : {
        "properties" : {
          "firstName" : {
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "roleCode" : {
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "TargetCurrency" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "decimalPlaces" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "TeamBrokerAccountItem" : {
        "properties" : {
          "accountId" : {
            "type" : "string"
          },
          "broker" : {
            "$ref" : "#/components/schemas/BrokerRef"
          },
          "brokerUsername" : {
            "type" : "string"
          },
          "declaredAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "status" : {
            "enum" : [ "REGISTER", "FIRST_DEPOSIT" ],
            "type" : "string"
          },
          "user" : {
            "$ref" : "#/components/schemas/BrokerAccountHolder"
          }
        },
        "type" : "object"
      },
      "UpdateCommissionRateRequest" : {
        "properties" : {
          "fixedAmount" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "percentage" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "rateType" : {
            "$ref" : "#/components/schemas/PatchableCommissionRateType"
          },
          "roleId" : {
            "$ref" : "#/components/schemas/PatchableObject"
          }
        },
        "type" : "object"
      },
      "UpdateOwnProfileRequest" : {
        "properties" : {
          "addressLine1" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "addressLine2" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "city" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "companyPhone" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "currentPassword" : {
            "type" : "string"
          },
          "email" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "firstName" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "lastName" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "phone" : {
            "$ref" : "#/components/schemas/PatchableString"
          }
        },
        "type" : "object"
      },
      "UpdatePackageRequest" : {
        "properties" : {
          "code" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "currencyId" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "description" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "name" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "scope" : {
            "$ref" : "#/components/schemas/PatchableProductScope"
          }
        },
        "type" : "object"
      },
      "UpdateProductCommentRequest" : {
        "properties" : {
          "comment" : {
            "$ref" : "#/components/schemas/PatchableString",
            "description" : "Texto nuevo, de 1 a 1000 caracteres tras recortar. No admite nulo."
          },
          "rating" : {
            "$ref" : "#/components/schemas/PatchableInteger",
            "description" : "Puntuación nueva, entera de 1 a 5. No admite nulo."
          }
        },
        "type" : "object"
      },
      "UpdateProductRequest" : {
        "properties" : {
          "code" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "currencyId" : {
            "$ref" : "#/components/schemas/PatchableUUID"
          },
          "description" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "icon" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "implementation" : {
            "$ref" : "#/components/schemas/PatchableProductImplementation"
          },
          "name" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "price" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "purchasePrice" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "scope" : {
            "$ref" : "#/components/schemas/PatchableProductScope"
          },
          "sourceMembershipId" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "targetMembershipId" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "type" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "validityDays" : {
            "$ref" : "#/components/schemas/PatchableInteger"
          },
          "videoUrl" : {
            "$ref" : "#/components/schemas/PatchableString"
          }
        },
        "type" : "object"
      },
      "UpdateRoleRequest" : {
        "properties" : {
          "description" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "name" : {
            "$ref" : "#/components/schemas/PatchableString"
          }
        },
        "type" : "object"
      },
      "UpdateUserCommissionRateRequest" : {
        "properties" : {
          "fixedAmount" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "percentage" : {
            "$ref" : "#/components/schemas/PatchableBigDecimal"
          },
          "rateType" : {
            "$ref" : "#/components/schemas/PatchableCommissionRateType"
          },
          "userId" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "validFrom" : {
            "$ref" : "#/components/schemas/PatchableObject"
          },
          "validTo" : {
            "$ref" : "#/components/schemas/PatchableLocalDate"
          }
        },
        "type" : "object"
      },
      "UpdateUserRequest" : {
        "properties" : {
          "addressLine1" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "addressLine2" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "city" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "companyPhone" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "countryId" : {
            "$ref" : "#/components/schemas/PatchableUUID"
          },
          "documentNumber" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "documentTypeId" : {
            "$ref" : "#/components/schemas/PatchableUUID"
          },
          "email" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "firstName" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "lastName" : {
            "$ref" : "#/components/schemas/PatchableString"
          },
          "phone" : {
            "$ref" : "#/components/schemas/PatchableString"
          }
        },
        "type" : "object"
      },
      "UserCommissionRateItem" : {
        "properties" : {
          "deletedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "fixedAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "percentage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/CommissionRateProduct"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "user" : {
            "$ref" : "#/components/schemas/UserRef"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserCommissionRatePageResponse" : {
        "properties" : {
          "content" : {
            "items" : {
              "$ref" : "#/components/schemas/UserCommissionRateItem"
            },
            "type" : "array"
          },
          "page" : {
            "format" : "int32",
            "type" : "integer"
          },
          "size" : {
            "format" : "int32",
            "type" : "integer"
          },
          "sort" : {
            "type" : "string"
          },
          "totalElements" : {
            "format" : "int64",
            "type" : "integer"
          },
          "totalIsExact" : {
            "type" : "boolean"
          },
          "totalPages" : {
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "UserCommissionRateResponse" : {
        "properties" : {
          "fixedAmount" : {
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "percentage" : {
            "type" : "number"
          },
          "product" : {
            "$ref" : "#/components/schemas/CommissionRateProduct"
          },
          "rateType" : {
            "enum" : [ "PORCENTAJE", "FIJO" ],
            "type" : "string"
          },
          "user" : {
            "$ref" : "#/components/schemas/UserRef"
          },
          "validFrom" : {
            "format" : "date",
            "type" : "string"
          },
          "validTo" : {
            "format" : "date",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserDetailResponse" : {
        "properties" : {
          "contact" : {
            "$ref" : "#/components/schemas/ContactRef"
          },
          "country" : {
            "$ref" : "#/components/schemas/CountryRef"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "document" : {
            "$ref" : "#/components/schemas/DocumentRef"
          },
          "effectivePermissions" : {
            "items" : {
              "type" : "string"
            },
            "type" : "array"
          },
          "email" : {
            "type" : "string"
          },
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastLoginAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "lockedUntil" : {
            "format" : "date-time",
            "type" : "string"
          },
          "membership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "roles" : {
            "items" : {
              "$ref" : "#/components/schemas/RoleRef"
            },
            "type" : "array"
          },
          "status" : {
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserMembershipResponse" : {
        "properties" : {
          "code" : {
            "type" : "string"
          },
          "endsAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "level" : {
            "format" : "int32",
            "type" : "integer"
          },
          "name" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserRef" : {
        "properties" : {
          "fullName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserResponse" : {
        "properties" : {
          "contact" : {
            "$ref" : "#/components/schemas/ContactRef"
          },
          "country" : {
            "$ref" : "#/components/schemas/CountryRef"
          },
          "createdAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "document" : {
            "$ref" : "#/components/schemas/DocumentRef"
          },
          "email" : {
            "type" : "string"
          },
          "firstName" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lastName" : {
            "type" : "string"
          },
          "membership" : {
            "$ref" : "#/components/schemas/MembershipRef"
          },
          "mustChangePassword" : {
            "type" : "boolean"
          },
          "roles" : {
            "items" : {
              "$ref" : "#/components/schemas/RoleRef"
            },
            "type" : "array"
          },
          "status" : {
            "type" : "string"
          },
          "supervisor" : {
            "$ref" : "#/components/schemas/SupervisorRef"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "UserStatusResponse" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lockedUntil" : {
            "format" : "date-time",
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "updatedAt" : {
            "format" : "date-time",
            "type" : "string"
          },
          "username" : {
            "type" : "string"
          }
        },
        "type" : "object"
      }
    },
    "securitySchemes" : {
      "bearerAuth" : {
        "bearerFormat" : "JWT",
        "description" : "Token de acceso emitido por `POST /api/v1/auth/login`. Pegue solo el valor de `accessToken`: el prefijo `Bearer` lo añade la interfaz.",
        "in" : "header",
        "scheme" : "bearer",
        "type" : "http"
      }
    }
  },
  "info" : {
    "description" : "API del módulo `SP`. **Toda operación exige un token de acceso**\nsalvo el inicio, la renovación y el cierre de sesión.\n\nPara probar cualquier endpoint: obtenga el token con\n`POST /api/v1/auth/login`, pulse **Authorize** y pegue el valor de\n`accessToken` — sin el prefijo `Bearer`, que Swagger añade solo.\n\nEl token vive **quince minutos** y no es revocable: cuando expire,\nrenuévelo con `POST /api/v1/auth/refresh` y vuelva a autorizar.\n",
    "title" : "NEXUS — API del Sistema Principal",
    "version" : "v1"
  },
  "openapi" : "3.1.0",
  "paths" : {
    "/api/v1/audit/changes" : {
      "get" : {
        "description" : "Altas y ediciones de todo el sistema, **del más reciente al más\nantiguo**. El orden no se puede cambiar: es parte del significado de un\nregistro cronológico.\n\n**`changes` se devuelve tal como se escribió**, sin interpretar: en un\n`CREATE` es el estado inicial del registro y en un `UPDATE` solo los\ncampos que cambiaron, cada uno con su `before` y su `after`.\n\n**`correlationId` enlaza con la petición** que produjo el evento, y con\nlo que esa misma petición dejó en los otros tres registros. Cuando son\nnulos, `correlationId` e `ipAddress` lo son **a la vez**: significa que\nel cambio no vino de la red —una migración, una tarea programada—, y\nnunca que se olvidara registrarlo. `actorId` nulo se lee igual: lo hizo\nel sistema.\n\n**Ningún filtro es obligatorio**, ni siquiera el rango de fechas: la\nconsulta que más valor tiene es la línea de tiempo completa de un\nregistro. `from` y `to` son **instantes** con zona horaria, no fechas\nsueltas, y el rango es **semiabierto** —incluye `from`, excluye `to`—\npara que dos rangos consecutivos no devuelvan dos veces el evento de la\nfrontera.\n\n**`entityId` y `actorId` no se validan contra nada**: la auditoría\nconserva eventos de registros que ya no existen, que es su razón de ser.\n",
        "operationId" : "cambios",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "module",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "entity",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "entityId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "actorId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "action",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "from",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "to",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "correlationId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de eventos de cambio."
          },
          "400" : {
            "description" : "Rango de fechas incoherente (`VAL-001`), paginación fuera de límites (`VAL-002`) o filtro fuera de su dominio (`VAL-003`). Se devuelven **juntos**"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseChangeAuditItem"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseChangeAuditItem"
                }
              }
            },
            "description" : "Autenticado sin `audit:read-changes` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseChangeAuditItem"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la auditoría de cambios",
        "tags" : [ "Auditoría" ]
      }
    },
    "/api/v1/audit/deletions" : {
      "get" : {
        "description" : "Qué se eliminó, quién lo eliminó y **por qué**, del más reciente al más\nantiguo.\n\n**`snapshot` es lo que hace útil a este registro**: el estado completo\ndel registro en el momento de borrarse. Sin él, la fila diría que un\nidentificador fue eliminado y nadie recordaría qué era.\n\n**`reason` va vacío en las eliminaciones de asociación** —retirar un\npermiso de un rol, por ejemplo—, y es correcto: lo que desaparece es una\nasociación y no una entidad de negocio, de modo que no exige motivo.\n\nEl filtro `reason` **busca por texto** dentro del motivo, sin distinguir\nacentos ni mayúsculas: es prosa que escribió una persona y nadie\nrecuerda cómo la redactó. Por lo mismo, buscar por motivo deja fuera las\neliminaciones de asociación, que no lo llevan.\n",
        "operationId" : "eliminaciones",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "module",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "entity",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "entityId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "actorId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "deletionType",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "reason",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "from",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "to",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "correlationId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de eventos de eliminación."
          },
          "400" : {
            "description" : "Rango incoherente (`VAL-001`), paginación fuera de límites (`VAL-002`) o tipo de eliminación fuera de su dominio (`VAL-003`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseDeletionAuditItem"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseDeletionAuditItem"
                }
              }
            },
            "description" : "Autenticado sin `audit:read-deletions` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseDeletionAuditItem"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la auditoría de eliminación",
        "tags" : [ "Auditoría" ]
      }
    },
    "/api/v1/audit/errors" : {
      "get" : {
        "description" : "Fallos no controlados y rechazos por regla de negocio, del más reciente\nal más antiguo.\n\n**El filtro para el que existe esta consulta es `correlationId`**:\nalguien reporta un error citando el identificador que la respuesta le\ndevolvió, y con él se llega al fallo concreto y a la traza técnica que\nlo acompaña.\n\n**Qué NO aparece aquí, y es la frontera que más importa:** la denegación\nde autorización (`403`) vive en la auditoría de **seguridad**, porque no\nes un fallo del sistema sino el sistema funcionando. Tampoco la\nvalidación de formato, el `401` ni el `404` — esos los cubre el registro\nde peticiones.\n\n`message` viene **ya saneado** desde que se escribió: sin trazas, sin\nSQL, sin rutas y sin versiones.\n",
        "operationId" : "errores",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "errorType",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "severity",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "errorCode",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "resource",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "actorId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "from",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "to",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "correlationId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de fallos registrados."
          },
          "400" : {
            "description" : "Rango incoherente (`VAL-001`), paginación fuera de límites (`VAL-002`) o tipo o severidad fuera de su dominio (`VAL-003`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseErrorAuditItem"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseErrorAuditItem"
                }
              }
            },
            "description" : "Autenticado sin `audit:read-errors` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseErrorAuditItem"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la auditoría de error",
        "tags" : [ "Auditoría" ]
      }
    },
    "/api/v1/audit/security" : {
      "get" : {
        "description" : "Entradas, bloqueos, cambios de privilegio y denegaciones de\nautorización, del más reciente al más antiguo.\n\n**Esta consulta se registra a sí misma.** Cada llamada deja un evento\nde seguridad con quién la hizo, cuándo y **con qué filtros**: en este\nregistro, el acto de mirar es en sí mismo información de seguridad — y\nquién revisó los accesos de una cuenta ajena es exactamente la clase de\nhecho que conviene conservar. Es la diferencia con los otros tres, que\nse conforman con el registro de peticiones.\n\n**`targetUserId` es el filtro de la investigación de una cuenta**: qué\nle pasó a esta persona en estas fechas. Los **intentos de acceso\nfallidos no llevan usuario afectado** y por tanto no aparecen ahí: la\npresencia de ese campo delataría que la cuenta existe. Se localizan por\ntipo de evento y rango, leyendo el identificador intentado en `detail`.\n\n**`detail` no contiene credenciales en ninguna forma** —ni contraseñas,\nni resúmenes, ni tokens—, y eso se garantiza al escribir.\n",
        "operationId" : "seguridad",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "eventType",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "severity",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "outcome",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "actorId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "targetUserId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "ipAddress",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "from",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "to",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de eventos de seguridad."
          },
          "400" : {
            "description" : "Rango incoherente (`VAL-001`), paginación fuera de límites (`VAL-002`) o tipo de evento, severidad o resultado fuera de su dominio (`VAL-003`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseSecurityAuditItem"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseSecurityAuditItem"
                }
              }
            },
            "description" : "Autenticado sin `audit:read-security` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseSecurityAuditItem"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la auditoría de seguridad",
        "tags" : [ "Auditoría" ]
      }
    },
    "/api/v1/auth/login" : {
      "post" : {
        "description" : "Autentica con **nombre de usuario o correo** —el mismo campo para los\ndos— y entrega las credenciales de sesión.\n\nEl rechazo por credenciales lleva `remainingAttempts`: **cuántos\nintentos quedan** antes del bloqueo. El identificador que no\ncorresponde a ninguna cuenta los gasta igual, de modo que el número no\npermite averiguar qué cuentas existen.\n\nLa cuenta bloqueada recibe `423` y un mensaje que la identifica como\ntal: es una excepción consciente al mensaje genérico, porque quien\nprovocó el bloqueo ya sabe que la cuenta existe. Si el bloqueo es\n**automático**, la respuesta añade `unlockAt` —el instante en que se\nlevanta— y `retryAfterSeconds` —lo que falta—. El bloqueo **manual**\nno los lleva: esa cuenta no se desbloquea sola.\n\n**La espera no va escrita en el mensaje, y es deliberado.** Un texto\ncon «vuelva en dos minutos» es cierto al serializarse y deja de serlo\nenseguida. Con `retryAfterSeconds` el cliente **descuenta** —una\ncuenta regresiva no envejece— sin depender de que su reloj coincida\ncon el del servidor, que es lo que sí ocurriría calculándola a partir\nde `unlockAt`.\n\nSi la contraseña la fijó otra persona, la respuesta autentica **y\nadvierte** con `mustChangePassword`: hace falta una sesión para poder\ncambiarla.\n",
        "operationId" : "login",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/LoginRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SessionResponse"
                }
              }
            },
            "description" : "Credenciales de sesión."
          },
          "400" : {
            "description" : "Identificador o contraseña ausentes"
          },
          "401" : {
            "description" : "Credenciales inválidas, cuenta inexistente, inactiva o eliminada — los cuatro casos comparten cuerpo y mensaje, sin una sola diferencia observable. Lleva `remainingAttempts`; el intento que agota el contador añade además `unlockAt` y `retryAfterSeconds`"
          },
          "423" : {
            "description" : "Cuenta bloqueada. El bloqueo automático lleva `unlockAt` y `retryAfterSeconds`; el manual no los lleva, porque no expira solo"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Iniciar sesión",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/logout" : {
      "post" : {
        "description" : "Revoca el refresh token de la sesión, o **todos** los de la persona si\nse pide.\n\nResponde `204` para **cualquier** token sintácticamente válido, exista o\nno, esté vigente o revocado. Distinguirlos permitiría comprobar con dos\npeticiones si una cadena de texto es un refresh token del sistema, y el\nresultado prometido —que ese token no sirva— se cumple igual.\n",
        "operationId" : "logout",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/LogoutRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Sesión cerrada."
          },
          "400" : {
            "description" : "Refresh token ausente o con formato imposible"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Cerrar sesión",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/password" : {
      "post" : {
        "description" : "Sustituye la contraseña de **quien porta el token**.\n\n**No lleva identificador de usuario**, y esa ausencia es la\nimplementación de la regla: no hay campo por el que dirigir la\noperación a un tercero. Restablecer la contraseña de otra persona es\notra operación, con su propio permiso.\n\nLa contraseña actual se comprueba **la última**, después del formato, de\nque la nueva sea distinta y de la política. Es el único paso que consume\nel contador de intentos: ponerlo antes haría que cinco peticiones\ndescuidadas de un cliente propio bloquearan la cuenta de su titular.\n\nLa contraseña actual incorrecta devuelve `422` y **no `401`**: un `401`\nle diría al cliente que su sesión ya no vale y lo mandaría a iniciar\nsesión, cuando lo único que ocurrió es que escribió mal su contraseña.\n\n**Aquí sí se dice qué falló**, al revés que al iniciar sesión: quien\nhace esta petición ya está autenticado y no se le revela nada que no\nsupiera.\n\nAl cambiarla, **todas las sesiones se revocan**, incluida la que ejecutó\nel cambio.\n",
        "operationId" : "cambiarContrasena",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangePasswordRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Contraseña sustituida."
          },
          "400" : {
            "description" : "Falta alguna de las dos (`VAL-001`, `VAL-002`), la nueva no cumple la política (`VAL-004`) o es igual a la actual (`VAL-005`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "422" : {
            "description" : "La contraseña actual no es correcta (`VAL-003`). Consume un intento"
          },
          "423" : {
            "description" : "La cuenta quedó bloqueada al alcanzar el umbral de fallos"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ {
          "bearerAuth" : [ ]
        } ],
        "summary" : "Cambiar la propia contraseña",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/password-recovery" : {
      "post" : {
        "description" : "Emite un permiso temporal de un solo uso y lo envía al correo de la\ncuenta. Público: quien olvidó su contraseña no puede autenticarse.\n\n**La respuesta es idéntica exista o no la identidad**, en el cuerpo y\nen el estado. No hay forma de usar este endpoint para averiguar qué\ncuentas existen, que es exactamente para lo que se usaría si\ndistinguiera.\n\nY lo es **también en el tiempo**: el envío ocurre después de responder\ny por otro camino. Igualar solo el mensaje dejaría la defensa\ndeclarada y no real — emitir y enviar cuesta cientos de milisegundos\nque se miden desde fuera con un cronómetro.\n\n**Tampoco rechaza nada más.** Ni la cuenta bloqueada, ni la inactiva:\ncualquiera de esos rechazos diría algo. El `202` es además el estado\nhonesto — el sistema no puede afirmar que algo se entregó.\n\n**Emitir uno invalida el anterior**, de modo que nunca hay dos puertas\nabiertas a la vez sobre la misma cuenta.\n",
        "operationId" : "solicitarRecuperacion",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/PasswordRecoveryRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "202" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PasswordRecoveryResponse"
                }
              }
            },
            "description" : "Solicitud aceptada. **La misma respuesta exista o no la identidad**"
          },
          "400" : {
            "description" : "Falta el identificador (`VAL-001`)"
          },
          "429" : {
            "description" : "Demasiadas solicitudes, por identidad o por origen (`ERR-429`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Solicitar la recuperación de la propia contraseña",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/password-recovery/confirmation" : {
      "post" : {
        "description" : "Consume el permiso y fija la contraseña nueva. Público: lo que\nautoriza es el permiso, no un token.\n\n**La política se comprueba antes de tocar el permiso.** Una contraseña\nque no la cumple es un error de la persona legítima, y consumir su\npermiso por ello la obligaría a pedir otro —y a esperar otro correo—\npor haber escrito una contraseña corta.\n\n**El `422` no distingue sus cuatro causas** —permiso inexistente,\ncaducado, ya usado o sustituido—: hacerlo le diría a quien prueba\npermisos al azar cuál estuvo a punto de acertar.\n\nLa contraseña que se fija aquí **no es provisional**: la eligió su\ntitular y nadie más la conoce, de modo que la cuenta no queda marcada\npara cambio obligatorio. Es la diferencia deliberada con el\nrestablecimiento por un administrador.\n\n**No levanta el bloqueo ni cambia el estado de la cuenta.** Recuperar\nla contraseña prueba que se tiene el correo, no que alguien decidiera\ndevolver el acceso.\n\nAl completarse, **todas las sesiones se revocan** y los tokens de\nacceso ya emitidos dejan de admitirse.\n",
        "operationId" : "confirmarRecuperacion",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/PasswordRecoveryConfirmation"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Contraseña sustituida."
          },
          "400" : {
            "description" : "Falta el permiso (`VAL-002`) o la contraseña (`VAL-003`), o esta no cumple la política (`VAL-004`). **No consume el permiso**"
          },
          "422" : {
            "description" : "El permiso no es válido (`VAL-005`): inexistente, caducado, ya usado o sustituido. **Los cuatro casos comparten respuesta**"
          },
          "429" : {
            "description" : "Demasiadas confirmaciones desde el mismo origen (`ERR-429`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Confirmar la recuperación con el permiso recibido",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/refresh" : {
      "post" : {
        "description" : "Rota el refresh token y entrega credenciales nuevas.\n\n**La rotación es la defensa:** el token entregado deja de servir, de\nmodo que presentarlo de nuevo significa que existe una copia. En ese\ncaso se revoca la **familia entera** de la sesión y se emite la alarma.\n\nLas cinco condiciones de rechazo devuelven la **misma** respuesta: el\ncliente no debe poder deducir si el token fue robado, si expiró o si la\ncuenta fue desactivada.\n",
        "operationId" : "refresh",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RefreshRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SessionResponse"
                }
              }
            },
            "description" : "Credenciales renovadas."
          },
          "400" : {
            "description" : "Refresh token ausente"
          },
          "401" : {
            "description" : "La sesión no es válida — cinco causas posibles, una sola respuesta"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Renovar la sesión",
        "tags" : [ "Sesión" ]
      }
    },
    "/api/v1/auth/registration" : {
      "post" : {
        "description" : "Crea una cuenta de cliente **sin autenticación**, a partir del enlace\nque un vendedor reparte. Es el **primer endpoint público del sistema\nque escribe**.\n\nEl enlace lleva dos datos y **ninguno es un secreto**: el producto y el\nvendedor. **Los dos viajan dentro de `movement`** (`productId` y\n`sellerUsername`) y no en el primer nivel: hasta el 09-09-2026 estaban\nademás duplicados como `product` y `referrer`, y dos campos para un\ndato son dos valores que pueden discrepar.\n\n`countryCode` va en alfa-3 y `documentType` por abreviación, porque es\nun formulario público al que no se le puede pedir que conozca los\nidentificadores del sistema. Los que sí son UUID son los de lo que el\nformulario **acaba de leer de un catálogo**: el broker de cada cuenta y\nel producto.\n\n**`companyPhone` NO se admite aquí**, y enviarlo devuelve `400` por\npropiedad desconocida. El resto del sistema lo declara desde el\n10-09-2026, pero quien se registra por un enlace es un cliente y\npreguntarle por el teléfono de una empresa que no tiene añadiría un\ncampo que nadie rellenaría. Puede ponerlo después desde su propio perfil\n(`PATCH /api/v1/users/me`). **Ignorarlo en silencio sería peor que\nrechazarlo**: haría creer a quien lo manda que quedó guardado.\n\n**El registro anota una venta** (`RN-SP-043`), siempre — también en el\nenlace gratuito. Nace `PENDIENTE`, de modo que registrarse **no paga\nnada**, y su código se devuelve en `sale`.\n\n**`brokerAccounts` es una LISTA: se declaran UNA O MÁS cuentas** en el\nmismo registro, porque una persona puede operar con varios brokers y\nesta es hoy la única vía para declararlos.\n\n**Es obligatoria —al menos una— cuando el producto del enlace es una\nmembresía `BECA → BECA`** (`RN-SP-042`), y solo entonces. El motivo es\nel estado en el que nace la cuenta: sin cuenta de broker declarada,\nnadie podrá atribuirle el depósito que la saca de `FTD_PENDIENTE`. Con\notro producto **se admiten igualmente** las que se envíen.\n\n**El nombre de usuario en el broker no se pide**: lo rellena después el\nwebhook del propio broker.\n\n**Dos cuentas iguales en la misma petición se rechazan** con `VAL-014`,\ny no con el `409` del índice: ese mensaje diría «ya está declarada por\notra persona», y la otra persona sería ella misma.\n\n**El estado en que nace la cuenta depende del producto**\n(`RN-SP-044`). Con un enlace `BECA → BECA` nace en `FTD_PENDIENTE`, que\n**autentica y no opera**: quien se registra puede iniciar sesión y ver\nlo suyo, y lo que le falta para operar es el primer depósito. Con un\nproducto **de pago** nace **`ACTIVO`** —no hay ningún depósito que\nesperar— y recibe la membresía **del suelo**, no la comprada: esa la\nconcede **confirmar la venta**, y no registrarse.\n\n**La contraseña la elige la persona**, y por eso la cuenta **no** queda\nmarcada para cambio obligatorio — se marca lo que fijó otro, no lo que\nuno fijó.\n\n**No devuelve credenciales de sesión**: registrarse no es iniciar\nsesión. Quien acaba de crear su cuenta pasa por el inicio de sesión\ncomo todo el mundo.\n\n**Los rechazos no dicen qué pasó, salvo dos.** Producto inexistente,\ninactivo o retirado comparten respuesta, y lo mismo el vendedor que no\nexiste y el que no es fuerza comercial: distinguirlos convertiría este\nendpoint en una forma de enumerar el catálogo y la plantilla. **Sí\ndicen** cuál de las dos identidades —nombre de usuario o correo— está\nen uso, y que la cuenta de broker ya está declarada por otra persona:\nlos dos los necesita quien se registra para poder corregir.\n",
        "operationId" : "registrar_8",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/SelfRegistrationRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SelfRegistrationResponse"
                }
              }
            },
            "description" : "Cuenta creada —`FTD_PENDIENTE` con el enlace gratuito, `ACTIVO` con uno de pago— y venta anotada en estado `PENDIENTE`"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SelfRegistrationResponse"
                }
              }
            },
            "description" : "Datos inválidos (serie `VAL-nnn`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SelfRegistrationResponse"
                }
              }
            },
            "description" : "Nombre de usuario o correo en uso (`EX-005`), cuenta de broker ya declarada (`EX-009`), o la venta no procede — método de pago en una venta gratuita, o ausente en una con importe (`RN-MV-022`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SelfRegistrationResponse"
                }
              }
            },
            "description" : "El enlace no procede (`EX-001`, `EX-002`, `EX-003`, `EX-006`, `EX-007`, `EX-008`) o el movimiento no procede (`EX-010`)"
          },
          "429" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SelfRegistrationResponse"
                }
              }
            },
            "description" : "Demasiadas peticiones desde este origen"
          }
        },
        "summary" : "Registrarse desde el enlace de un vendedor",
        "tags" : [ "Registro por enlace" ]
      }
    },
    "/api/v1/broker-accounts" : {
      "get" : {
        "description" : "Devuelve **todas** las cuentas de broker del sistema, paginadas y con\nsu titular en cada fila. **Sin ningún filtro devuelve el sistema\nentero**: es lo que `broker-accounts:read` significa.\n\n**`supervisorId` devuelve LA RED ENTERA de esa persona, en\nprofundidad** (`RN-SP-047`): sus subordinados, los subordinados de\nestos, y así hasta abajo. **No es un solo nivel**, al revés que\n`GET /api/v1/users/{id}/team` y que\n`GET /api/v1/users/me/team/broker-accounts` — aquellos se autorizan\npor la estructura y por eso se acotan; este se autoriza por permiso,\nque ya alcanza a todos, de modo que la profundidad **ahorra recorrer\nel árbol y no concede nada**.\n\n**La raíz NO se incluye en su red.** Preguntar por la red de alguien\ndevuelve la de los suyos; sus propias cuentas se piden con `userId`, y\n**los dos filtros se combinan** — juntos responden «de la red de este\nvendedor, las cuentas de esta persona».\n\n**Solo la estructura vigente.** Quien dejó la red ayer no aparece, ni\nlos que colgaban de él por esa vía. Y una persona **eliminada** no\naparece **sin que su rama se corte**: quienes dependían de ella siguen\nsaliendo.\n\n**Filtros, todos opcionales y combinables con Y:**\n\n- `supervisorId` — la red, en profundidad.\n- `userId` — una persona concreta.\n- `status` — `REGISTER` o `FIRST_DEPOSIT`. **Cualquier otro valor es\n  `400`**, no una página vacía.\n- `brokerId` — un broker del catálogo.\n- `search` — fragmento de **número de cuenta**, nombre de usuario,\n  correo o nombre completo. Sin acentos y sin distinguir mayúsculas.\n- `from` / `to` — **cuándo se declaró la cuenta**. Son **instantes con\n  zona**, no fechas sueltas, y el rango es **semiabierto**: incluye\n  `from`, excluye `to`. `from` posterior a `to` es `400`.\n\n**Un identificador inexistente devuelve la página vacía sin error** —\n`supervisorId`, `userId` y `brokerId` no se validan contra su tabla—,\nal revés que `status` y que el rango: aquellos son preguntas legítimas\ncon respuesta vacía y estos son preguntas mal escritas.\n\n`totalElements` **cuenta lo filtrado**.\n\n**La respuesta lleva además un `summary` con TRES totales y sus\ndesgloses por broker**, los tres con la misma forma —un `total` y un\n`byBroker`—:\n\n- `summary.accounts` — cuántos registros cumplen el filtro. Su\n  `total` es **el mismo número que `totalElements`**, y sale de la\n  misma consulta: no pueden discrepar.\n- `summary.register` — cuántos de esos están en `REGISTER`.\n- `summary.firstDeposit` — cuántos de esos están en `FIRST_DEPOSIT`.\n\n**`accounts` es siempre `register` + `firstDeposit`**, total a total y\nbroker a broker. La redundancia es deliberada: van los dos estados\npara no obligar a restar, y va el total porque es la pregunta que más\nse hace.\n\n**El resumen respeta TODOS los filtros, incluido `status`**, y de ahí\nsale lo único que hay que saber antes de pintarlo: **con\n`?status=REGISTER`, `summary.firstDeposit.total` vale SIEMPRE cero**,\ny con `?status=FIRST_DEPOSIT` vale siempre el total. **Ese cero no\nsignifica «nadie ha depositado»: significa «no pediste ninguno».** Un\ntablero que lo enseñe sin decirlo miente; para ver el embudo completo,\nconsulte **sin** el filtro `status`.\n\n**`byBroker` trae TODOS los brokers del catálogo**, ordenado por\nnombre y **con cero donde el filtro no deja ninguna**: su longitud\n**no cambia** al filtrar, de modo que las columnas de una tabla se\narman una vez y no bailan. Un broker **desactivado sigue apareciendo**\n— apagarlo no borra lo que ya se declaró en él. Los tres desgloses\n**suman exactamente** su `total`.\n\n**Con la página vacía el resumen va en ceros**, no ausente: el\n`byBroker` sigue trayendo el catálogo entero, todo a cero.\n\n**La fila es idéntica campo por campo a la de\n`GET /api/v1/users/me/team/broker-accounts`**, a propósito: las dos\npantallas se pintan con el mismo componente. **Lo que aquella no\nlleva es el `summary`**: el resumen se pidió para el listado de\nadministración.\n\n**Hoy todas las cuentas están en `REGISTER`**: quien mueve una a\n`FIRST_DEPOSIT` es el webhook del broker, que todavía no existe.\n\n**Esta ruta no sustituye a `GET /api/v1/users/{id}/broker-accounts`**,\nque existe para **el superior comercial sin permiso**.\n",
        "operationId" : "listar_8",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "supervisorId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "userId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "brokerId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "search",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "from",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "to",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsPage"
                }
              }
            },
            "description" : "Página de cuentas, ordenada por titular, broker e identificador de cuenta, **con su resumen**. Vacía —y el resumen en ceros— si ningún registro cumple el filtro."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsPage"
                }
              }
            },
            "description" : "`status` fuera de `REGISTER`/`FIRST_DEPOSIT`, `from` posterior a `to`, identificador malformado (`VAL-001`) o paginación fuera de límites (`VAL-003`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsPage"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsPage"
                }
              }
            },
            "description" : "Autenticado sin `broker-accounts:read` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsPage"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar y filtrar todas las cuentas de broker",
        "tags" : [ "Cuentas de broker" ]
      }
    },
    "/api/v1/broker-accounts/indicators" : {
      "get" : {
        "description" : "Devuelve **el árbol de la fuerza comercial**, cada nodo con dos\nbloques de números: `own` —lo que cuelga **directamente** de esa\npersona— y `network` —esa persona **y todo lo que cuelga de ella**, a\ncualquier profundidad—.\n\n**La regla es una sola en todos los niveles**: el `network` de alguien\nes su `own` más la suma de los `network` de sus hijos. Un agente\nrecibe lo de sus clientes directos; un director, lo suyo más lo de sus\nagentes; un manager, lo suyo más lo de sus directores.\n\n**NO SUME LA COLUMNA `network` DE UNA LISTA.** El `network` de un\ndirector **ya contiene** el de sus agentes, de modo que sumar totales\nde distintos niveles cuenta dos veces. Para agregar, sume `own`; para\nleer el total de una rama, use su `network`.\n\n**Qué cuenta como uno:** `accounts` cuenta **cuentas** y `consumers`\ncuenta **personas**, y no tienen por qué coincidir — un cliente con\ndos cuentas es una persona y dos cuentas. **Solo cuentan las cuentas\nde consumidores**: la cuenta personal de un vendedor no entra en\nningún indicador.\n\n**Los consumidores no son nodos.** Aportan el número y no aparecen: el\nárbol es de la fuerza comercial.\n\n**`conversion` es `ftd / accounts`, un cociente entre 0 y 1 —no un\nporcentaje— y es NULA cuando no hay cuentas**, no cero: cero se lee\ncomo «nadie convirtió» y la verdad sería «no hay nada que convertir».\nSe recalcula en cada nodo sobre sus propios totales; **no es el\npromedio de las de sus hijos**.\n\n**`unassigned` es lo que hace que los números cuadren**: las cuentas\nde consumidores que no cuelgan de ningún vendedor no entran en ningún\nnodo. Se cumple que `totals` + `unassigned` es igual al\n`totalElements` de `GET /api/v1/broker-accounts` sin filtros. Se omite\ncuando se pide una rama con `rootId`, donde no significa nada.\n\n**`rootId` acota el árbol a esa rama e INCLUYE a la persona como\nraíz**, al revés que el `supervisorId` de `GET /api/v1/broker-accounts`:\nallí se piden las cuentas de su red —y las suyas no son de su red— y\naquí se pide el nodo que lleva sus números.\n\n**Hoy `ftd` será cero en todas partes** y el embudo se verá entero en\n`pending`: quien mueve una cuenta a `FIRST_DEPOSIT` es el webhook del\nbroker, que todavía no existe. Es el estado real, no un fallo del\nindicador.\n",
        "operationId" : "indicadoresDeLaRed",
        "parameters" : [ {
          "description" : "Acota el árbol a la rama de esa persona, ella incluida.",
          "in" : "query",
          "name" : "rootId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "El árbol, con los totales y lo no atribuido. `nodes` vacío si no hay fuerza comercial."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "`rootId` malformado (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "Autenticado sin `broker-accounts:read` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "`rootId` no designa a nadie de la fuerza comercial, o está eliminado (`VAL-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/NetworkIndicatorsResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar los indicadores de la red comercial",
        "tags" : [ "Cuentas de broker" ]
      }
    },
    "/api/v1/brokers" : {
      "get" : {
        "description" : "Devuelve los brokers con los que opera la plataforma, **ordenados por\nnombre**.\n\n**Un broker guarda de momento solo su nombre**, y eso tiene una\nconsecuencia que conviene conocer antes de integrarse: **el nombre es la\nclave de negocio**, no un texto descriptivo. No hay un `code` estable\nfrente a un cambio de nombre comercial — quien necesite referenciar un\nbroker debe hacerlo por su **identificador**.\n\n**No se administra por API** (`RN-SP-039`): no hay alta, ni edición, ni\neliminación, ni cambio de estado. Se puebla por migración, igual que los\ncatálogos de monedas y de tipos de documento, y cada alta queda en el\nhistorial del repositorio en lugar de en una fila que alguien insertó.\n\n**Puede responder la colección vacía**, y eso no es un fallo: es el\nestado de un catálogo que todavía no se ha sembrado.\n\n**No se pagina**: son unos pocos elementos y partirlos obligaría a dos\nllamadas para pintar un desplegable.\n",
        "operationId" : "listar_7",
        "parameters" : [ {
          "in" : "query",
          "name" : "includeInactive",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerCatalogResponse"
                }
              }
            },
            "description" : "Catálogo, ordenado por nombre"
          },
          "401" : {
            "description" : "Token ausente o inválido"
          },
          "403" : {
            "description" : "El actor no posee `brokers:read`"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de brokers",
        "tags" : [ "Brokers" ]
      }
    },
    "/api/v1/commission-rates" : {
      "get" : {
        "description" : "Devuelve **todas las tasas de rol que se han configurado**, cada una\n**con su producto** (`id`, `code`, `name`, su **`price`** y su\n**`currency`**: `id`, `code`, `decimalPlaces`) y su rol, paginadas. Es\nlo que el responsable del proyecto pidió leer: qué paga cada producto,\nen una sola lista —y sobre qué precio y en qué moneda, porque un\nporcentaje es una parte del precio y un importe fijo es dinero en la\nmoneda del producto—.\nSe filtra por `productId`, `roleId` y `rateType`, y con\n`includeDeleted` entran las retiradas.\n\n**Toda tasa viva rige** sobre su producto (`RN-CM-021`): ya no hay un\ncontador de asociaciones que mirar.\n\nLas tasas **personalizadas** no salen en este listado: están en\n`GET /api/v1/user-commission-rates`. **No hay filtro por fecha**, porque\nlas tasas de rol no tienen vigencia.\n",
        "operationId" : "listar_5",
        "parameters" : [ {
          "in" : "query",
          "name" : "filtros",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListCommissionRatesRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRatePageResponse"
                }
              }
            },
            "description" : "Página de tasas"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRatePageResponse"
                }
              }
            },
            "description" : "Parámetros inválidos"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRatePageResponse"
                }
              }
            },
            "description" : "Sin permiso"
          }
        },
        "summary" : "Consultar las tasas de comisión por rol",
        "tags" : [ "Comisiones" ]
      },
      "post" : {
        "description" : "Declara cuánto gana un rol de tipo **vendedor** por vender **este\nproducto**. **`productId` es obligatorio** (`RN-CM-021`, 15-09-2026): la\ntasa nace con su producto, **rige sobre él desde este instante** y no\ncambia de producto — cambiar es retirarla y registrar otra.\n\n**Registrar es poner en vigor.** No hay paso de asociación ni borrador:\nuna tasa registrada «para probar» sobre un producto activo es una\ncomisión real. Hasta el 15-09-2026 la tasa se registraba sin producto y\nno pagaba nada hasta asociarse; las tasas de aquel catálogo se\nborraron (`V94`) y hay que registrarlas de nuevo, producto a producto.\n\n**Un solo rol por producto** entre las vivas (`RN-CM-013`): la segunda\nresponde `409`. Y todo lo que antes se comprobaba al asociar se\ncomprueba aquí: producto **vivo y no retirado**, el **tope** —la suma de\nlo que el producto paga a sus roles no pasa de cien, `RN-CM-019`—, la\nregla del **gratuito** —solo importe fijo, `RN-CM-020`— y, para un\nimporte fijo, **los decimales de la moneda del producto**\n(`RN-CM-017`).\n\n**El porcentaje cero es válido** y significa «esto no comisiona», que **no\nes lo mismo** que no declarar la tasa.\n",
        "operationId" : "registrar_7",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterCommissionRateRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Tasa registrada, y rigiendo desde ya"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Datos inválidos, el rol no es vendedor, o el importe fijo no cabe en los decimales de la moneda del producto (`VAL-014`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Sin permiso"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Ya hay una tasa viva de ese rol sobre ese producto (`EX-007`), el producto pagaría más de cien (`EX-005`), o es gratuito y la tasa es de porcentaje (`EX-006`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "El rol no existe (`EX-002`), o el producto no existe o está retirado"
          }
        },
        "summary" : "Registrar la tasa de comisión de un rol sobre un producto",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/commission-rates/{id}" : {
      "patch" : {
        "description" : "Corrige el **valor** —la forma y la cifra—, que es lo único corregible.\n\n**Esta operación borra el pasado.** Las tasas de rol no tienen vigencia:\npasar un `AGENTE` de 10 a 12 **borra el 10**. No hay dos filas contando\ncada una su parte; hay una que ahora dice otra cosa.\n\nLo único que preserva lo ya pagado es que la liquidación haya copiado el\nporcentaje que aplicó — y esa liquidación **todavía no existe**.\n\n`rateType: null` se **rechaza**. **Ni el rol ni el producto se corrigen**:\nenviar `roleId` devuelve `400` con `VAL-009`, y `productId` es un campo\ndesconocido que también responde `400`. Se rechazan y no se ignoran,\nporque ignorarlos haría creer que el cambio se aplicó.\n\n**El tope se revisa contra su producto** (`RN-CM-019`): si el nuevo valor\ndejaría al producto pagando más del 100 % de sí mismo entre sus tasas de\nrol, la corrección se rechaza con `409`. **Un producto GRATUITO solo\nadmite importe fijo** (`RN-CM-020`): corregir a porcentaje una tasa de un\nproducto de precio cero se rechaza con `409` (`EX-008`). Y un importe\nfijo tiene que **caber en los decimales de la moneda del producto**\n(`VAL-013`).\n",
        "operationId" : "corregir_3",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateCommissionRateRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Tasa corregida"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Datos inválidos o campos no corregibles"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "Sin permiso"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "No existe, o está retirada"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommissionRateResponse"
                }
              }
            },
            "description" : "El nuevo valor dejaría al producto pagando más de cien (`EX-006`), o dejaría de porcentaje la tasa de un producto gratuito (`EX-008`)"
          }
        },
        "summary" : "Corregir el valor de una tasa de rol",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/commission-rates/{id}/deletion" : {
      "post" : {
        "description" : "Retira una tasa con **motivo obligatorio** (`RN-CM-005`), y desde ese\ninstante **el producto deja de pagar a ese rol**: es la única forma de\ndejar de pagar, a la vista y con instantánea.\n\nHasta el 15-09-2026 una tasa asociada no se retiraba (`RN-CM-015`);\ndesde que la tasa nace con su producto no hay asociación que la\nsostenga, y esa condición queda solo para la personalizada.\n\n**No es idempotente**: retirar dos veces devuelve `409`.\n",
        "operationId" : "retirar_3",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeleteCommissionRateRequest"
              }
            }
          }
        },
        "responses" : {
          "204" : {
            "description" : "Tasa retirada"
          },
          "400" : {
            "description" : "Motivo ausente, en blanco o demasiado largo"
          },
          "403" : {
            "description" : "Sin permiso"
          },
          "404" : {
            "description" : "La tasa no existe"
          },
          "409" : {
            "description" : "Ya estaba retirada"
          }
        },
        "summary" : "Retirar una tasa de comisión de rol",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/commissions/effective" : {
      "get" : {
        "description" : "Responde **cuánto le corresponde a una persona por vender un producto un\ndía concreto**, y **por qué**: devuelve la tasa que ganó y de cuál de las\ndos piezas salió.\n\n**La precedencia son dos niveles, y los dos exigen ASOCIACIÓN:** si la\npersona tiene una tasa **personalizada** vigente ese día **asociada a ese\nproducto**, es esa. Si no, la que su **rol vendedor** tenga **asociada a\nese producto**. Si ninguna, no hay tarifa.\n\n**Hasta el 11-09-2026 el primer nivel no miraba el producto**: una\nexcepción tapaba el catálogo entero de su titular y el segundo nivel no\nllegaba a consultarse nunca. Desde esa fecha las dos clases de tasa se\nasocian igual, y `RN-CM-012` —ninguna tasa rige hasta que se asocia—\ndeja de tener excepción.\n\n**Tres desenlaces, y ninguno es un error:**\n\n- `RESUELTA`: hay tasa. `source` dice si fue `PERSONALIZADA` o `ROL`. El\n  porcentaje puede ser **cero**, y eso significa «no comisiona» — es una\n  decisión declarada.\n- `SIN_TARIFA`: la persona vende y **no hay tasa aplicable**. La causa más\n  probable **no es que nadie la declarara**, sino que **nadie la asoció** a\n  ese producto: la tasa puede existir en el catálogo y no regir. El\n  porcentaje llega **nulo y presente**, nunca cero.\n- `NO_COMISIONA`: la persona **no porta rol vendedor** y **tampoco tiene\n  tasa personalizada**.\n\n**`roleId` puede llegar nulo con `outcome` igual a `RESUELTA`**, y no es\nincoherente: significa que ganó la tasa personalizada de alguien que ya no\nporta rol vendedor. Las personalizadas dejaron de llevar rol el\n01-09-2026, y **siguen rigiendo aunque su titular deje de vender**.\n\n**Nulo y cero no son lo mismo**, y quien consuma esto va a pagar con esa\ncifra.\n\nSin `onDate`, se resuelve con la fecha de hoy. Un producto **retirado** se\nresuelve con normalidad: preguntar qué se pagaba por algo que ya no se\nvende es legítimo.\n",
        "operationId" : "efectiva",
        "parameters" : [ {
          "in" : "query",
          "name" : "userId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "productId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "onDate",
          "required" : false,
          "schema" : {
            "format" : "date",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EffectiveCommissionResponse"
                }
              }
            },
            "description" : "Cualquiera de los tres desenlaces"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EffectiveCommissionResponse"
                }
              }
            },
            "description" : "Parámetros inválidos"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EffectiveCommissionResponse"
                }
              }
            },
            "description" : "Sin permiso"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EffectiveCommissionResponse"
                }
              }
            },
            "description" : "La persona o el producto no existen"
          }
        },
        "summary" : "Consultar la comisión efectiva",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/countries" : {
      "get" : {
        "description" : "Devuelve el catálogo completo, sin paginar, **ordenado alfabéticamente\npor nombre** según la intercalación del español — no por orden de bytes.\n\nLa búsqueda actúa sobre código y nombre, ignorando mayúsculas y acentos.\nSin coincidencias devuelve una colección vacía, no un error.\n\nLos países inactivos no aparecen salvo que se pidan, y entonces se\n**añaden** a los activos. La búsqueda y el estado son independientes.\n",
        "operationId" : "consultar_1",
        "parameters" : [ {
          "in" : "query",
          "name" : "peticion",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListCountriesRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CountryCatalogResponse"
                }
              }
            },
            "description" : "Catálogo devuelto. Puede venir vacío."
          },
          "400" : {
            "description" : "El parámetro de inclusión no es booleano"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de lectura de países (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de países",
        "tags" : [ "Países" ]
      },
      "post" : {
        "description" : "Registra un país en el catálogo. Queda **activo** siempre: el estado no\nse envía y enviarlo devuelve `400`.\n\nEl código se normaliza a mayúsculas y se recorta, porque lo fija\nISO 3166-1 alfa-3 y no lo inventa nadie: `col`, ` COL` y `COL` son el\nmismo país. Son tres letras y no dos.\n\nEl nombre admite acentos y caracteres no latinos sin transformación\nalguna, pero **no puede coincidir** con otro ya registrado ignorando\nmayúsculas y acentos: el catálogo no se edita, y el duplicado sería\npermanente.\n",
        "operationId" : "registrar_6",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterCountryRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CountryResponse"
                }
              }
            },
            "description" : "País registrado, con su código ya normalizado."
          },
          "400" : {
            "description" : "Código o nombre inválidos, o campo no admitido en el cuerpo"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de creación de países (`AUTH-002`)"
          },
          "409" : {
            "description" : "Código o nombre ya presentes en el catálogo (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Registrar un país",
        "tags" : [ "Países" ]
      }
    },
    "/api/v1/countries/{id}/status" : {
      "patch" : {
        "description" : "Cambia el estado de un país. Es lo **único** que la API puede modificar\nde un país.\n\nSe envía el estado destino, no una acción: pedir dos veces lo mismo deja\nel mismo estado y **no** registra un segundo evento.\n\n**Desactivar no es corregir**: el código y el nombre permanecen, y los\ndatos que ya los referencian siguen resolviéndolos. Evita que el error se\npropague desde ese momento, no lo repara.\n\nEl cuerpo no admite ningún otro campo ni motivo alguno.\n",
        "operationId" : "cambiarEstado_5",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeCountryStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CountryResponse"
                }
              }
            },
            "description" : "Estado aplicado. Devuelve el país tal como quedó."
          },
          "400" : {
            "description" : "Identificador o cuerpo inválidos, o campo no admitido (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de modificación de países (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe país con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Activar o desactivar un país",
        "tags" : [ "Países" ]
      }
    },
    "/api/v1/currencies" : {
      "get" : {
        "description" : "Devuelve el catálogo completo, sin paginar, ordenado por código.\n\n`decimalPlaces` es el campo del que depende el redondeo de todo cálculo\nfinanciero: cero es un valor legítimo y el campo nunca es nulo.\n\nLas monedas inactivas no aparecen salvo que se pidan con\n`includeInactive=true`, que **añade** a las activas en lugar de\nsustituirlas.\n",
        "operationId" : "consultar_3",
        "parameters" : [ {
          "in" : "query",
          "name" : "peticion",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListCurrenciesRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CurrencyCatalogResponse"
                }
              }
            },
            "description" : "Catálogo devuelto. Siempre es una colección, aunque tenga un solo elemento."
          },
          "400" : {
            "description" : "El parámetro de inclusión no es booleano"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de lectura de monedas (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de monedas",
        "tags" : [ "Monedas" ]
      }
    },
    "/api/v1/currencies/{id}/status" : {
      "patch" : {
        "description" : "Cambia el estado de una moneda del catálogo. Es lo **único** que la API\npuede modificar de una moneda.\n\nSe envía el estado destino, no una acción: pedir dos veces lo mismo deja\nel mismo estado y **no** registra un segundo evento.\n\nLa moneda por defecto **no puede desactivarse**: los importes del sistema\nquedarían sin referencia válida. Cambiar cuál es la moneda por defecto es\nuna operación de migración, no de API.\n\nEl cuerpo no admite ningún otro campo ni motivo alguno.\n",
        "operationId" : "cambiarEstado_4",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeCurrencyStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CurrencyResponse"
                }
              }
            },
            "description" : "Estado aplicado. Devuelve la moneda completa, con su definición sin cambios."
          },
          "400" : {
            "description" : "Identificador o cuerpo inválidos, o campo no admitido (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de modificación de monedas (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe moneda con ese identificador (`EX-002`)"
          },
          "409" : {
            "description" : "Se intenta desactivar la moneda por defecto (`RN-SP-010`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Activar o desactivar una moneda",
        "tags" : [ "Monedas" ]
      }
    },
    "/api/v1/document-types" : {
      "get" : {
        "description" : "Devuelve los documentos de identidad con los que una persona puede\nregistrarse, con su **nombre** y su **abreviación**.\n\n**El catálogo contiene solo documentos de persona mayor de edad**, y esa\nes la validación entera: no hay ninguna marca que distinga cuáles sí y\ncuáles no, porque los que no están **no están**. Registrar a un menor no\nse rechaza — no se puede expresar.\n\n**No se administra por API** (`RN-SP-036`): no hay alta, ni edición, ni\neliminación, ni cambio de estado. Se puebla por migración, y lo que deja\nde admitirse se retira también por migración. Es la asimetría deliberada\ncon los catálogos de países y monedas, que sí tienen operación para\ncambiar su estado.\n\n**No se pagina**: son unos pocos elementos, y partirlos obligaría a dos\nllamadas para pintar un desplegable.\n",
        "operationId" : "listar_6",
        "parameters" : [ {
          "in" : "query",
          "name" : "includeInactive",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/DocumentTypeCatalogResponse"
                }
              }
            },
            "description" : "Catálogo completo, ordenado por nombre"
          },
          "401" : {
            "description" : "Token ausente o inválido"
          },
          "403" : {
            "description" : "El actor no posee `document-types:read`"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de tipos de documento",
        "tags" : [ "Tipos de documento" ]
      }
    },
    "/api/v1/exchange-rates" : {
      "post" : {
        "description" : "Declara **a cuánto se cambia una moneda por otra y desde cuándo**.\n\n`price` es **cuántas unidades de la moneda de destino da UNA de la de\norigen**: `USD → COP` a `4150` significa que un dólar da cuatro mil\nciento cincuenta pesos. **La dirección contraria no se deduce** — la\ninversa aritmética casi nunca es la tasa real, y calcularla produciría\nun número plausible y falso. Quien necesite las dos declara **dos\ntasas**.\n\n**Ocho decimales**, y no los de ninguna moneda: una tasa **no es un\nimporte**, es un cociente entre dos monedas. Con cuatro, `COP → USD`\nse guardaría redondeada y una moneda más devaluada, como cero.\n\n**`validFrom` es obligatoria y `validTo` no**: sin ella la tasa es\n**vitalicia**. Ausente y `null` significan lo mismo. Una vigencia que\nempieza y termina el mismo día es legítima, y una que **empieza en el\nfuturo** también — es lo que hace innecesario un estado «programada».\n\n**`isActive` es opcional y por omisión verdadero.**\n\n**Dos tasas activas del mismo par no pueden solaparse** (`RN-SP-032`),\ny los **dos extremos de la vigencia cuentan**: una que termina el 30 de\njunio choca con otra que empieza el 30 de junio. El choque devuelve\n`409` y **no registra nada**: el sistema **no** cierra ni recorta la\ntasa anterior, porque resolver el conflicto es una decisión de quien\nadministra, no del servidor.\n",
        "operationId" : "registrar_5",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterExchangeRateRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Tasa registrada."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Datos inválidos (serie `VAL-nnn`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `exchange-rates:create` (`AUTH-002`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Se solapa con una tasa vigente del mismo par (`EX-002`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ExchangeRateResponse"
                }
              }
            },
            "description" : "Moneda inexistente o desactivada (`EX-001`)"
          }
        },
        "summary" : "Registrar una tasa de cambio",
        "tags" : [ "Tasas de cambio" ]
      }
    },
    "/api/v1/hotlinks/{username}/packages/{code}" : {
      "get" : {
        "description" : "Devuelve, **sin token**, el paquete que el enlace señala —con sus productos,\nel descuento de cada uno y **la cuenta hecha**— y el nombre y apellido de\nquien lo reparte.\n\n**Cada producto va en la misma forma que el hotlink del producto**, tal\ncual: código, nombre, descripción, icono, video, portada, membresía en tres\ncampos, precio, conversión y `rating`. A su lado, `discount` y\n`priceInPackage` — lo que vale ese producto **dentro** del paquete, con el\nprecio de catálogo de hoy. `listPrice` es la suma sin descuentos, `price` lo\nque vale el paquete y `savings` la diferencia; `exchange` convierte `price`\na la moneda de casa si hay tasa. **`priceInPackage` es la cuenta de hoy y\nno una reserva**: si un producto cambia de precio, el paquete cambia solo,\ny un fijo que hoy supera su precio cuenta cero.\n\n**Solo publica paquetes activos y de alcance `HOTLINK` o `AMBOS`**, y **el\nalcance de los productos no filtra dentro del paquete**: el canal lo decide\nel paquete. Sin `purchasePrice` ni `status` en ningún nivel.\n\n**Todo lo que no procede responde el MISMO `404`, y el mismo que el hotlink\ndel producto**: usuario inexistente, persona que no es vendedora, código\ninexistente, paquete inactivo, retirado o de alcance `TIENDA` o `NINGUNO`,\ny **también el paquete que hoy no se puede ofrecer** —un producto suyo\ninactivo o retirado, menos de dos productos, sin descripción—. **No dice cuál\nfalló**: distinguirlos publicaría, sin token, que el paquete existe y qué le\npasa. Quien tiene que saberlo es administración, y lo sabe por el detalle.\n",
        "operationId" : "hotlinkDePaquete",
        "parameters" : [ {
          "in" : "path",
          "name" : "username",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "code",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageHotlinkResponse"
                }
              }
            },
            "description" : "El enlace, con su paquete y su vendedor."
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageHotlinkResponse"
                }
              }
            },
            "description" : "El enlace no lleva a ninguna parte. **El mismo cuerpo en todos los casos**, y el mismo que el hotlink del producto (`EX-001`)"
          },
          "429" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageHotlinkResponse"
                }
              }
            },
            "description" : "Demasiadas peticiones desde ese origen"
          }
        },
        "summary" : "Consultar el hotlink de un paquete",
        "tags" : [ "Hotlinks" ]
      }
    },
    "/api/v1/hotlinks/{username}/{code}" : {
      "get" : {
        "description" : "Devuelve, **sin token**, el producto que el enlace señala y el **nombre\ny apellido** de quien lo reparte.\n\n**Es público por decisión, no por definición.** Las otras cinco rutas\npúblicas del sistema lo son porque quien las llama no puede portar\ntodavía un token; esta lo es porque un enlace se abre antes de\nregistrarse.\n\n**Solo publica productos activos y de alcance `HOTLINK` o `AMBOS`**\n(`HOTLINKS` hasta el 15-09-2026), y solo el\nnombre de quien porta un rol de tipo `VENDEDOR`. Ni correo, ni\nidentificador, ni estado, ni roles.\n\n**De la membresía se publican tres campos** —código, nombre y color—,\ny no los cinco que devuelven los otros endpoints: el identificador no\nsirve a quien no puede llamar a nada más, y el nivel publicaría la\nforma de la cadena comercial sin token.\n\n**El precio llega en la moneda del producto y, si hay tasa vigente,\nconvertido a la moneda por omisión.** `rate` viaja como **cadena**\nporque tiene ocho decimales y un número JSON pasa por coma flotante en\ncualquier cliente JavaScript.\n\n**Viene UN importe** (`RN-PM-024`, reescrita el 12-09-2026): `price`,\nel que se cobra. **El precio de compra no viaja por aquí ni se\nconsulta**: es lo que NEXUS paga por el producto, y este endpoint es\npúblico. Entre el 08-09-2026 y el 12-09-2026 viajó también\n`publicPrice`, cuando ese importe era lo que se anunciaba; ese campo\n**ya no existe**. **La conversión se calcula sobre `price`.**\n\n**Viene `videoUrl`, y sin token** (`RN-PM-032`): la dirección del video\nque presenta el producto, **tal cual la escribió administración** y\n**presente y nula** cuando no tiene. El sistema no la sigue ni la\nvalida más allá de su forma: un enlace roto se publica igual. Es la\núnica columna opcional del producto que este endpoint trae y el\nprecio de compra no — el costo enseñaría el margen; el video existe\npara que lo vean.\n\n**La conversión es INFORMATIVA**: lo que se cobra no es ese número. Una\nventa va en una sola moneda y congela su importe al registrarse; esta\nconversión se calcula al vuelo, cambia el día que cambie la tasa y **no\nreserva nada**. Cuando no hay tasa vigente —o el producto ya está en la\nmoneda de casa— `exchange` llega **presente y nulo**, y el producto se\ndevuelve igual.\n\n**Todo lo que no procede responde el MISMO `404`**: usuario\ninexistente, persona que no es vendedora, código inexistente, producto\ninactivo, retirado o de alcance `TIENDA` o `NINGUNO`. **No dice cuál falló**, y esa\nuniformidad es deliberada — distinguirlos convertiría el enlace en un\noráculo que dice quién existe.\n",
        "operationId" : "hotlink",
        "parameters" : [ {
          "in" : "path",
          "name" : "username",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "code",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HotlinkResponse"
                }
              }
            },
            "description" : "El enlace, con su producto y su vendedor."
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HotlinkResponse"
                }
              }
            },
            "description" : "El enlace no lleva a ninguna parte. **El mismo cuerpo en los seis casos** (`EX-001`)"
          },
          "429" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HotlinkResponse"
                }
              }
            },
            "description" : "Demasiadas peticiones desde ese origen"
          }
        },
        "summary" : "Consultar un hotlink",
        "tags" : [ "Hotlinks" ]
      }
    },
    "/api/v1/memberships" : {
      "get" : {
        "description" : "Devuelve la cadena completa, de la superior al extremo inferior, sin paginar.\n\n`level` es la distancia hasta la cima: `1` es la superior. El orden de la\ncolección lo refleja.\n\nNo se admite paginación ni ordenamiento: el orden **es** la información.\nLos parámetros de paginación se ignoran y devuelven la cadena entera.\n",
        "operationId" : "consultar",
        "parameters" : [ {
          "in" : "query",
          "name" : "peticion",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListMembershipsRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/MembershipChainResponse"
                }
              }
            },
            "description" : "Cadena devuelta. Puede venir vacía si aún no hay membresías."
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `memberships:read` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la cadena de membresías",
        "tags" : [ "Membresías" ]
      },
      "post" : {
        "description" : "Inserta una membresía en la cadena, por encima de la hija indicada.\n\nSi no se indica hija, la membresía queda en el extremo inferior y no se\nreordena nada. Si se indica, la nueva ocupa su nivel, hereda su superior\ny todo lo que estaba en ese nivel o por debajo baja uno.\n\nEl nivel y la superior **no se envían**: los calcula el sistema. Enviar\n`level` o `parentMembershipId` devuelve `400`.\n",
        "operationId" : "registrar_4",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterMembershipRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/MembershipResponse"
                }
              }
            },
            "description" : "Membresía registrada, con la posición que ocupó."
          },
          "400" : {
            "description" : "Formato u obligatoriedad incumplidos, o campo no admitido en el cuerpo"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `memberships:create` (`AUTH-002`)"
          },
          "409" : {
            "description" : "Código o nombre ya en uso (`EX-001`), o la cadena cambió durante la operación (`EX-003`) y debe reintentarse"
          },
          "422" : {
            "description" : "La membresía hija indicada no existe (`EX-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Registrar una membresía",
        "tags" : [ "Membresías" ]
      }
    },
    "/api/v1/memberships/{id}" : {
      "get" : {
        "description" : "Devuelve la membresía con sus dos vecinos inmediatos, expandidos.\n\nLos vecinos llegan solo hasta el primer grado y no traen los suyos: la\ncadena entera se obtiene con el listado.\n",
        "operationId" : "consultarDetalle",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/MembershipDetailResponse"
                }
              }
            },
            "description" : "Detalle devuelto."
          },
          "400" : {
            "description" : "El identificador no es un UUID (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `memberships:read` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe membresía con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de una membresía",
        "tags" : [ "Membresías" ]
      }
    },
    "/api/v1/movements" : {
      "post" : {
        "description" : "Deja constancia de **qué le vendió la empresa a un cliente**, como un hecho que\n**todavía no está pagado**.\n\n**La venta nace `PENDIENTE`, y eso significa que no concede nada.** No sube de\nnivel a nadie, no habilita ninguna cuenta y no comisiona: registrar una venta\n**no cambia absolutamente nada fuera de este módulo**. Confirmarla es otra\noperación.\n\n**El precio no se envía: se toma del catálogo.** Se indica qué productos y\ncuántos, nunca cuánto cuestan — un precio que llegara en la petición sería un\ndescuento sin autorización y sin rastro. Tampoco se envían la moneda, la\nvigencia ni el vendedor: la moneda y la vigencia salen del producto, y **el\nvendedor sale del cliente** y se congela en la venta.\n\n**Lo copiado queda congelado.** Corregir mañana el precio de un producto, o\nreasignar el cliente a otro agente, no cambia lo que se vendió hoy.\n\nReglas de composición: **como mucho un upgrade** por venta y con cantidad uno,\nsin productos repetidos y todas las líneas en la misma moneda.\n",
        "operationId" : "registrar_3",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterSaleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SaleResponse"
                }
              }
            },
            "description" : "Venta registrada, pendiente de pago."
          },
          "400" : {
            "description" : "Lo que se ve mirando la petición: falta el cliente o el método de pago, no hay líneas, la cantidad no es positiva, un producto se repite, o la fecha del hecho está en el futuro."
          },
          "403" : {
            "description" : "Sin el permiso `movements:create`."
          },
          "409" : {
            "description" : "Lo que solo se sabe después de resolver: la cuenta no puede operar todavía, el cliente no cuelga de ningún vendedor, un producto no está en su oferta, el upgrade BAJA de nivel —renovar el mismo sí se admite—, hay dos upgrades, las monedas difieren, o el método de pago está desactivado."
          },
          "422" : {
            "description" : "Un dato bien formado que no resuelve: el cliente, un producto o el método de pago no existen."
          }
        },
        "summary" : "Registrar una venta a nombre de un cliente",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v1/movements/mine" : {
      "get" : {
        "description" : "Devuelve **los movimientos en los que usted participó**, paginados y del más\nreciente al más antiguo.\n\n**«Propio» son DOS papeles.** Un movimiento lleva a quien recibe lo comprado y a\nquien lo vendió, y usted puede ser cualquiera de los dos — o **los dos a la vez**,\nsi compró para sí mismo algo que se le atribuye. Cada movimiento dice en qué papel\naparece usted con `role`: `BUYER`, `SELLER` o `BOTH`. El que es las dos cosas\n**aparece una sola vez**.\n\n**No hay forma de preguntar por otra persona**, ni indicándola ni teniendo\npermisos: quien pregunta sale de la credencial. Consultar las ventas de terceros es\notra operación, con su permiso.\n\n**Las líneas no viajan aquí.** Una venta puede llevar varias, y meterlas\nmultiplicaría la respuesta por un dato que solo se mira al abrir uno: están en el\ndetalle.\n\n**El vendedor puede ser nulo**, y viaja igual: es el caso normal de quien no cuelga\nde ningún vendedor.\n\nEl orden es fijo y no se puede cambiar. `status` filtra por estado.\n",
        "operationId" : "mios",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseMyMovement"
                }
              }
            },
            "description" : "La página de movimientos propios."
          },
          "400" : {
            "description" : "Paginación inválida (`VAL-002`) o estado no admitido (`VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar los movimientos propios",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v1/movements/mine/{id}" : {
      "get" : {
        "description" : "Devuelve **lo mismo que devuelve registrar una venta**, con sus líneas: qué\nproductos, cuántos, a qué precio y con qué vigencia. Quien registró una venta y\nquien la consulta después ven la misma forma.\n\n**Un movimiento que no es suyo responde `404`**, exactamente igual que uno que no\nexiste. No es un descuido: un `403` confirmaría que el identificador existe.\n",
        "operationId" : "mio",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SaleResponse"
                }
              }
            },
            "description" : "El movimiento, con sus líneas."
          },
          "400" : {
            "description" : "Identificador malformado (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "404" : {
            "description" : "No existe **o no es suyo** (`VAL-002`). Las dos son la misma respuesta"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de un movimiento propio",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v1/packages" : {
      "get" : {
        "description" : "La lista de administración de los paquetes, paginada, **con la cuenta hecha\npor fila**: cada paquete con `itemCount`, `listPrice`, `price`, `savings`,\n`exchange` y `offerable`, y sin sus líneas — para eso está el detalle. Los\nimportes salen de la misma cuenta que el detalle y cuadran con él.\n\nFiltra por `status`, `scope`, `currencyId` y `q` —búsqueda por nombre, sin\ndistinguir mayúsculas ni acentos—, y se combinan. **Excluye los retirados**\nsalvo `includeDeleted=true`, y entonces los trae con `deletedAt`.\n**`offerable` no es filtro**: quien administra quiere ver precisamente los\nque no se ofrecen; se queda como columna, y el motivo lo da el detalle.\n\nOrden: `createdAt` (omisión, descendente), `name` y `price`, con\n`,asc`/`,desc`. **El orden por `price` ordena por el precio calculado**: el\nprecio no está en ninguna columna, de modo que un paquete cuyo producto\ncambió de precio cambia de posición en la siguiente lectura. Se ordena con\nla suma sin redondear; la diferencia con el importe que viaja cabe en un\ncéntimo por producto y puede alterar el orden entre paquetes casi iguales.\n\nLos filtros inválidos se devuelven **juntos** con `400`. Exige\n`packages:read`; `products:read` no habilita.\n",
        "operationId" : "listar_4",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "sort",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "scope",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "currencyId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "q",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "includeDeleted",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackagePageResponse"
                }
              }
            },
            "description" : "La página de paquetes."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackagePageResponse"
                }
              }
            },
            "description" : "Filtros, orden o paginación inválidos, juntos (`VAL-001` a `VAL-005`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackagePageResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackagePageResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:read` (`AUTH-002`)"
          }
        },
        "summary" : "Consultar los paquetes",
        "tags" : [ "Paquetes" ]
      },
      "post" : {
        "description" : "Registra un paquete de productos, **vacío e inactivo**: los productos entran\nuno a uno después (`POST /packages/{id}/products`) y publicarlo es otra\noperación (`PATCH /packages/{id}/status`), que exige descripción y al menos\ndos productos.\n\n**El paquete no declara precio: se calcula.** Su `price` es la suma de sus\nproductos con su descuento, `listPrice` la suma sin descuentos y `savings` la\ndiferencia, todo con el precio de catálogo **de hoy** y redondeado por\nproducto a los decimales de la moneda. No se guarda en ninguna columna: si un\nproducto cambia de precio, el paquete cambia solo. Enviar `price` —o `products`,\no `status`— responde `400`.\n\n**La moneda es obligatoria e inmutable**, y solo se le asocian productos en\nesa moneda: un paquete recién creado no tiene productos y aun así sabe en qué\nse expresa. `scope` dice en qué vistas se publica —`TIENDA`, `HOTLINK`, `AMBOS`\no `NINGUNO`, que existe y se activa pero no se ofrece en ninguna— y es del\npaquete: el alcance de sus productos no filtra dentro de él.\n\nLa respuesta es la misma forma que el detalle: `items` vacío, los tres\nimportes en cero, `exchange` nulo, y `offerable: false` con su motivo — «menos\nde dos productos». `offerable` y `offerableReason` viajan **siempre**.\n\nExige `packages:create`. **Los `products:` no habilitan**: un actor con los\ncuatro permisos del catálogo y ninguno de paquetes recibe `403`.\n",
        "operationId" : "register_1",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterPackageRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Paquete registrado, vacío y en estado `INACTIVO`."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Datos inválidos, juntos (`VAL-001` a `VAL-004`), o un cuerpo con `price`, `products` o `status` (`VAL-005`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:create` (`AUTH-002`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Código ya en uso —también por un paquete retirado— o nombre ya en uso por un paquete vivo (`EX-001`, `EX-002`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Moneda inexistente o inactiva (`EX-003`)"
          }
        },
        "summary" : "Registrar un paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}" : {
      "get" : {
        "description" : "El paquete con sus productos y **la cuenta hecha**: cada producto con su\ndescuento y su `priceInPackage`, y `listPrice`, `price` y `savings` que\ncuadran con las líneas. **El precio se calcula en cada lectura** con el\nprecio de catálogo **de hoy** de cada producto: corregir el precio de un\nproducto cambia el paquete sin tocarlo, y un fijo que hoy supera su precio\ncuenta **cero**, nunca negativo. `exchange` es la conversión de `price` a la\nmoneda de casa; nula si el paquete ya está en ella o no hay tasa.\n\nEs la lectura de administración: **devuelve todo**. Un producto inactivo o\nretirado dentro del paquete **se devuelve igual**, con su `status` y su\n`deleted`, y **sigue sumando** — esta es la única pantalla desde la que se\narregla. Y `purchasePrice` de cada producto viaja, presente y nulo cuando\nno se conoce; la oferta y el hotlink no lo llevan.\n\n`offerable` dice si el paquete se puede ofrecer hoy, y `offerableReason` el\n**primer** motivo que lo impide, en un orden fijo: menos de dos productos,\nsin descripción, inactivo, retirado, y un producto inactivo o retirado —\n**nombrado por su código**—. Nulo cuando es ofrecible.\n\n**Un paquete retirado no es un `404`**: se devuelve con `deletedAt` y\n`deletionReason`, como el producto. Exige `packages:read`.\n",
        "operationId" : "detalle_3",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete con su cuenta hecha."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificador inválido (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:read` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe (`EX-001`)"
          }
        },
        "summary" : "Consultar el detalle de un paquete",
        "tags" : [ "Paquetes" ]
      },
      "patch" : {
        "description" : "Corrige **nombre, descripción y alcance**, por separado o juntos, y devuelve\nel detalle. Cada campo puede venir ausente —no se toca—, con valor, o\n**presente y nulo**: el nulo vacía la descripción y **se rechaza** en nombre\ny alcance, que son obligatorios.\n\n**El código y la moneda no se pueden modificar**, y enviarlos responde `400`\n—se rechazan, no se ignoran—. La moneda es la unidad en la que se suma el\npaquete: cambiarla dejaría los descuentos fijos expresados en otra cosa.\n\n**Vaciar la descripción de un paquete activo se permite**: no lo desactiva,\npero lo saca de la oferta hasta que vuelva a tenerla, y el detalle lo dice\nen `offerableReason`. Cambiar el alcance de `AMBOS` o `HOTLINK` a `TIENDA`\nhace que el hotlink del paquete deje de resolver; la oferta no cambia.\n\nUn cuerpo sin cambios de valor responde `200` sin avanzar `updatedAt` ni\nauditar. Exige `packages:update`.\n",
        "operationId" : "corregir_2",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdatePackageRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete corregido, con su cuenta hecha."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificador inválido, nombre vacío o de más de 150, alcance nulo o fuera de dominio, cuerpo vacío, o `code`/`currencyId` presentes (`VAL-001` a `VAL-005`, `EX-003`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe o está retirado (`EX-001`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El nombre ya lo usa otro paquete vivo (`EX-002`)"
          }
        },
        "summary" : "Corregir un paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}/cover" : {
      "delete" : {
        "description" : "Vacía la portada y **la imagen se borra**: su dirección deja de servir.\nEl paquete vuelve a pintarse con **el icono de promoción y el color por\nomisión** que el cliente le pone cuando `coverImageUrl` es nula.\n\n**Nunca responde `400` por el estado del paquete** — es la única\ndiferencia con `DELETE /api/v1/products/{id}/cover`: el paquete no\ndeclara icono ni color (`RN-PM-045`), de modo que no puede quedarse sin\nnada con qué pintarse. Un paquete activo y ofrecible sigue activo y\nofrecible sin portada.\n\n**Sin portada responde igual y no escribe nada**: ni auditoría ni\n`updatedAt`. Responde `200` con el paquete, y no `204`: se vacía un\ncampo, no se retira una entidad. Sin cuerpo; si llega uno, se ignora.\n",
        "operationId" : "quitarPortada_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete, con `coverImageUrl` nulo."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un paquete vivo con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Quitar la portada de un paquete",
        "tags" : [ "Paquetes" ]
      },
      "put" : {
        "description" : "**La misma imagen que la del producto, con las mismas condiciones y la\nmisma ruta** (`RN-PM-045`, `requirements/pm.md` §5.2.12): recibe **un\narchivo** —`multipart/form-data`, una sola parte llamada `file`—,\n`JPEG`, `PNG` o `WebP` **reconocidos por sus bytes** (la cabecera y el\nnombre se ignoran; `VAL-003`), **hasta 5 MB** (`VAL-004`), sin archivo\no vacío `VAL-002`, y lo guarda **tal cual**. La imagen se sirve sin token\nen `GET /api/v1/product-images/{imageId}`, que no sabe si lo que sirve es\nla portada de un producto o de un paquete.\n\n**Si el paquete ya tenía portada, la reemplaza**: la nueva estrena\nidentificador, **la anterior se borra** y **la dirección cambia**. **Sin\ncondición de estado ni de contenido**: un paquete inactivo, vacío o sin\ndescripción la admite igual, y la activación sigue sin mirarla.\n\n**El paquete no declara icono ni color.** Cuando `coverImageUrl` es\nnula, el cliente pinta **el icono de promoción y el color por omisión\ndel sistema**, los mismos para todos los paquetes — y por eso quitar la\nportada nunca se rechaza. El alta no admite la imagen: `POST\n/api/v1/packages` sigue siendo JSON. Responde con el paquete entero, con\nsu cuenta hecha, como todas sus escrituras; **un paquete retirado no\nadmite portada**.\n",
        "operationId" : "subirPortada_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "multipart/form-data" : {
              "schema" : {
                "properties" : {
                  "file" : {
                    "format" : "binary",
                    "type" : "string"
                  }
                },
                "type" : "object"
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete, con `coverImageUrl` señalando la imagen nueva."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`), sin archivo o vacío (`VAL-002`), ni JPEG ni PNG ni WebP por sus bytes (`VAL-003`), más de 5 MB (`VAL-004`) o petición que no es `multipart/form-data` (`EX-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un paquete vivo con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Subir o reemplazar la portada de un paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}/deletion" : {
      "post" : {
        "description" : "Retira el paquete con **eliminación lógica y motivo obligatorio** (Art. V.13):\nla fila permanece con su `status` y **sus filas de asociación**, y el detalle\nsigue devolviéndolo con `deletedAt` y `deletionReason`. Sale de la oferta y\ndel hotlink en el acto; **sus productos no cambian** — eran suyos antes del\npaquete y lo siguen siendo después, en el catálogo y en los demás paquetes.\n\nEl motivo se valida **antes de cualquier consulta**. El paquete inexistente\nes `404` y el **ya retirado es `409`**, y se distinguen a propósito: el\ncatálogo devuelve los retirados a quien tiene `packages:read`, y quien\nretira dos veces merece saber que la primera funcionó.\n\n**El código de un paquete retirado no se libera**: un alta con ese código\nresponde `409`. El nombre sí. Exige `packages:delete`.\n",
        "operationId" : "retirar_2",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeletePackageRequest"
              }
            }
          }
        },
        "responses" : {
          "204" : {
            "description" : "Paquete retirado."
          },
          "400" : {
            "description" : "Identificador inválido o motivo ausente, vacío o de más de 500 (`VAL-001` a `VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `packages:delete` (`AUTH-002`)"
          },
          "404" : {
            "description" : "El paquete no existe (`EX-001`)"
          },
          "409" : {
            "description" : "El paquete ya está retirado (`EX-002`)"
          }
        },
        "summary" : "Retirar un paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}/products" : {
      "post" : {
        "description" : "Mete un producto en el paquete con su descuento, y devuelve **el paquete\nentero** con el producto nuevo, su `priceInPackage` y los totales rehechos.\nEs la operación que define al paquete: siete verificaciones en fila, cada\nuna con su código.\n\n**Forma y valor van juntos y son obligatorios.** `PORCENTAJE` de `0` a\n`100` con hasta dos decimales; `FIJO` de `0` al precio del producto, con los\ndecimales de la moneda del paquete. **El cero se admite**: es un producto que\nentra al paquete a su precio.\n\n**La cota se comprueba contra el precio de hoy, y nadie la vuelve a\nmirar.** Un fijo mayor que el precio del producto responde `409` (`EX-006`,\nnombrando el precio); sobre un producto **gratuito** solo se admite cero. Si\ndespués el precio del producto baja por debajo de su fijo, el producto\ncuenta **cero** dentro del paquete — nunca negativo — y nada avisa.\n\n**Qué significa cada `409`**: `EX-003` el producto existe pero está inactivo\no retirado — se distingue del inexistente, que es `422` (`EX-002`), porque\nquien llama ve el catálogo entero—; `EX-004` el producto está en otra\nmoneda, y el mensaje nombra las dos; `EX-005` el producto ya está en el\npaquete — corrija su descuento en lugar de asociarlo de nuevo—; `EX-006` el\ndescuento deja al producto por debajo de cero; `EX-007` **el paquete ya\ntiene un upgrade** (`RN-PM-046`, desde el 16-09-2026): un paquete lleva **un\nsolo `UPGRADE_MEMBRESIA`** —del origen y el destino que sea, y aunque hoy esté\ninactivo: el sitio lo ocupa la fila— y cuantos bots se quiera. El mensaje\n**nombra el código** del upgrade que ya está, y el sitio se libera al\ndesasociarlo. Hasta el 16-09-2026 este código significaba «el upgrade no\ncomparte origen con los ya dentro»; con uno solo no hay orígenes que\ncomparar. Los bots entran sin mirar cuántos upgrades hay.\n\nSe asocia **a un paquete inactivo** —es el estado en el que un paquete se\narma— y no a uno retirado (`404`). El producto no cambia: la fila es del\npaquete, y el mismo producto puede estar en otros paquetes con otro\ndescuento. Exige `packages:update`.\n",
        "operationId" : "associate",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AssociatePackageProductRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Producto asociado; el paquete entero con su precio recalculado."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificador inválido, campos ausentes o forma del descuento inválida (`VAL-001` a `VAL-005`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe o está retirado (`EX-001`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Producto inactivo o retirado, en otra moneda, ya en el paquete, descuento por encima del precio, o el paquete ya tiene un upgrade (`EX-003` a `EX-007`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El producto no existe (`EX-002`)"
          }
        },
        "summary" : "Asociar un producto al paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}/products/{productId}" : {
      "delete" : {
        "description" : "Quita el producto del paquete **sin cuerpo y sin motivo** —la fila es una\nasociación (Art. V.13): se borra físicamente y su baja queda en la\nauditoría de eliminación como `ASSOCIATION`, con el descuento y el precio\ndel producto en la instantánea— y responde **`200` con el paquete**, no\n`204`: lo que cambió es su precio.\n\nEl paquete **no cambia de estado** aunque quede con uno o con cero\nproductos: deja de ofrecerse por «menos de dos» y el detalle lo dice. Si se\nquita el upgrade, **el sitio queda libre** (`RN-PM-046`): el siguiente\nupgrade que entre puede ser de cualquier origen, y mientras no entre ninguno\nel paquete —solo bots— se ofrece a todo el mundo. **El producto no cambia**: sigue\nactivo en el catálogo y en los demás paquetes que lo contengan.\n\nEl producto que no está —también el que ya se quitó— es `404`: con el\nborrado físico no queda nada que los distinga. Exige `packages:update`.\n",
        "operationId" : "desasociar",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "productId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete sin el producto, con su cuenta rehecha."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificadores inválidos (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe o está retirado, o el producto no está en él (`EX-001`, `EX-002`)"
          }
        },
        "summary" : "Quitar un producto del paquete",
        "tags" : [ "Paquetes" ]
      },
      "patch" : {
        "description" : "Corrige **forma y valor juntos** —son un solo dato, y los dos son\nobligatorios: no es parcial— y devuelve el paquete entero con su\n`priceInPackage` y sus totales rehechos.\n\n**La cota se comprueba contra el precio de hoy del producto**, no contra el\nque tenía al asociarlo: un fijo que entró cuando el producto valía 100 y hoy\nvale 50 no se puede corregir a 60 — sí a 50 o menos. Es donde el hueco\ntemporal de `RN-PM-037` se cierra solo. Sobre un producto gratuito solo se\nadmite cero.\n\n**No mira el estado del producto**: un producto inactivo dentro del paquete\nse corrige igual — la fila existe y el descuento es del paquete—, y el\npaquete sigue sin ofrecerse por él. `productId` va en la ruta; en el cuerpo\nes un campo desconocido y responde `400`.\n\nEl producto que no está en el paquete es `404`, sin distinguir «no existe»\nde «no está aquí»: lo que se corrige es la pareja. Sin cambio de valor\nresponde `200` sin auditar. Exige `packages:update`.\n",
        "operationId" : "corregirDescuento",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "productId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CorrectPackageDiscountRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete con el descuento corregido y su cuenta rehecha."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificadores inválidos, forma o valor ausentes, o forma inválida (`VAL-001` a `VAL-005`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe o está retirado, o el producto no está en él (`EX-001`, `EX-002`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El descuento deja al producto por debajo de cero hoy (`EX-003`)"
          }
        },
        "summary" : "Corregir el descuento de un producto del paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/packages/{id}/status" : {
      "patch" : {
        "description" : "Cambia el estado del paquete y devuelve el detalle.\n\n**Activar exige descripción y al menos dos productos**, y los dos motivos\nvan **juntos** en el mismo `409` cuando faltan a la vez. **No exige que los\nproductos estén activos hoy**: eso lo mira la oferta cada vez, y un paquete\nactivo con un producto inactivo dentro es un estado legítimo — el detalle lo\ndevuelve `offerable: false` nombrando el producto.\n\n**Desactivar no tiene condiciones**: el paquete sale de la oferta y del\nhotlink, y sus productos no cambian.\n\nPedir el estado que ya tiene responde `200` sin escribir ni auditar. Exige\n`packages:update`.\n",
        "operationId" : "cambiarEstado_3",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangePackageStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete en su estado nuevo, con su cuenta hecha."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Identificador inválido o `status` ausente o fuera de dominio (`VAL-001`, `VAL-002`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `packages:update` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "El paquete no existe o está retirado (`EX-001`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PackageDetailResponse"
                }
              }
            },
            "description" : "Activar sin descripción o con menos de dos productos (`EX-002`, `EX-003`), juntos"
          }
        },
        "summary" : "Activar o desactivar un paquete",
        "tags" : [ "Paquetes" ]
      }
    },
    "/api/v1/payment-methods" : {
      "get" : {
        "description" : "Devuelve los métodos de pago **activos y de visibilidad `PUBLICO`**, sin\npaginar y ordenados por código.\n\n**Es una consulta PÚBLICA** (`RN-MV-024`): responde **sin token**, porque\nel formulario de registro por enlace elige con qué se paga antes de que\nexista la cuenta. **Con sesión devuelve exactamente lo mismo** — no hay\nun catálogo para el anónimo y otro para quien ha entrado, y no existe\nparámetro que cambie lo que sale.\n\n**Lo que NO devuelve, y no hay forma de pedirlo**: ni los métodos\ndesactivados —un método retirado no se puede usar en una venta nueva— ni\nlos de visibilidad `INTERNO` (`RN-MV-023`). Estos últimos **sirven para\npagar y no los elige ninguna persona**: los pone el sistema. Es el caso\nde `GRATIS`, con el que se anotan las ventas de importe cero\n(`RN-MV-022`): quien depure una de esas verá un `paymentMethodId` que\n**este catálogo no resuelve**, y eso es deliberado.\n\n**La respuesta no publica el campo de visibilidad.** Si todo lo que sale\nes `PUBLICO`, declararlo sugeriría que puede salir otra cosa.\n\n**`excludedCountries` dice dónde NO vale cada medio.** Una lista vacía\nsignifica que vale en todos los países, y el campo **viaja siempre**:\nno hay que distinguir «sin exclusiones» de «no vino el campo».\n\n**Esta restricción es informativa y el servidor NO la aplica.** Registrar\nuna venta con un método excluido en algún país **se registra con\nnormalidad**: quien decide qué ofrecer es el cliente que consume esta\nrespuesta. El servidor no sabe de qué país es quien compra — ninguna\npersona del sistema tiene país todavía.\n\n**No admite parámetros**, y en particular no admite un país: filtrar aquí\nharía creer que el servidor sabe cuál corresponde.\n\n**Está acotada por origen** —120 peticiones por minuto, con cubo propio—,\ncomo los demás catálogos públicos: superar la cota responde `429`.\n",
        "operationId" : "consultar_2",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PaymentMethodCatalogResponse"
                }
              }
            },
            "description" : "El catálogo de métodos de pago activos y públicos. Se responde igual con token y sin él."
          },
          "429" : {
            "description" : "Se superó la cota de peticiones por origen (120 por minuto). La cabecera `Retry-After` dice cuánto esperar."
          }
        },
        "summary" : "Consultar los métodos de pago",
        "tags" : [ "Métodos de pago" ]
      }
    },
    "/api/v1/permissions" : {
      "get" : {
        "description" : "Devuelve el catálogo completo, sin paginar, ordenado por recurso y acción.\n\nLos tres filtros son opcionales y ninguno se valida: un valor que no\ncorresponda a ningún permiso produce una colección vacía, no un error.\nLos parámetros de paginación no se admiten y se ignoran.\n",
        "operationId" : "list_1",
        "parameters" : [ {
          "description" : "Filtro por recurso. Coincidencia exacta.",
          "in" : "query",
          "name" : "request",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListPermissionsRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionCatalogResponse"
                }
              }
            },
            "description" : "Catálogo devuelto. La colección puede venir vacía."
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionCatalogResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionCatalogResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `permissions:read` (`AUTH-002`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionCatalogResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de permisos",
        "tags" : [ "Permisos" ]
      }
    },
    "/api/v1/permissions/{id}" : {
      "get" : {
        "description" : "Devuelve qué habilita un permiso concreto: código, recurso, acción,\nnombre y descripción. Es la consulta de apoyo a la asignación de\npermisos a un rol, y se llega a ella desde el catálogo.\n\n**Se accede solo por identificador, nunca por código**: admitir dos\nformas de direccionar el mismo recurso obliga a distinguir en cada\npetición cuál llegó. El código es la vía para *encontrar* el permiso, y\npara eso está el filtro del catálogo.\n\n**No devuelve los roles que lo declaran** —es el recorrido inverso y\ncorresponde a una consulta propia— ni marcas temporales: en una tabla\nque solo cambia por migración, esas fechas dicen cuándo se desplegó una\nmigración y no cuándo ocurrió algo de negocio.\n\n`description` puede venir vacía, y entonces se devuelve como `null`,\nnunca omitida.\n",
        "operationId" : "detail",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "Detalle del permiso."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "El identificador no es un UUID en forma canónica (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `permissions:read` (`AUTH-002`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "No existe un permiso con ese identificador (`EX-001`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PermissionResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de un permiso",
        "tags" : [ "Permisos" ]
      }
    },
    "/api/v1/product-commission-rates" : {
      "get" : {
        "description" : "Devuelve una entrada **por cada rol** que cobra comisión por ese producto,\ncon el valor ya resuelto: las tasas de rol **vivas** de ese producto\n(`RN-CM-021`). Es lo mismo que `GET /api/v1/commission-rates?productId=`,\nsin paginar y con la forma de siempre.\n\n**Una lista vacía significa que ese producto no paga comisión a nadie**\n— nadie registró una tasa sobre él (`RN-CM-012`).\n\n**Esto no resuelve la comisión de una persona, y solo devuelve roles.** Las\ntasas personalizadas también se asocian a productos desde el 11-09-2026 y\nganan sobre estas, pero **no aparecen aquí**: qué personas tienen excepción\nen este producto lo responde `GET /api/v1/user-commission-rates?productId=`,\npaginado y con su historial. Para saber qué cobra alguien concreto, use\n`GET /api/v1/commissions/effective`.\n",
        "operationId" : "porProducto",
        "parameters" : [ {
          "in" : "query",
          "name" : "productId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductAssociationResponse"
                }
              }
            },
            "description" : "Las asociaciones del producto"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductAssociationResponse"
                }
              }
            },
            "description" : "Parámetros inválidos"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductAssociationResponse"
                }
              }
            },
            "description" : "Sin permiso"
          }
        },
        "summary" : "Consultar qué comisiona un producto, y a qué rol",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/product-images/{imageId}" : {
      "get" : {
        "description" : "Devuelve **los bytes** de la imagen de portada de un producto, con el\n`Content-Type` real —el detectado al subirla— y **sin token**: la\ndirección la dan las cuatro lecturas del producto en `coverImageUrl`,\ny una de ellas —el hotlink— es pública, de modo que un `<img>` tiene\nque poder pintarla sin credencial. Con token responde lo mismo.\n\n**La dirección señala una imagen concreta y no «la portada de un\nproducto»**: cada subida estrena identificador y la reemplazada se\nborra, de modo que una dirección **nunca cambia de contenido** — o\nsirve siempre la misma imagen, o responde `404`. Por eso se sirve con\n`Cache-Control: public, max-age=31536000, immutable`: el navegador la\nguarda un año y no vuelve a preguntar, y una portada nueva es otra\ndirección.\n\n**No mira el producto**: sirve la imagen aunque el producto esté\ninactivo, retirado o sea de alcance `TIENDA` (`requirements/pm.md`\n§5.2.9). El identificador no se lista en ningún sitio sin token para\nlo que no se publica. `404` con el mismo cuerpo para un identificador\ninexistente, reemplazado o quitado. Solo `JPEG`, `PNG` y `WebP`, con\n`X-Content-Type-Options: nosniff`.\n",
        "operationId" : "imagen",
        "parameters" : [ {
          "in" : "path",
          "name" : "imageId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "image/jpeg" : {
                "schema" : {
                  "format" : "binary",
                  "type" : "string"
                }
              },
              "image/png" : {
                "schema" : {
                  "format" : "binary",
                  "type" : "string"
                }
              },
              "image/webp" : {
                "schema" : {
                  "format" : "binary",
                  "type" : "string"
                }
              }
            },
            "description" : "Los bytes de la imagen, con su tipo real y caché inmutable de un año."
          },
          "400" : {
            "content" : {
              "application/problem+json" : { }
            },
            "description" : "Identificador sin forma canónica (`VAL-001`)"
          },
          "404" : {
            "content" : {
              "application/problem+json" : { }
            },
            "description" : "La imagen no existe — nunca existió, se reemplazó o se quitó (`EX-001`)"
          },
          "429" : {
            "content" : {
              "application/problem+json" : { }
            },
            "description" : "Demasiadas peticiones desde ese origen"
          },
          "500" : {
            "content" : {
              "application/problem+json" : { }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "security" : [ ],
        "summary" : "Obtener la imagen de una portada, sin autenticación",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products" : {
      "get" : {
        "description" : "Devuelve el catálogo **paginado**, con el destino de cada upgrade y la\nmoneda de cada precio ya resueltos: no hace falta una segunda consulta.\n\n**Los retirados quedan fuera salvo que se pidan** con `includeDeleted`.\nAl pedirlos se indica **desde cuándo** lo están; el **motivo** no viaja\nen el listado —uno a uno lo devuelve el detalle (`RF-PM-003`), en\nbloque sería una exportación de decisiones comerciales—. Verlos **no\nexige un permiso propio**: basta `products:read`.\n\nCada fila trae **los dos precios**: `price` —el que se cobra— y\n`purchasePrice` —el **precio de compra**, lo que NEXUS paga por el\nproducto—, este **presente y nulo** en los productos cuyo costo no se\nconoce. Este listado y el detalle son **los dos únicos sitios** donde\nse ven juntos: la oferta y el hotlink no devuelven el de compra\n(`RN-PM-024`, 12-09-2026).\n\nY trae **`exchange`**, la conversión a la moneda por omisión con la\ntasa vigente hoy, calculada **sobre `price`** —el de compra nunca se\nconvierte—. Llega **presente y nula** cuando el producto ya está en\nesa moneda o cuando nadie declaró una tasa: eso **no es un error** y\nel producto se devuelve igual.\n\nCada fila trae también **`videoUrl`**, la dirección del video que\npresenta el producto, **tal cual se guardó** y **presente y nula**\ncuando no tiene (`RN-PM-032`). **No es un filtro.**\n\nSolo se puede ordenar por la lista blanca —`name`, `price`,\n`createdAt`—, con `,asc` o `,desc`. **`purchasePrice` no está en\nella**: ordenar por una columna que admite nulos obligaría a decidir\ndónde van los productos sin costo conocido, y nadie lo ha decidido.\n**Un campo fuera de la lista se rechaza y no se ignora**:\nignorarlo devolvería un orden distinto del pedido sin decirlo. Por omisión se ordena por **fecha de alta\ndescendente**, y el orden aplicado viaja en la respuesta.\n\n**`targetMembershipId` no se valida contra el catálogo de membresías.**\nFiltrar por un destino inexistente devuelve la colección vacía y no es\nun error; combinarlo con `type=BOT` también, porque ningún\nbot tiene destino.\n\nLa búsqueda va sobre el nombre, **sin distinguir acentos ni\nmayúsculas** y por fragmento. En blanco equivale a no filtrar.\n\n**`scope` e `implementation` filtran como `type` y `status`**: se\nadmiten en cualquier caja y un valor fuera de dominio se rechaza junto\nal resto de parámetros inválidos, no en una vuelta aparte. El de\nalcance admite los cuatro valores —`TIENDA`, `HOTLINK`, `AMBOS`,\n`NINGUNO`— y **es la única lectura donde se ve un producto\n`NINGUNO`**, porque ninguna vista de venta lo ofrece.\n\nUn filtro sin coincidencias devuelve `200` con la colección vacía, y\nuna página más allá de la última hace lo mismo **con el total real**.\nNo hay `404` ni `422`.\n",
        "operationId" : "listar_3",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "sort",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "type",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "scope",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "implementation",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "sourceMembershipId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "targetMembershipId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "search",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "includeDeleted",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductPageResponse"
                }
              }
            },
            "description" : "Página del catálogo, con el orden aplicado."
          },
          "400" : {
            "description" : "Paginación fuera de rango (`VAL-001`), tipo (`VAL-002`) o estado (`VAL-003`) fuera de su dominio, identificador mal formado (`VAL-004`) o campo de ordenamiento no admitido (`VAL-005`). Los cuatro primeros se devuelven **juntos**"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:read` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de productos",
        "tags" : [ "Productos" ]
      },
      "post" : {
        "description" : "Registra un producto del catálogo, **siempre inactivo**: publicarlo es otra\noperación (`RF-PM-005`).\n\nEl tipo decide qué campos son obligatorios: un `UPGRADE_MEMBRESIA` debe\ndeclarar **las dos** membresías —de qué nivel sale y a cuál lleva— y un\n`BOT` no puede declarar ninguna.\n\n**El origen no puede estar por encima del destino**: eso sería vender un\ndescenso llamándolo upgrade, y se rechaza con `422` (`EX-006`). **Sí\npuede ser el MISMO**, y entonces el producto es una **renovación**: lo\nque vende es tiempo —su `validityDays`— y no un cambio de nivel.\n\n**Y no tiene por qué ser el inmediatamente inferior**: saltar niveles es\nlegítimo, y es la razón de que el origen se declare en lugar de\ndeducirse de la cadena.\n\n`icon` es el **nombre** del icono con el que el frontend pinta el producto\n—no una imagen—, en minúsculas y guion medio. Es **opcional**, y solo un\n`UPGRADE_MEMBRESIA` puede llevarlo: en un `BOT` se rechaza (`RN-PM-016`).\n\n`videoUrl` es **la dirección de un video** que presenta el producto —no\nel video—. Es **opcional y vale en los dos tipos**, sin la condición\ndel icono. Se comprueba **solo la forma**: una URL absoluta `http` o\n`https`, sin espacios y de hasta 500 caracteres; lo demás se rechaza\ncon `VAL-017`. **El sistema no sigue el enlace** —no comprueba que el\nvideo exista ni lo descarga—, y lo guarda **tal cual se escribió**,\nrecortado y sin normalizar nada más. Ausente o nulo significan lo\nmismo: no tiene video. **Al revés que el precio de compra, sale en las\ncuatro lecturas**, el hotlink sin token incluido (`RN-PM-032`).\n\nLa vigencia es opcional en los dos tipos: sin ella, lo adquirido no caduca.\n\n`scope` e `implementation` son **obligatorios y en los dos tipos**, y\n**no tienen valor por omisión**: `scope` dice **en qué vistas de venta**\nse ofrece el producto —`TIENDA` (solo la oferta), `HOTLINK` (solo el\ncanal de hotlinks), `AMBOS`, o `NINGUNO` (en ninguna: solo lo ve\nadministración, aunque esté activo)— e `implementation` dice si lo\ncomprado se aplica solo (`AUTOMATICA`) o espera a que un funcionario\nlo autorice (`MANUAL`). **`HOTLINKS` dejó de existir el 15-09-2026**\n—lo que significaba se llama `AMBOS`— y se rechaza con `400` como\ncualquier valor fuera del dominio.\n\n**Un producto lleva DOS precios y solo uno se cobra.** `price` es el\ndel sistema —el que copia la venta y sobre el que se comisiona— y\n`purchasePrice` es el **precio de compra**: lo que NEXUS paga por el\nproducto cuando tiene que comprarlo, y donde se guarda lo que costó.\nEs **opcional**, no interviene en ningún cálculo, se expresa en la\n**misma moneda** y **no sale de administración**: la oferta y el\nhotlink no lo devuelven. Ausente o nulo significan lo mismo —no se\nconoce todavía—, y **eso no es «costó cero»** (`RN-PM-023`). Se llamó\n`publicPrice` hasta el 12-09-2026; ese nombre es hoy una propiedad\ndesconocida y devuelve `400`.\n\nLos dos importes se validan igual: **no negativos** —el **cero se\nadmite** desde que existe la renovación de una membresía gratuita— y\ncon los decimales que declare su moneda. El rechazo **nombra el\ncampo** que incumple.\n\n**Los dos solo se ven desde administración** (`RN-PM-024`): la oferta\nde un cliente y el hotlink público devuelven **uno**.\n",
        "operationId" : "register",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterProductRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Producto registrado, en estado `INACTIVO`."
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Datos inválidos (serie `VAL-nnn`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Autenticado sin el permiso `products:create` (`AUTH-002`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Código o nombre ya en uso (`EX-005`, `EX-001`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductResponse"
                }
              }
            },
            "description" : "Membresía inexistente, moneda inexistente o inactiva, o un origen POR ENCIMA del destino (`EX-002`, `EX-003`, `EX-006`)"
          }
        },
        "summary" : "Registrar un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/available" : {
      "get" : {
        "description" : "Devuelve lo que **quien llama** puede comprar hoy, y no el catálogo.\n\n**No admite ningún parámetro**: ni de persona, ni de filtro, ni de\npaginación. El actor sale del token, de modo que no hay forma de\npreguntar qué puede comprar otra persona.\n\n**Exige `products:sale`**, un permiso propio de esta vista y no\n`products:read` — ese abre el catálogo administrativo entero, lo\ninactivo, lo retirado y el motivo del retiro, para que alguien pudiera\nver tres líneas.\n\n**Solo lo activo.** Ni lo inactivo ni lo retirado aparecen aquí,\naunque el catálogo de `RF-PM-002` sí los muestre a quien administra.\n\n**Upgrades: los declarados DESDE la membresía vigente de quien mira**,\ny ninguno más. La coincidencia es exacta: no se comparan niveles ni se\nrecorre la cadena. Quien registró el producto ya decidió a quién va\ndirigido.\n\nEso incluye **todos** los declarados desde ahí y no solo el inmediato\n—quien está en el peldaño más bajo elige cuánto saltar, y el precio de\ncada uno ya expresa el salto—, y llegan ordenados **del salto más\ncorto al más largo**.\n\n**La RENOVACIÓN entra aquí**, y va primera: un producto `X → X`\ndeclarado desde su propia membresía es el salto de longitud cero. Lo\nque vende es **tiempo** —su `validityDays`— y no un cambio de nivel.\n\n**Un upgrade hacia el nivel que ya se tiene NO se ofrece cuando su\norigen no es el suyo**: sería el salto de otra persona que acaba donde\nquien mira ya está. Y **ninguna bajada** llega hasta aquí, aunque este\nfiltro ya no la mire: lo impide `RN-PM-017` **al registrar**, porque un\nproducto declarado desde mi membresía no puede apuntar por debajo.\n\n**Bots: todos los activos, para cualquiera.** No dependen del nivel de\nquien mira ni de que tenga uno.\n\n**Cada producto trae `videoUrl`**, la dirección del video que lo\npresenta, tal cual se guardó y **presente y nula** cuando no tiene\n(`RN-PM-032`). Es lo contrario del precio de compra: material de\nventa, que existe para que lo vea quien compra, y por eso **sí** viaja\npor aquí.\n\n**Publica `scope` e `implementation` de cada producto, y filtra por el\nprimero**: solo `TIENDA` y `AMBOS` (`RN-PM-019`, desde el 15-09-2026).\nUn producto `HOTLINK` o `NINGUNO` no es de la tienda y no aparece\naquí aunque esté activo y sea de la membresía de quien llama. La\nimplementación viaja para que quien compra sepa **antes de pagar** si\nlo que se lleva se le entrega en el acto.\n\n**Quien no tiene membresía vigente —incluida la vencida— no ve ningún\nupgrade**, y sí todos los bots. No hay nivel desde el que subir, y\nofrecerle el primero sería venderle una membresía, que no es lo que un\nupgrade hace.\n\n**Quien está en la cima recibe la lista de upgrades vacía.** No es un\nerror ni un mensaje especial: es una lista vacía.\n\n**El precio es igual para todos**: un importe distinto según quién\nmira sería un descuento, y los descuentos son promociones, que están\nfuera de alcance.\n\n**Viene UN importe** (`RN-PM-024`, reescrita el 12-09-2026): `price`,\nel que la venta cobra. **El precio de compra no viaja por aquí**: es\nlo que NEXUS paga por el producto, y quien compra no tiene por qué\nconocer el margen. Entre el 08-09-2026 y el 12-09-2026 esta respuesta\ntraía también `publicPrice`, cuando ese importe era lo que se\nanunciaba; ese campo **ya no existe**.\n\nY viene **`exchange`**, la conversión de `price` a la moneda por\nomisión con la tasa vigente hoy. **Presente y nula** cuando no hay\nnada que convertir.\n\n**Quien construya la pantalla de compra tiene que saberlo**: el\nimporte que confirma la venta es `price`, el mismo que se enseña.\n\nLas dos colecciones viajan **envueltas en un objeto** y no como\narreglos en la raíz: hoy la oferta no se pagina, y así el día que\nhaya que paginarla no romperá a ningún cliente.\n",
        "operationId" : "oferta",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/OfferResponse"
                }
              }
            },
            "description" : "La oferta del actor, con el nivel desde el que mira."
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:sale` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar la oferta disponible para uno mismo",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/hotlinks" : {
      "get" : {
        "description" : "**Lo que un vendedor puede repartir**: los productos **activos y de\nalcance `HOTLINK` o `AMBOS`**, de los dos tipos, en `upgrades` y `services`\n— exactamente el conjunto que `GET /api/v1/hotlinks/{username}/{code}`\nresuelve enlace a enlace (`RN-PM-021`), visto entero y con token.\n\n**No mira la membresía de quien llama**, y ahí se aparta de la oferta\n(`GET /api/v1/products/available`): el vendedor no compra lo que\nreparte, de modo que un `BECA → ORO` le interesa aunque él esté en\n`ORO`. Por eso la respuesta no trae `currentMembership`. Cada producto\nva en la forma de venta —`price`, `exchange`, `videoUrl`,\n`coverImageUrl`, `rating`— y **sin `purchasePrice`** (`RN-PM-024`).\n\n**No trae el enlace armado.** El cliente lo compone con el `username`\nde `GET /api/v1/users/me` y el `code` de cada producto:\n`/api/v1/hotlinks/{username}/{code}`. Que ese enlace resuelva exige\nademás que quien lo reparte sea fuerza comercial (`RN-PM-022`); esta\nlista dice qué se publica, no quién puede publicarlo. **Sin\nparámetros y sin paginar**: es el catálogo publicable, entero. Los\npaquetes llegarán a esta misma respuesta con `RF-PM-026`.\n",
        "operationId" : "catalogoDeHotlinks",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HotlinkCatalogResponse"
                }
              }
            },
            "description" : "El catálogo de hotlinks: upgrades y bots publicables."
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:hotlink` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el catálogo de hotlinks",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/{id}" : {
      "get" : {
        "description" : "Devuelve el producto con su membresía destino y su moneda **resueltas**,\nsin exigir una segunda consulta.\n\nTrae **los dos precios** —`price`, el que se cobra, y `purchasePrice`,\nel **precio de compra**: lo que NEXUS paga por el producto, que llega\n**nulo y presente** si no se conoce— y **`exchange`**, la conversión\nde `price` a la moneda por omisión con la tasa vigente hoy. La\nconversión llega **presente y nula** cuando el producto ya está en esa\nmoneda o cuando nadie declaró una tasa (`RN-PM-024`). El precio de\ncompra solo se ve aquí y en el listado: la oferta y el hotlink no lo\ndevuelven.\n\nTrae **`videoUrl`**, la dirección del video que presenta el producto,\n**tal cual se guardó** y **presente y nula** cuando no tiene\n(`RN-PM-032`) — también en un producto retirado.\n\n**Un producto retirado se devuelve marcado como tal**, no como\ninexistente: `deletedAt` dice desde cuándo y `deletionReason` **por\nqué**. Los dos campos **solo aparecen si el producto está retirado** —\nsu ausencia significa que sigue vivo—. El motivo llega con\n`products:read` y **sin exigir permiso de auditoría**; es la\ncontrapartida asumida de que el detalle lo devuelva.\n\n**No devuelve autoría en ninguna forma**: ni quién lo creó, ni quién lo\ncorrigió, ni quién lo retiró. Eso vive en la auditoría y tiene su\npropio permiso.\n\n`targetMembership` y `validityDays` viajan **presentes en nulo** cuando\nno aplican: un bot no tiene destino y un producto puede no\ncaducar, y un campo ausente es indistinguible de uno que el cliente no\nconoce.\n\nEl **nivel** del destino es el **actual**, no el que tenía cuando se\ncreó el producto: la cadena de membresías se reordena al insertar un\neslabón.\n\nUn identificador con forma laxa se rechaza como **dato inválido**\n(`400`), no como recurso no encontrado.\n",
        "operationId" : "detalle_2",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductDetailResponse"
                }
              }
            },
            "description" : "El producto, con su destino y su moneda resueltos."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:read` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de un producto",
        "tags" : [ "Productos" ]
      },
      "patch" : {
        "description" : "Corrige el **nombre**, la **descripción**, el **icono**, el **enlace\ndel video**, **los dos precios**, la **moneda**, la **vigencia**, el\n**alcance** y la **implementación**. Se aplica lo que llega y se deja\nintacto lo que no.\n\n**Distingue el campo ausente del enviado vacío**, y de ahí salen dos\ncomportamientos opuestos: `description: null`, `icon: null`,\n`videoUrl: null`, `validityDays: null` y `purchasePrice: null`\n**vacían** el campo,\nmientras que `name: null` y `price: null` se **rechazan**, porque un\nproducto sin nombre o sin precio del sistema no puede existir.\n\n**`purchasePrice` es el precio de compra** —lo que NEXUS paga por el\nproducto— y esta operación es donde hoy se registra lo que costó.\n**Vaciarlo NO es ponerlo a cero**: con nulo el costo pasa a «no se\nconoce», y con cero a «no costó nada». Son dos estados distintos y\nlos dos se alcanzan desde aquí. Se llamó `publicPrice` hasta el\n12-09-2026; ese nombre es hoy una propiedad desconocida y devuelve\n`400`.\n\n**`scope: null` e `implementation: null` también se rechazan**, y ahí\nvan con el nombre y no con la descripción: son obligatorios en la\ncolumna, de modo que «bórralo» no tiene ningún estado al que llevar el\nproducto. Devuelven `400` con `VAL-007` y `VAL-008`.\n\n**El alcance y la implementación SÍ se corrigen, aunque el tipo y las\nmembresías no**: ninguna de las dos define qué derecho otorga el\nproducto —una dice en qué vistas se ofrece y la otra quién lo aplica—,\nde modo que corregirlas no reescribe lo que compró quien lo compró.\n`scope` admite `TIENDA`, `HOTLINK`, `AMBOS` y `NINGUNO`; **corregir\nun producto activo a `NINGUNO` no lo desactiva**: sigue activo y deja\nde ofrecerse. `HOTLINKS` se rechaza con `400`, como cualquier valor\nfuera del dominio.\n\n**El icono sí se corrige, aunque el tipo no**: es el aspecto del\nproducto y no lo que otorga. En un `BOT`, cualquier valor distinto de\nnulo se rechaza con `VAL-013` (`RN-PM-016`).\n\n**`videoUrl` se corrige y se vacía en los DOS tipos** —con `null` o\ncon `\"\"`, que aquí también es un vaciado— y sin la condición del\nicono (`RN-PM-032`). Se comprueba **solo la forma** —URL absoluta\n`http` o `https`, sin espacios, hasta 500 caracteres— y lo demás se\nrechaza con `VAL-009` **sin aplicar ningún otro cambio** de la misma\npetición. Se guarda tal cual, recortado, y el sistema no lo sigue.\n\n**El tipo, el código y la membresía destino NO se pueden corregir**, y\nenviarlos devuelve `400` con `VAL-006`. Se rechazan y no se ignoran:\nignorarlos haría creer que el cambio se aplicó. Definen qué derecho\notorga el producto, y cambiarlos convertiría lo comprado en otra cosa.\n\n**Los DOS importes se validan contra la moneda que va a quedar**, y no\nsolo el que llega en la petición: cambiar **solo** la moneda puede\ndejar sin caber a un precio que nadie tocó, y el rechazo **nombra el\ncampo** que no cabe. Y el importe **no se convierte**: el sistema no\nhace conversión de divisa — cambiar de moneda es declarar que ese\nnúmero siempre estuvo en la otra.\n\n**Un producto retirado no se corrige**: lo que se retiró debe quedar\ncomo estaba para que lo que lo referencie siga diciendo la verdad.\n\n**No se exige motivo**, ni siquiera al cambiar el precio. Una petición\nque no cambia nada devuelve `200` y **no registra evento**.\n",
        "operationId" : "corregir_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateProductRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductDetailResponse"
                }
              }
            },
            "description" : "El producto, ya corregido."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`), nombre vacío o ningún campo informado (`VAL-002`), longitud excedida (`VAL-003`), precio negativo o precio del sistema vaciado (`VAL-004`), decimales que la moneda no admite (`VAL-005`), campos inmutables en la petición (`VAL-006`) o vigencia no positiva (`VAL-011`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto vivo con ese identificador (`EX-001`)"
          },
          "409" : {
            "description" : "El nombre ya lo tiene otro producto vivo (`EX-002`)"
          },
          "422" : {
            "description" : "La moneda no existe o está desactivada (`EX-003`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Corregir un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/{id}/comments" : {
      "get" : {
        "description" : "Devuelve, **sin token**, las reseñas vivas de un producto, paginadas y de la más\nreciente a la más antigua. **Es público por decisión** (`RF-PM-012`): la pantalla\ndel hotlink las necesita y no tiene con qué autenticarse.\n\n**Del autor solo viaja su nombre y apellido** (`RN-PM-030`): ni identificador, ni\nnombre de usuario, ni correo, ni estado, ni roles. Es la segunda ruta pública del\nsistema que publica el nombre de una persona, después del hotlink, y hereda sus dos\ndecisiones.\n\n**La misma respuesta con token que sin él.** No marca cuál reseña es la de quien\nllama; para eso está `GET .../comments/mine`.\n\n**El `404` es uniforme**: producto inexistente, inactivo, retirado o identificador\nmal formado responden **el mismo cuerpo** (`RN-PM-028`). Un anónimo no debe poder\nsaber si un identificador corresponde a un producto en preparación.\n\n`updatedAt` distinto de `createdAt` significa «corregida»; no hay campo aparte.\n\n**El texto NO está saneado**: escápelo al pintarlo.\n\nAcotada por origen: 120 por minuto sobre la familia `/api/v1/products/*/comments`.\n",
        "operationId" : "list",
        "parameters" : [ {
          "description" : "Identificador del producto (UUID)",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseProductCommentPublicItem"
                }
              }
            },
            "description" : "Página de reseñas vivas del producto."
          },
          "400" : {
            "description" : "Paginación inválida (`VAL-003` de `shared/pagination`)"
          },
          "404" : {
            "description" : "El producto no existe, está inactivo, está retirado o el identificador está mal formado (`EX-001`) — el mismo cuerpo en los cuatro casos"
          },
          "429" : {
            "description" : "Límite de tasa por origen superado"
          }
        },
        "security" : [ ],
        "summary" : "Consultar las reseñas de un producto",
        "tags" : [ "Reseñas" ]
      },
      "post" : {
        "description" : "Registra **la** reseña de quien llama sobre un producto: una puntuación entera de\n1 a 5 y un texto de 1 a 1000 caracteres, **las dos obligatorias** (`RN-PM-025`).\n\n**El autor sale del token.** El cuerpo no admite campo de persona: un `userId`\nes un campo desconocido y se rechaza con `400`. No existe forma de reseñar en\nnombre de otro.\n\n**Una por persona y producto, entre las vivas** (`RN-PM-026`). La segunda sobre el\nmismo producto responde `409` mientras la primera siga viva; quien cambie de\nopinión la corrige (`PATCH .../comments/{commentId}`). Retirada la suya, puede\nescribir otra.\n\n**Solo se reseña lo que se puede comprar** (`RN-PM-028`): un producto activo y no\nretirado. **El `404` no distingue** entre inexistente, inactivo y retirado — quien\nreseña es un cliente y ve la oferta, que tampoco distingue.\n\n**Un decimal en la puntuación se rechaza, no se redondea**, y una cadena tampoco es\nuna puntuación: los dos responden `400`.\n\nDesde la primera reseña escrita, las cuatro lecturas del producto publican\n`rating` con el promedio (dos decimales, **nulo** sin reseñas) y la cantidad de\nreseñas **vivas** (`RN-PM-031`).\n\n**El texto se guarda tal cual y NO se sanea.** La lista es pública: el front debe\nescaparlo al pintarlo.\n",
        "operationId" : "create",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CreateProductCommentRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductCommentResponse"
                }
              }
            },
            "description" : "Reseña registrada. Sin autor: quien la recibe es quien la escribió."
          },
          "400" : {
            "description" : "Identificador mal formado (`VAL-001`), puntuación ausente (`VAL-002`) o fuera de 1..5 (`VAL-003`), texto vacío (`VAL-004`) o de más de 1000 caracteres (`VAL-005`), o campos no admitidos en el cuerpo (`VAL-006`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:comment` (`AUTH-002`)"
          },
          "404" : {
            "description" : "El producto no existe, está inactivo o está retirado (`EX-001`)"
          },
          "409" : {
            "description" : "Quien llama ya tiene una reseña viva sobre este producto (`EX-002`)"
          }
        },
        "summary" : "Reseñar un producto",
        "tags" : [ "Reseñas" ]
      }
    },
    "/api/v1/products/{id}/comments/mine" : {
      "get" : {
        "description" : "Devuelve **la** reseña viva de quien llama sobre el producto, con su identificador —\nque es lo que la corrección y el retiro reciben en la ruta—. **No admite parámetro\nde persona**: responde sobre quien llama y sobre nadie más.\n\n**El `404` no dice nada del producto**: responde igual si quien llama no ha\nreseñado, si retiró la suya o si el producto no existe. Y **sí responde** sobre un\nproducto inactivo o retirado cuando la reseña existe: el autor tiene que poder\nllegar a la suya para corregirla o retirarla (`RN-PM-028`).\n",
        "operationId" : "mine",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductCommentResponse"
                }
              }
            },
            "description" : "La reseña de quien llama."
          },
          "400" : {
            "description" : "Identificador mal formado (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:comment` (`AUTH-002`)"
          },
          "404" : {
            "description" : "Quien llama no tiene reseña viva sobre este producto (`EX-001`)"
          }
        },
        "summary" : "Consultar la reseña propia sobre un producto",
        "tags" : [ "Reseñas" ]
      }
    },
    "/api/v1/products/{id}/comments/{commentId}" : {
      "delete" : {
        "description" : "Retira lógicamente la reseña de quien llama, **sin motivo y sin cuerpo**. Es la\nprimera baja de una entidad de negocio del sistema que no pide `reason`: el\nArt. V.13 admite desde el 14-09-2026 el **contenido propio** como excepción, porque\nel único «por qué» posible —su autor ya no la quiere ahí— está en el propio evento.\nEl registro de eliminación se escribe igual, con la instantánea y un motivo fijo.\n\nPor eso es un `DELETE` y no un `POST /deletion`: el retiro del producto usa `POST`\nporque su cuerpo lleva el motivo, y aquí no hay cuerpo que proteger. Un cuerpo, si\nllega, se ignora.\n\n**Solo el autor** (`RN-PM-027`), sin excepción: el superadministrador recibe `403`\nsobre una ajena. **No existe moderación.**\n\n**Retirar dos veces responde `404`**, no `409`: la reseña retirada no la devuelve\nnadie, de modo que «retirada» y «no existe» son lo mismo. Retirada la suya, la\npersona puede escribir otra, y el promedio del producto la deja de contar en el\nacto.\n",
        "operationId" : "delete",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "commentId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "204" : {
            "description" : "Reseña retirada."
          },
          "400" : {
            "description" : "Identificador mal formado (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:comment` (`AUTH-002`), o **con él pero sin ser el autor** (`EX-002`)"
          },
          "404" : {
            "description" : "La reseña no existe, ya está retirada o no es del producto de la ruta (`EX-001`)"
          }
        },
        "summary" : "Retirar la reseña propia",
        "tags" : [ "Reseñas" ]
      },
      "patch" : {
        "description" : "Corrige la puntuación, el texto o los dos. **Lo que no viene no cambia**, y\n**ningún campo admite el nulo explícito**: los dos son obligatorios. Un cuerpo sin\nningún campo responde `400`, como en la edición del producto.\n\n**Solo el autor** (`RN-PM-027`). El permiso `products:comment` habilita la operación;\nser el autor la autoriza, y son dos cosas distintas: un administrador con el\npermiso corrige las suyas y recibe `403` en las ajenas. **No existe moderación.**\n\nPrimero «existe» (`404`) y después «es tuya» (`403`). Una reseña **retirada**\nresponde `404` también a su autor: no se revive corrigiendo. La reseña debe ser\n**del producto de la ruta**; si no, `404`.\n\n**Se corrige aunque el producto ya no se venda.** Un cuerpo cuyos valores\ncoinciden con los guardados responde `200` sin escribir nada y sin avanzar\n`updatedAt`.\n",
        "operationId" : "update",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "path",
          "name" : "commentId",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateProductCommentRequest"
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductCommentResponse"
                }
              }
            },
            "description" : "La reseña corregida, con `updatedAt` avanzado si algo cambió."
          },
          "400" : {
            "description" : "Identificador mal formado (`VAL-001`), puntuación nula o fuera de 1..5 (`VAL-002`, `VAL-003`), texto nulo, vacío o largo (`VAL-004`), campos no admitidos (`VAL-006`) o cuerpo sin ningún campo (`VAL-005`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:comment` (`AUTH-002`), o **con él pero sin ser el autor** (`EX-002`)"
          },
          "404" : {
            "description" : "La reseña no existe, está retirada o no es del producto de la ruta (`EX-001`)"
          }
        },
        "summary" : "Corregir la reseña propia",
        "tags" : [ "Reseñas" ]
      }
    },
    "/api/v1/products/{id}/cover" : {
      "delete" : {
        "description" : "Vacía la portada: el producto vuelve a pintarse con su icono —o con el\nque el frontend le pone por omisión, si es un bot— y **la imagen se\nborra**: su dirección deja de servir.\n\n**Es la única de las tres operaciones de la portada que puede decir\nque no**: un upgrade **sin icono** no puede quedarse sin portada\n(`RN-PM-034`), y se rechaza con `VAL-002` nombrando `icon` — lo que\nfalta. El orden para arreglarlo es declarar el icono con\n`PATCH /api/v1/products/{id}` y después quitar la portada. A un bot\nse le quita siempre.\n\n**Sin portada responde igual y no escribe nada**: ni auditoría ni\n`updatedAt`. Responde `200` con el producto, y no `204`: se vacía un\ncampo, no se retira una entidad. Sin cuerpo; si llega uno, se ignora.\n",
        "operationId" : "quitarPortada",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductDetailResponse"
                }
              }
            },
            "description" : "El producto, con `coverImageUrl` nulo."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`) o upgrade sin icono que se quedaría sin nada que pintar (`VAL-002`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto vivo con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Quitar la portada de un producto",
        "tags" : [ "Productos" ]
      },
      "put" : {
        "description" : "Recibe **un archivo** —`multipart/form-data`, una sola parte llamada\n`file`— y lo convierte en la portada del producto. **Es el primer\narchivo que el sistema guarda** (`RN-PM-033`, `requirements/pm.md`\n§5.2.9), y lo guarda **tal cual**: ni recorte, ni redimensión, ni\nconversión.\n\n**El tipo lo deciden los bytes, no la cabecera**: `JPEG`, `PNG` o\n`WebP`, reconocidos por su firma; la cabecera `Content-Type` de la\nparte y el nombre del archivo se ignoran. Un `GIF`, un `SVG` o un\ntexto con extensión `.png` se rechazan con `VAL-003`. **Hasta 5 MB**\n(`VAL-004`); sin archivo o vacío, `VAL-002`. Los tres nombran `file`.\n\n**Si el producto ya tenía portada, la reemplaza**: la nueva estrena\nidentificador, **la anterior se borra** y **la dirección cambia** —\n`coverImageUrl` señala una imagen concreta, no «la portada del\nproducto», y por eso `GET /api/v1/product-images/{imageId}` se sirve\ncon caché inmutable. **En los dos tipos y sin condición**: subir una\nportada nunca deja al producto peor.\n\n**El alta no admite la imagen**: `POST /api/v1/products` sigue siendo\nJSON, y la portada se sube después con esta operación. Responde con\nel producto, como la corrección, y **un producto retirado no admite\nportada**.\n",
        "operationId" : "subirPortada",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "multipart/form-data" : {
              "schema" : {
                "properties" : {
                  "file" : {
                    "format" : "binary",
                    "type" : "string"
                  }
                },
                "type" : "object"
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductDetailResponse"
                }
              }
            },
            "description" : "El producto, con `coverImageUrl` señalando la imagen nueva."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`), sin archivo o vacío (`VAL-002`), ni JPEG ni PNG ni WebP por sus bytes (`VAL-003`), más de 5 MB (`VAL-004`) o petición que no es `multipart/form-data` (`EX-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto vivo con ese identificador (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Subir o reemplazar la portada de un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/{id}/deletion" : {
      "post" : {
        "description" : "Retira el producto: **eliminación lógica y con motivo** (Art. V.13).\nLa fila se conserva —lo vendido tiene que seguir resolviendo a lo que\nse vendió— y el producto deja de ofrecerse.\n\n**`POST` sobre un subrecurso y no `DELETE` con cuerpo**, igual que al\neliminar un rol o una persona: RFC 9110 no define semántica para el\ncuerpo de un `DELETE` y un intermediario puede descartarlo, con lo que\nla petición se convertiría en un rechazo por motivo ausente que quien\nla envió no puede entender ni corregir. Y tampoco en la URL, donde el\nmotivo quedaría escrito en los registros de acceso de cualquier proxy.\n\n**El motivo es obligatorio** y se comprueba lo primero de todo, antes\nde tocar la base.\n\n**El estado no se modifica al retirar.** El registro de eliminación\nconserva si el producto **estaba a la venta**: desactivarlo «de paso»\nharía que todos los registros dijeran «inactivo» y ese dato dejaría de\nsignificar nada.\n\n**Qué libera el retiro y qué no**: el **destino** del upgrade queda\nlibre para que otro se active, y el **nombre** queda libre para otro\nproducto. El **código no se libera nunca** — el día que una factura\ndiga `UPGRADE_ORO` tiene que resolver a un solo producto para siempre.\n\n**No es idempotente a propósito**: retirar uno ya retirado devuelve\n`409`. Dos motivos sobre un solo hecho es evidencia contradictoria.\n\nNo devuelve el producto: lo que se acaba de retirar no es algo que el\nsistema deba seguir ofreciendo a quien lo pidió.\n",
        "operationId" : "retirar_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeleteProductRequest"
              }
            }
          }
        },
        "responses" : {
          "204" : {
            "description" : "Producto retirado."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`), motivo ausente o en blanco (`VAL-002`) o demasiado largo (`VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:delete` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto con ese identificador (`EX-001`)"
          },
          "409" : {
            "description" : "El producto ya estaba retirado (`EX-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Retirar un producto del catálogo",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/products/{id}/status" : {
      "patch" : {
        "description" : "Cambia el estado del producto entre `ACTIVO` e `INACTIVO`. **Es un\nrecurso propio y no un campo de la edición**: publicar y corregir son\ndecisiones distintas, y mezclarlas haría que una corrección de texto\npudiera poner algo a la venta.\n\n**Pedir el estado que el producto ya tiene devuelve `200` sin cambiar\nnada y sin registrar evento.** No es un error: quien pulsa dos veces el\nmismo botón no ha hecho nada malo.\n\n**No se publica un producto sin descripción** (`RN-PM-014`). Sí se\npermite **desactivarlo** sin ella: la regla acota lo que se ofrece, no\nlo que se retira.\n\n**Solo puede haber un upgrade activo hacia cada membresía destino**\n(`RN-PM-004`). Al activar uno cuyo destino ya está ocupado, el rechazo\n**nombra el producto que lo ocupa**, para que se sepa cuál desactivar.\nDesactivar no comprueba nada: liberar un destino nunca produce\nconflicto.\n\n**Un producto retirado no vuelve a la venta por aquí**: responde que no\nexiste.\n\nNo se exige motivo para activar ni para desactivar.\n",
        "operationId" : "cambiarEstado_2",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeProductStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProductDetailResponse"
                }
              }
            },
            "description" : "El producto, con su estado ya aplicado."
          },
          "400" : {
            "description" : "Identificador sin forma canónica (`VAL-001`), estado ausente o fuera de su dominio (`VAL-002`), o activación de un producto sin descripción (`VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso `products:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe un producto vivo con ese identificador (`EX-001`)"
          },
          "409" : {
            "description" : "Ya hay otro upgrade activo hacia esa membresía destino (`EX-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Publicar o retirar de la oferta un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v1/roles" : {
      "get" : {
        "description" : "Listado paginado con filtros, búsqueda y ordenamiento. Es la entrada\nnatural a la administración de accesos: de aquí se navega al detalle de\ncada rol.\n\n**El orden por defecto es `code,asc`**: el código es el identificador\ncon el que se habla de un rol en la documentación y en la auditoría.\nSolo se puede ordenar por la lista blanca —`code`, `name`, `roleType`,\n`status`, `createdAt`, `updatedAt`—; cualquier otro campo devuelve\n`400` y **no llega a la base de datos**.\n\n**No incluye los permisos de cada rol** ni el número de usuarios\nasignados: la primera pregunta la responde el detalle\n(`GET /api/v1/roles/{id}`) y la segunda se hace sobre un rol concreto,\nno sobre la lista.\n\n**`parentRoleId` no se valida contra el catálogo.** Filtra por el padre\n**directo** —no por el subárbol— y uno inexistente devuelve la\ncolección vacía, que no es un error.\n\nLa búsqueda va sobre código y nombre, **sin distinguir acentos ni\nmayúsculas**, y por fragmento: `ministracion` encuentra\n`ADMINISTRACION`. Un término en blanco equivale a no filtrar.\n\n**Los eliminados quedan fuera salvo que se pidan** con\n`includeDeleted=true`; cuando se piden, `deletedAt` es lo que permite\ndistinguirlos.\n",
        "operationId" : "listar_2",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "sort",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "roleType",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "parentRoleId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "search",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "includeDeleted",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de roles."
          },
          "400" : {
            "description" : "Paginación fuera de límites, campo de ordenamiento no admitido (`VAL-003`), o `status`/`roleType` fuera de su dominio (`VAL-004`). Se evalúan y se devuelven **juntos** en `errors`"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `roles:read` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar roles",
        "tags" : [ "Roles" ]
      },
      "post" : {
        "description" : "Registra un rol bajo un rol padre existente y activo, con los permisos que\nse declaren.\n\nLos permisos deben estar contenidos en los del rol padre (`RN-SEG-003`) y en\nlos permisos efectivos de quien ejecuta el alta (`RN-SEG-010`). Declararlos es\nopcional: sin ellos el rol queda registrado y a la espera de `RF-SP-005`.\n\nEl rol nace siempre **activo** y nunca de sistema. Ni `status` ni `isSystem`\nse admiten en el cuerpo: enviarlos devuelve `400`.\n",
        "operationId" : "registrar_2",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CreateRoleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol registrado. La cabecera `Location` lleva su dirección."
          },
          "400" : {
            "description" : "Formato u obligatoriedad incumplidos (`VAL-001` a `VAL-004`, `VAL-007`, `VAL-008`), o el cuerpo trae un campo que este endpoint no admite"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `roles:create` (`AUTH-002`)"
          },
          "409" : {
            "description" : "Código o nombre ya en uso (`RN-SEG-001`), permiso fuera del rol padre (`RN-SEG-003`) o fuera del alcance del actor (`RN-SEG-010`)"
          },
          "422" : {
            "description" : "El rol padre no existe o no está activo (`EX-002`), o algún permiso no está en el catálogo (`EX-005`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Registrar un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}" : {
      "get" : {
        "description" : "Devuelve el alcance exacto de un rol: **la lista completa de los\npermisos que declara**, su rol padre, **cuántos** roles cuelgan de él y\n**cuántas** personas lo tienen asignado.\n\n**Los permisos son los declarados, sin recorrer la cadena de\nancestros** (`RN-SEG-004`): el modelo no usa herencia, y esa decisión\nexiste precisamente para que esta pregunta se responda leyendo una sola\nlista. Van completos y sin paginar, ordenados por código.\n\n**`childRoleCount` es un número, nunca una lista**: el listado de hijos\nse obtiene con `GET /api/v1/roles?parentRoleId={id}`, que ya está\npaginado. Así el tamaño de la respuesta no depende de cuántos hijos\ntenga el rol.\n\n**`assignedUserCount` cuenta personas distintas**, en cualquier estado\n—incluidas las inactivas y las bloqueadas— y **excluyendo las\neliminadas**: alguien bloqueado sigue portando el rol, y su existencia\nes lo que impide borrarlo. Cero es un dato, no una ausencia. **No exige\n`users:read`**: es un agregado sin identidad, y quien quiera saber\nquiénes son usa `GET /api/v1/users?roleId={id}`.\n\nUn rol **eliminado devuelve el mismo `404`** que uno inexistente, sin\nninguna pista de que existió.\n",
        "operationId" : "detalle_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleDetailResponse"
                }
              }
            },
            "description" : "Detalle del rol."
          },
          "400" : {
            "description" : "El identificador no es un UUID en forma canónica (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `roles:read` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe el rol, o está eliminado lógicamente (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de un rol",
        "tags" : [ "Roles" ]
      },
      "patch" : {
        "description" : "Modifica el **nombre** y la **descripción**. Nada más: el código es\nestable por diseño —cambiarlo rompería cualquier referencia externa— y\nla clasificación es **inmutable**, porque determina si el rol puede\nllevar membresía y cambiarla dejaría membresías colgando de un rol que\nya no es consumidor. Los permisos, el estado y el rol padre tienen cada\nuno su operación.\n\nAl menos uno de los dos campos debe venir informado. `description`\nadmite `null` explícito —borrarla es una orden legítima—; `name` no,\nporque un rol sin nombre no existe.\n\n**Reenviar los valores actuales no es un error ni registra evento**: si\nnada cambió, no hay nada que auditar.\n\n**Un rol de sistema devuelve `409` y un rol que usted tiene asignado,\n`403`**. Lo segundo impide ampliar el alcance del rol con el que se\nestá entrando; alcanza solo a los roles asignados **directamente**, de\nmodo que un ancestro del propio sí puede editarse.\n",
        "operationId" : "editar_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateRoleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol actualizado."
          },
          "400" : {
            "description" : "Ningún campo informado (`VAL-001`), nombre vacío (`VAL-002`) o longitud excedida (`VAL-004`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Sin `roles:update` (`AUTH-002`), o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol no existe o está eliminado (`EX-004`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol de sistema (`RN-SEG-012`) o nombre ya en uso (`RN-SEG-001`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Editar un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}/deletion" : {
      "post" : {
        "description" : "Elimina lógicamente el rol. **El motivo es obligatorio** y se verifica\n**antes de ejecutar nada** (Art. V.13); no se admite un valor generado\npor el sistema.\n\n**Se rechaza si el rol tiene roles hijos vigentes** —diciendo cuáles— o\n**personas asignadas** —diciendo cuántas— (`RN-SEG-008`). En el segundo\ncaso la respuesta sugiere **desactivar** el rol, que suele ser lo que\nen realidad se quería: retirar el acceso sin perder el rol. El conteo\nincluye a las personas inactivas y bloqueadas, y excluye a las\neliminadas.\n\nTras eliminarlo, **su código y su nombre quedan libres** para un rol\nnuevo, y el rol deja de aparecer en el listado por defecto. La\nauditoría de eliminación conserva el motivo y el rol completo —con sus\npermisos por código— para poder reconstruir qué era.\n\n**`POST` sobre un subrecurso y no `DELETE`**: el motivo viaja en el\ncuerpo, y un intermediario puede descartar el cuerpo de un `DELETE`.\n",
        "operationId" : "eliminar_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeleteRoleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Rol eliminado. Sin cuerpo."
          },
          "400" : {
            "description" : "Motivo ausente o vacío (`VAL-001`, `VAL-002`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Sin `roles:delete`, o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "description" : "El rol no existe o ya está eliminado (`EX-006`)"
          },
          "409" : {
            "description" : "Rol de sistema (`RN-SEG-012`), rol raíz (`RN-SEG-007`), o el rol tiene hijos o personas asignadas (`RN-SEG-008`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Eliminar un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}/parent" : {
      "patch" : {
        "description" : "Reubica el rol bajo otro padre y **revalida la contención contra el\nnuevo** (`RN-SEG-013`): si el rol declara permisos que el nuevo padre\nno posee, la operación se rechaza **entera** enumerándolos, para que\nusted decida cuáles retirar. **Nunca se recorta nada en silencio.**\n\n**Los hijos acompañan al rol** y no hay que revisarlos: si este cabe en\nel nuevo padre, ellos caben en este por transitividad.\n\nEl nuevo padre debe existir y estar **activo**, y no puede ser el\npropio rol ni uno de sus descendientes —eso formaría un ciclo—. Su\nclasificación es indiferente: un rol comercial puede colgar de uno\nfuncionario, porque el padre acota privilegios, no clasifica.\n\n**El rol raíz no admite padre** y ningún rol puede quedarse sin él:\n`RN-SEG-007` exige exactamente una cima.\n",
        "operationId" : "cambiarPadre",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeRoleParentRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol con su nuevo rol padre."
          },
          "400" : {
            "description" : "Rol padre ausente o mal formado (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Sin `roles:update`, o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol no existe o está eliminado (`EX-006`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol de sistema (`RN-SEG-012`), rol raíz (`RN-SEG-007`), ciclo en la jerarquía (`RN-SEG-006`) o permisos que el nuevo padre no concede (`RN-SEG-013`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol padre indicado no existe o no está activo (`EX-004`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Cambiar el rol padre",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}/permissions" : {
      "post" : {
        "description" : "Asocia permisos al rol. **La operación se aplica entera o no se\naplica**: un solo permiso que incumpla la contención rechaza la\npetición completa, porque aplicar los válidos dejaría el rol en un\nestado que nadie pidió.\n\nCada permiso debe estar **contenido en los del rol padre**\n(`RN-SEG-003`) y **en los permisos efectivos de quien ejecuta la\noperación** (`RN-SEG-010`). Lo segundo no lo concede `roles:update`:\nese permiso habilita a modificar roles, no a decidir con qué alcance.\n\n**El rol raíz omite la primera comprobación** —no tiene cota superior—\ny conserva la segunda.\n\n**Es idempotente y nunca retira nada**: los permisos ya declarados se\nignoran sin error, y si no queda ninguno por agregar no se registra\nevento. Hasta 100 por petición.\n\n**Los roles de sistema también reciben permisos** por aquí: `RN-SEG-012`\nprotege su identidad y su posición, no lo que conceden. Los rangos de\nla fuerza comercial y `CLIENTE` nacen sin ninguno, y esta es su única\nvía. Para `AGENTE` hay que pasar antes por `MANAGER` y `DIRECTOR`,\nporque la contención se valida contra el padre inmediato.\n",
        "operationId" : "agregarPermisos",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RolePermissionsRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol con sus permisos actualizados."
          },
          "400" : {
            "description" : "Sin permisos (`VAL-001`) o más de 100 (`VAL-006`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Sin `roles:update`, o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol no existe o está eliminado (`EX-006`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Permiso fuera del rol padre (`RN-SEG-003`) o fuera del alcance del actor (`RN-SEG-010`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Uno o más permisos no existen en el catálogo (`EX-003`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Agregar permisos a un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}/permissions/revocations" : {
      "post" : {
        "description" : "Desasocia permisos del rol, con **eliminación física** de la asociación\ny **sin exigir motivo**: es una asociación y no una entidad de negocio.\n\n**Se rechaza si un rol hijo directo declara alguno de los permisos que\nse retiran** (`RN-SEG-005`), y el error dice **qué rol** declara **qué\npermiso**: sin ese detalle no habría forma de saber qué corregir. Los\nhijos **inactivos cuentan igual** que los activos —el invariante vale\nsiempre—, y los eliminados no.\n\n**No hay cascada**: nunca se retiran permisos de los descendientes.\nUsted decide en qué orden hacerlo.\n\n**Es idempotente**: los permisos que el rol no declaraba se ignoran sin\nerror. Hasta 100 por petición, el mismo límite que al agregarlos.\n\n**`POST` sobre un subrecurso y no `DELETE`**: la operación recibe una\nlista en el cuerpo, y RFC 9110 no define semántica para el cuerpo de un\n`DELETE`.\n\n**Alcanza también a los roles de sistema**: lo que se les concede tiene\nque poder corregirse por el mismo camino.\n",
        "operationId" : "retirarPermisos",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RolePermissionsRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol con sus permisos actualizados."
          },
          "400" : {
            "description" : "Sin permisos (`VAL-001`) o más de 100 (`VAL-004`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Sin `roles:update`, o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol no existe o está eliminado (`EX-004`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Un rol dependiente declara el permiso (`RN-SEG-005`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Retirar permisos de un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/roles/{id}/status" : {
      "patch" : {
        "description" : "**Desactivar no es retirar.** Las asignaciones a personas se conservan\nintactas; lo que cambia es que el rol **deja de conceder permisos de\ninmediato**, sin esperar a que ningún token expire.\n\nSe pide el **estado destino** —`ACTIVO` o `INACTIVO`— y no una acción,\nde modo que repetir la petición deja el mismo resultado y no registra\nevento.\n\n**El rol raíz no admite esta operación**, aparte de la prohibición\ngeneral sobre los roles de sistema: un rol raíz inactivo no concede\nnada, y eso dejaría al sistema sin su última vía de administración.\n\nEl cuerpo no admite motivo: el Art. V.13 lo exige solo en las\neliminaciones.\n",
        "operationId" : "cambiarEstado_1",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeRoleStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol con su estado actualizado."
          },
          "400" : {
            "description" : "Estado ausente o fuera del dominio (`VAL-001`)"
          },
          "401" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Sin `roles:update`, o el rol está asignado al propio actor (`RN-SEG-011`)"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "El rol no existe o está eliminado (`EX-003`)"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Rol de sistema (`RN-SEG-012`) o rol raíz (`RN-SEG-007`)"
          },
          "500" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/RoleResponse"
                }
              }
            },
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Activar o desactivar un rol",
        "tags" : [ "Roles" ]
      }
    },
    "/api/v1/user-commission-rates" : {
      "get" : {
        "description" : "Devuelve las tasas **tal como se declararon**, cada una **con su\nproducto** (`id`, `code`, `name`, `price` y `currency`), y no resuelve\ncuál se aplica: para eso está `GET /api/v1/commissions/effective`.\n\n**Incluye el historial**: las vencidas viajan junto a la vigente salvo que\nse filtre por fecha con `onDate`. Y es **el único historial que le queda al\nmódulo** — las tasas de rol perdieron la vigencia.\n\n**Filtrar por persona devuelve las declaradas PARA esa persona**, no la que\nle aplica hoy sobre un producto.\n\n**Filtrar por producto (`productId`) devuelve las de ese producto**, de\ncualquier persona: es la respuesta a «quién tiene excepción aquí». Se\ncombina con los demás filtros — persona y producto juntos responden\n«¿tiene esta persona excepción en este producto?», con su historial. Un\nproducto donde nadie tiene excepción devuelve la página vacía, y **no\nsignifica que no comisione**: significa que todos cobran por su rol.\n\nHasta el 16-09-2026 cada fila traía `associatedProducts`, cuántos\nproductos hacían regir la excepción; hoy trae **cuál**, porque es uno.\n",
        "operationId" : "listar_1",
        "parameters" : [ {
          "in" : "query",
          "name" : "filtros",
          "required" : true,
          "schema" : {
            "$ref" : "#/components/schemas/ListUserCommissionRatesRequest"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRatePageResponse"
                }
              }
            },
            "description" : "Página de tasas personalizadas"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRatePageResponse"
                }
              }
            },
            "description" : "Parámetros inválidos"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRatePageResponse"
                }
              }
            },
            "description" : "Sin permiso"
          }
        },
        "summary" : "Consultar las tasas personalizadas",
        "tags" : [ "Comisiones" ]
      },
      "post" : {
        "description" : "Declara que **esta persona** gana esto —un porcentaje o un importe—\n**por este producto**, desde una fecha y, opcionalmente, hasta otra.\n**`productId` es obligatorio** (`RN-CM-021`, 16-09-2026): la tasa nace\ncon su producto, **rige sobre él desde su inicio de vigencia** y no\ncambia de producto — cambiar es retirarla y registrar otra. Una\nexcepción que abarque varios productos son varias tasas.\n\n**Registrar es poner en vigor.** No hay paso de asociación ni\nborrador. Del 11-09-2026 al 16-09-2026 la tasa se creaba sin producto y\nno pagaba nada hasta asociarse; las de aquel modelo se borraron (`V10`)\ny hay que registrarlas de nuevo, una por producto.\n\n**Una sola vigente por persona, producto y día** (`RN-CM-006`): un\nperiodo que pise a otra tasa viva de esa persona sobre ese producto\nresponde `409` — también cuando las dos llegan a la vez, porque la\nregla vive en el motor. Sobre **otro** producto no hay conflicto, y las\nconsecutivas son el historial.\n\nY todo lo que antes se comprobaba al asociar se comprueba aquí:\nproducto **vivo y no retirado**; el **tope individual** —un importe\nfijo no pasa del precio del producto, `RN-CM-019`—; la regla del\n**gratuito** —solo importe fijo, `RN-CM-020`—; y, para un importe\nfijo, **los decimales de la moneda del producto** (`RN-CM-017`).\n\n**No lleva rol, y eso tiene una consecuencia que conviene conocer**: la\ntasa sigue rigiendo aunque su titular pase a un rol que no comisiona.\nHasta el 01-09-2026 el rol era obligatorio precisamente para impedirlo.\n\n`validFrom` es obligatorio; sin `validTo`, rige indefinidamente.\n",
        "operationId" : "registrar_1",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterUserCommissionRateRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Tasa registrada, y rigiendo desde su inicio"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Datos inválidos, o el importe fijo no cabe en los decimales de la moneda del producto (`VAL-014`)"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Sin permiso"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Esa persona ya tiene una tasa viva sobre ese producto en parte de ese periodo (`EX-006`), el importe pasa del precio del producto (`EX-007`), o el producto es gratuito y la tasa es de porcentaje (`EX-008`)"
          },
          "422" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "La persona no existe (`EX-002`), o el producto no existe (`EX-003`) o está retirado (`EX-004`)"
          }
        },
        "summary" : "Registrar la tasa personalizada de una persona sobre un producto",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/user-commission-rates/{id}" : {
      "patch" : {
        "description" : "Corrige el **valor** —la forma y la cifra— y el **fin de vigencia**. Se\naplica lo que llega y se deja intacto lo que no.\n\n**Corregir no es cambiar**, y aquí la distinción sigue viva: corregir\narregla un error y reescribe lo que esa tasa dice que rigió; cambiar lo que\ngana alguien a partir de una fecha es **cerrar la vigente y registrar\notra**.\n\n`validTo: null` **vacía** el fin de vigencia y la tasa vuelve a regir\nindefinidamente; `rateType: null` se **rechaza**.\n\n**La persona, el producto y el inicio de vigencia no se corrigen**:\n`userId` y `validFrom` devuelven `400` con `VAL-009`, y `productId` es un\ncampo desconocido que también responde `400`.\n\n**Todo se revisa contra el producto de la tasa** (`RN-CM-021`): alargar\n`validTo` no puede pisar a otra tasa viva de la misma persona sobre él\n(`409`, `EX-006`); un `fixedAmount` que supere su precio se rechaza con\n`409` (`EX-007`); sobre un producto **gratuito** solo cabe el importe\nfijo (`RN-CM-020`): dejarla de porcentaje se rechaza con `409`\n(`EX-008`), y el fijo entra del importe que sea; y un importe fijo tiene\nque **caber en los decimales de la moneda del producto** (`VAL-014`).\n",
        "operationId" : "corregir",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateUserCommissionRateRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Tasa corregida"
          },
          "400" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Datos inválidos o campos no corregibles"
          },
          "403" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "Sin permiso"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "No existe, o está retirada"
          },
          "409" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserCommissionRateResponse"
                }
              }
            },
            "description" : "La vigencia resultante se solapa (`EX-006`), el valor pasaría del precio del producto (`EX-007`), o dejaría de porcentaje la tasa de un producto gratuito (`EX-008`)"
          }
        },
        "summary" : "Corregir una tasa personalizada",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/user-commission-rates/{id}/deletion" : {
      "post" : {
        "description" : "Retira una tasa que **no debió existir**, con **motivo obligatorio**, y\ndesde ese instante **esa persona deja de cobrar por ese producto** por\nesta tasa.\n\n**Retirar no es cerrar la vigencia**: se cierra lo que dejó de regir, se\nretira lo que fue un error. Por eso el retiro **no toca la vigencia** — el\nregistro de eliminación debe poder decir qué periodo cubría lo retirado.\n\nLos días que ocupaba **quedan libres**, de modo que puede declararse otra\ntasa que los cubra. Del 11-09-2026 al 16-09-2026 una tasa asociada no\nse retiraba; sin asociación no hay condición.\n\n**No es idempotente**: retirar dos veces devuelve `409`.\n",
        "operationId" : "retirar",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeleteCommissionRateRequest"
              }
            }
          }
        },
        "responses" : {
          "204" : {
            "description" : "Tasa retirada"
          },
          "400" : {
            "description" : "Motivo ausente, en blanco o demasiado largo"
          },
          "403" : {
            "description" : "Sin permiso"
          },
          "404" : {
            "description" : "La tasa no existe"
          },
          "409" : {
            "description" : "Ya estaba retirada"
          }
        },
        "summary" : "Retirar una tasa personalizada",
        "tags" : [ "Comisiones" ]
      }
    },
    "/api/v1/users" : {
      "get" : {
        "description" : "Listado paginado con filtros, búsqueda y ordenamiento.\n\n**El orden por defecto es `lastName,asc`** y no el nombre de usuario:\nesta es la lista desde la que se administra el acceso, y quien la mira\nbusca a alguien por su apellido.\n\nSolo se puede ordenar por la lista blanca —`username`, `email`,\n`firstName`, `lastName`, `status`, `createdAt`, `updatedAt`—. **No se\nadmite ordenar por ningún campo de la credencial**: ordenar por la marca\nde cambio obligatorio produciría la lista de quien no ha cambiado su\ncontraseña inicial.\n\n**`roleId`, `membershipId` y `countryId` no se validan contra su\ncatálogo.** Un filtro por algo inexistente devuelve la colección vacía y\nno es un error.\n\n**`countryId` NO se acota a países activos**, y va contra la intuición:\nfiltrar solo por los activos convertiría desactivar un país en una forma\nde **esconder a su gente**, y este listado es justamente la herramienta\ncon la que se busca a quien quedó dentro para moverlo. Quien esté en un\npaís retirado aparece con normalidad.\n\nCada fila trae su `country` **resuelto y nunca nulo** — es el único\nobjeto anidado de la fila del que se puede decir eso: `roles` puede venir\nvacía y `membership` y `deletedAt` pueden venir nulos.\n\n**El listado NO publica el documento ni los datos de contacto**, y no se\npuede buscar por ellos. Es deliberado: exponer un documento en un listado\npaginado alcanza a mucha más gente que devolverlo en un detalle. Quien\nnecesite el documento de una persona usa `GET /api/v1/users/{id}`.\n\nLa búsqueda va sobre nombre de usuario, correo y nombre completo,\n**sin distinguir acentos ni mayúsculas**, y por fragmento.\n\n`membership` **no es nula cuando está vencida**: vencer no es lo mismo\nque no tener. `current` dice cuál de los dos casos es.\n\nUn filtro sin coincidencias devuelve `200` con la colección vacía. No\nhay `404` ni `422`.\n",
        "operationId" : "listar",
        "parameters" : [ {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "sort",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "roleId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "membershipId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "countryId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "search",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "includeDeleted",
          "required" : false,
          "schema" : {
            "type" : "boolean"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponse"
                }
              }
            },
            "description" : "Página de personas."
          },
          "400" : {
            "description" : "Paginación fuera de límites (`VAL-001`, `VAL-002`), campo de ordenamiento no admitido (`VAL-003`), o estado fuera de su dominio (`VAL-004`). Se devuelven **juntos**"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:read` (`AUTH-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar personas",
        "tags" : [ "Usuarios" ]
      },
      "post" : {
        "description" : "Registra una persona con sus roles iniciales.\n\nLa cuenta nace **activa** y **marcada para cambio obligatorio de\ncontraseña**: quien prepara el alta conoce la credencial, y esa ventana\nse cierra en el primer inicio de sesión. Ni el estado ni la marca se\nenvían; enviarlos devuelve `400`.\n**`membershipId` es OPCIONAL, y el superior es condicional.** Desde el\n05-09-2026 toda persona nace con nivel: si no se indica ninguno, se le\nconcede el de arranque (`BECA`). Ya no hay rechazo por indicarlo sin rol\nde consumidor ni por omitirlo teniéndolo — esa exigencia murió con\n`RN-SP-013`.\n\nEl rol de vendedor y el superior comercial **sí siguen siendo\ncondicionales en los dos sentidos**: indicar uno sin el otro devuelve\n`409`, no se ignora.\n\nEl superior debe portar el **rol padre inmediato** del rol vendedor de\nmayor rango de la persona, y estar activo.\n\n## Identidad, contacto y país\n\n**`countryId`, `documentTypeId`, `documentNumber` y `phone` son\nobligatorios**, y a diferencia de la membresía y el superior **no\ndependen de qué roles se concedan**: su ausencia es `400`, nunca un\n`409` condicional.\n\n**`companyPhone`, `addressLine1`, `addressLine2` y `city` son\nopcionales.** Exigir una dirección postal a un funcionario interno\nbloquearía su alta sin que nadie la necesite. `city` es **texto\nlibre**: no hay catálogo de ciudades.\n\n**El tipo de documento sale de `GET /api/v1/document-types`, y ese\ncatálogo contiene SOLO documentos de persona mayor de edad.** Ahí está\nla validación de mayoría de edad del sistema entero: no hay ninguna\nmarca que diga cuáles acreditan y cuáles no, porque **los que no\nacreditan no están**. Registrar a un menor no se rechaza — no se puede\nexpresar, porque no hay identificador que enviar. Un desplegable\nalimentado por ese catálogo no puede ofrecer una opción que el alta vaya\na rechazar.\n\n**El par tipo + número es único entre TODAS las personas, incluidas las\neliminadas** (`RN-SP-035`), igual que el nombre de usuario y el correo.\nEl `409` que produce **no dice de quién es** el documento, ni si esa\npersona sigue vigente.\n\nEl número de documento se guarda **recortado y en mayúsculas**, y\nlos **dos teléfonos** —`phone`, el personal, y `companyPhone`, el de la\nempresa— **normalizados a dígitos con un `+` opcional**, fuera espacios,\nguiones y paréntesis. La dirección y la ciudad solo se recortan.\n\n**`companyPhone` es OPCIONAL y `phone` no** (`RN-SP-037`): exigir un\nteléfono de empresa bloquearía el alta de todo el que no tenga una. Sale\n**presente y en nulo** cuando no se declara, nunca ausente.\n\n**Ningún teléfono se valida contra el país**: eso exigiría un catálogo de\nprefijos que no existe.\n\nLa respuesta agrupa `country`, `document` y `contact` en tres objetos, y\ndevuelve el **tipo de documento resuelto** —abreviación y nombre— para\nque no haga falta una segunda llamada al catálogo, que además exige otro\npermiso.\n\nLa contraseña no se recorta: un espacio al principio o al final es parte\nde ella.\n",
        "operationId" : "registrar",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterUserRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserResponse"
                }
              }
            },
            "description" : "Persona registrada. `Location` apunta a su detalle."
          },
          "400" : {
            "description" : "Formato u obligatoriedad incumplidos —incluidos país, tipo y número de documento y teléfono, que son obligatorios (`VAL-014` a `VAL-017`)—, contraseña que no cumple la política, o campo no admitido en el cuerpo"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin el permiso de creación de usuarios (`AUTH-002`)"
          },
          "409" : {
            "description" : "Identidad ya en uso (`RN-SP-016`), documento ya registrado por otra persona —vigente o eliminada— o tipo de documento inactivo (`RN-SP-035`), país inactivo (`RN-SP-034`), rol que excede los privilegios del actor (`RN-SEG-010`), vendedor sin superior o al revés (`RN-SP-019`), o superior que no porta el rol padre (`RN-SP-020`)"
          },
          "422" : {
            "description" : "Algún rol no existe o no está activo (`EX-003`), el país no existe (`EX-009`) o el tipo de documento no existe (`EX-010`) — referencias que no resuelven, frente al `409` de las que resuelven y una regla rechaza"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Registrar una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/me" : {
      "get" : {
        "description" : "Devuelve el perfil de **quien porta el token**, con sus **permisos\nefectivos**.\n\nSu `contact` lleva **dos teléfonos** —`phone`, el personal, y\n`companyPhone`, el de la empresa, este último **opcional** y por tanto\npresente y en nulo cuando no se declaró— junto a la dirección. Es lo que\npermite precargar el formulario de `PATCH /api/v1/users/me` sin\nreescribir nada de memoria.\n\nSu `membership` —**ausente**, no nula, cuando la persona no tiene una\nvigente— trae `code`, `name`, `level`, `color` y `endsAt`. `name` y\n`color` entran el 14-09-2026: son lo que la pantalla de «mi perfil»\nnecesita para pintar el nivel con su color y llamarlo por su nombre sin\npedir la cadena entera de membresías. El color son seis hexadecimales\nsin `#`.\n\n`me` es un **literal**, no un identificador: no se admite pedir el\npropio detalle por la ruta con identificador, que es otra operación y\nexige permiso de lectura de usuarios.\n\n**Sin parámetros de ningún tipo** — ni de ruta, ni de consulta, ni de\ncuerpo.\n\nLos permisos salen del **mismo componente que autoriza**, de modo que lo\nque esta pantalla dice que la persona puede hacer es exactamente lo que\nel sistema le dejará hacer. Es lo que permite a una interfaz decidir qué\nmostrar sin duplicar en el navegador una regla que vive en el servidor.\n\n**No exige permiso alguno**, solo estar autenticado: no hay recurso\najeno que proteger.\n\nDevuelve **solo el superior comercial, nunca el equipo**: a quién\nreporta uno es un dato del actor; quiénes dependen de uno es un conjunto\nde terceros.\n\nTrae además el `country`, el `document` y el `contact` del actor.\n**`country` nunca falta y `document` sí puede faltar**: las personas\nregistradas antes de que el documento fuera obligatorio no lo tienen, y\nel campo llega ausente. El navegador tiene que contemplarlo.\n\n**El `contact` se publica aquí para que la pantalla de edición pueda\nprecargarse**: es exactamente lo que `PATCH /api/v1/users/me` deja\ncorregir. El `document` viaja en la misma respuesta y **no** es\neditable por el titular — esa diferencia es la razón de que los dos\nvayan en objetos separados en lugar de como campos sueltos.\n\n`lastLoginAt` es un dato **informativo de la sesión en curso**, no una\nseñal de intrusión: el inicio de sesión sobrescribe ese valor al entrar.\n",
        "operationId" : "miPerfil",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/OwnProfileResponse"
                }
              }
            },
            "description" : "El perfil."
          },
          "401" : {
            "description" : "Sin credencial válida, o la cuenta fue eliminada tras emitirse el token (`AUTH-001`) — lo que dejó de valer es la sesión, no la ruta, y por eso no es `404`"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el propio perfil",
        "tags" : [ "Usuarios" ]
      },
      "patch" : {
        "description" : "Corrige el **propio** nombre, apellidos, correo y **datos de contacto**\n—los **dos teléfonos**, dirección, complemento y ciudad—. Autenticado y **sin\nningún permiso**: `RF-SP-027` hace el mismo cambio pero exige\n`users:update`, que es un permiso de administración, de modo que\nconcedérselo a alguien para que arregle su propio apellido le daría de\npaso la capacidad de editar el de cualquiera.\n\n`me` es un **literal**, no un identificador, y el cuerpo **no admite\nninguno**: la operación no se puede desviar hacia otra persona.\n\n**`currentPassword` es obligatorio si y solo si se envía `email`.**\nDesde `RF-SP-040` el correo es la vía por la que se recupera una\ncontraseña olvidada, así que cambiarlo es cambiar **quién puede\nrecuperar la cuenta**. Una sesión robada no lleva la contraseña, y\nexigirla convierte el robo de sesión en algo que **caduca** en lugar de\nen una apropiación permanente. Cambiar solo el nombre no la pide,\nporque equivocar un apellido no abre ninguna puerta.\n\n**Enviar el correo que ya se tiene sigue exigiendo la contraseña.** Que\nel valor no cambie se sabe después de mirarlo, y condicionar la\nexigencia a eso daría una forma de averiguar el correo vigente probando\nvalores.\n\n## Qué se puede cambiar aquí y qué no\n\n**El contacto sí; la identidad no.** El tipo y el número de documento y\nel país **no están en este cuerpo**: son identidad, no contacto, y los\ncorrige un administrador por `PATCH /api/v1/users/{id}`. Enviarlos\ndevuelve `400` por propiedad desconocida, no se ignoran.\n\nLa línea no es técnica: un teléfono nuevo o una mudanza son hechos que\nla persona conoce mejor que nadie y no deberían costar un ticket; el\ndocumento es con lo que figura en la auditoría, y el país decide qué\nmedios de pago se le ofrecen.\n\n**Ningún teléfono exige `currentPassword`**, al contrario que el correo.\nLa contraseña se pide cuando el campo **es una vía de acceso**, y el\nteléfono hoy no lo es. El día que exista verificación por SMS o segundo\nfactor telefónico, esta decisión se revisa.\n\n**El nulo explícito no significa lo mismo en todo el cuerpo.**\n`addressLine1`, `addressLine2`, `city` y `companyPhone` **lo aceptan y vacían**\n—«ya no vivo ahí» es un hecho que hay que poder registrar—; el nombre,\nlos apellidos, el correo y el **teléfono personal** lo rechazan con `400`.\n\n**`companyPhone` cae del lado que vacía aunque sea un teléfono**, y ahí\nestá toda la diferencia: lo que decide no es qué dato es, sino si\n`RN-SP-037` lo exige. «Ya no tengo teléfono de empresa» es un hecho que\nhay que poder registrar.\n\nDevuelve el perfil ya actualizado, con **la misma forma** que\n`GET /api/v1/users/me`.\n",
        "operationId" : "editarMiPerfil",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateOwnProfileRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/OwnProfileResponse"
                }
              }
            },
            "description" : "El perfil actualizado."
          },
          "400" : {
            "description" : "Ningún campo informado (`VAL-001`), campo vaciado (`VAL-002`), correo inválido (`VAL-003`), longitud excedida (`VAL-005`), falta la contraseña actual habiendo correo (`VAL-006`) o campo desconocido"
          },
          "401" : {
            "description" : "Sin credencial válida, o la cuenta fue eliminada tras emitirse el token"
          },
          "403" : {
            "description" : "Hay un cambio obligatorio de contraseña pendiente: se atiende primero. Esta ruta **no** figura entre las alcanzables con esa marca"
          },
          "409" : {
            "description" : "Ese correo ya está en uso por otra persona (`RN-SP-016`)"
          },
          "422" : {
            "description" : "La contraseña actual no es correcta (`VAL-007`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Editar el propio perfil",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/me/team/broker-accounts" : {
      "get" : {
        "description" : "Devuelve, **paginadas**, las cuentas de broker de todas las personas\nque dependen **directamente** del actor, **cada fila con su titular**.\n\n**No hay identificador en la ruta y no lo habrá**: el conjunto de datos\nlo determina el sistema a partir de quién pregunta. Quien deba ver las\ncuentas de otra persona usa `GET /api/v1/users/{id}/broker-accounts`\ncon `broker-accounts:read`.\n\n**No exige ningún permiso**, solo estar autenticado: el alcance lo pone\nla estructura comercial (`RN-SP-046`). Quien **no tiene equipo** recibe\n`200` con la página vacía, no `404` ni `403`.\n\n**Un solo nivel**, el equipo directo. Las cuentas de quien depende de\nun subordinado del actor **no aparecen**: devolver la rama completa\npublicaría de una vez la estructura de la empresa.\n\n**Es un listado de CUENTAS y no de personas.** Quien está en el equipo\ny no declaró ninguna cuenta **no aparece**, y quien declaró dos aparece\n**dos veces**. Quién hay en el equipo lo responde\n`GET /api/v1/users/{id}/team`.\n\n**Filtros, ambos opcionales y combinables con Y:**\n\n- `status` — `REGISTER` o `FIRST_DEPOSIT`. **Cualquier otro valor es\n  `400`**, no una página vacía: una página vacía sería indistinguible\n  de «nadie está en ese estado».\n- `brokerId` — un broker del catálogo. **Un identificador inexistente\n  devuelve la página vacía sin error**, al revés que `status`: es una\n  pregunta legítima con respuesta vacía.\n\n`totalElements` **cuenta lo filtrado**.\n\n**Hoy todas las cuentas están en `REGISTER`**, y no es un fallo de esta\nconsulta: quien mueve una cuenta a `FIRST_DEPOSIT` es el webhook del\nbroker, que todavía no existe. Mientras no exista, ninguna cuenta tiene\ndepósito confirmado.\n\n**`brokerUsername` llega en nulo** mientras el broker no lo haya\nconfirmado, y el campo **está presente**: su nulo significa «aún no\nconfirmado», no «sin nombre».\n",
        "operationId" : "cuentasDeBrokerDelEquipo",
        "parameters" : [ {
          "description" : "Estado de la cuenta: `REGISTER` o `FIRST_DEPOSIT`.",
          "in" : "query",
          "name" : "status",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "description" : "Broker del catálogo. Uno inexistente devuelve la página vacía.",
          "in" : "query",
          "name" : "brokerId",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PageResponseTeamBrokerAccountItem"
                }
              }
            },
            "description" : "Página de cuentas del equipo directo, ordenada por titular, broker e identificador de cuenta. Vacía si el actor no tiene equipo."
          },
          "400" : {
            "description" : "`status` fuera de `REGISTER`/`FIRST_DEPOSIT` (`VAL-001`), `brokerId` malformado o paginación fuera de límites (`VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar las cuentas de broker de mi equipo",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}" : {
      "get" : {
        "description" : "Sus roles **con el estado de cada uno**, sus permisos efectivos, su\nmembresía y el contexto de su acceso.\n\nLas dos primeras juntas son lo único que explica por qué una persona\n**con roles** no puede hacer nada: porque todos están inactivos y la\nlista de permisos llega vacía.\n\nLos permisos efectivos salen del **mismo componente que autoriza**, de\nmodo que esta respuesta no puede contradecir a lo que el sistema hará\ncon la siguiente petición de esa persona.\n\n`lockedUntil` nulo significa dos cosas distintas y eso es información:\nla cuenta no está bloqueada, o lo está **por decisión de un actor** y\npor tanto sin expiración. El estado desambigua.\n\nTrae el `country`, el `document` y el `contact` de la persona. El\ncontacto lleva **dos teléfonos**: `phone`, el personal, y `companyPhone`,\nel de la empresa — este último **opcional**, de modo que llega presente y\nen nulo cuando la persona no lo declaró.\n\n**`country` nunca es nulo y `document` sí puede serlo**: quienes se\nregistraron antes de que el documento fuera obligatorio no lo tienen, y\n**esta pantalla es donde esa ausencia se ve** — es la que sirve para\nsaber a quién hay que completar.\n\n**El país y el tipo de documento se devuelven aunque estén inactivos.**\nRetirarlos del catálogo los quita de los desplegables del alta; no\ncambia dónde está ni con qué se identifica quien ya los tenía, ni lo\noculta a quien administra.\n\n**No devuelve intentos fallidos** —diría cuántos le quedan a una cuenta\nantes de bloquearse—, **ni dato alguno de la credencial**, **ni el\nsuperior comercial**, que tiene su propio endpoint.\n\nUna persona eliminada devuelve el **mismo** `404` que una inexistente,\nsin ninguna pista de que existió.\n",
        "operationId" : "detalle",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserDetailResponse"
                }
              }
            },
            "description" : "La persona."
          },
          "400" : {
            "description" : "El identificador no es un UUID en forma canónica (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:read` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe, o está eliminada (`EX-001`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el detalle de una persona",
        "tags" : [ "Usuarios" ]
      },
      "patch" : {
        "description" : "Modifica el nombre, los apellidos, el correo, el país, la **identidad\ndocumental** y los **datos de contacto**. `PATCH` y no `PUT`:\n`PUT` obligaría a enviar el recurso completo —incluidos el nombre de\nusuario, el estado y los roles, que esta operación **no** puede\nmodificar— y habría que decidir qué hacer si llegaran con otros valores.\n\n**Todos los campos son opcionales, y el nulo explícito NO significa lo\nmismo en todos.** El campo ausente nunca se toca; con el nulo hay **dos\nfamilias**, y la línea que las separa es la de lo obligatorio y lo\nopcional:\n\n- **Lo rechazan con `400`**: `firstName`, `lastName`, `email`,\n  `countryId`, `documentTypeId`, `documentNumber` y `phone`. Su columna\n  no admite ausencia, y aceptarlo produciría un `500` en lugar del `400`\n  que corresponde.\n- **Lo aceptan y VACÍAN el campo**: `addressLine1`, `addressLine2`,\n  `city` y `companyPhone`. Son opcionales, y «ya no vive ahí» —o «ya no\n  tiene ese número»— es un hecho que hay que poder registrar.\n\n**`companyPhone` cae del lado que vacía aunque sea un teléfono**, y es la\ncomprobación de que la línea está bien trazada: comparte forma y\nvalidación con `phone` y aun así va del otro lado, porque lo que decide\nno es qué dato es sino si `RN-SP-037` lo exige.\n\n**El tipo y el número de documento se envían juntos o no se envían.**\nEnviar uno solo devuelve `400`: un número sin decir de qué documento es\nno significa nada.\n\n**Esta es la ÚNICA operación que cambia el documento y el país.** El\ntitular no puede tocarlos desde `PATCH /api/v1/users/me`.\n\n**Se comprueba el destino, nunca el actual.** Un país o un tipo de\ndocumento inactivos se rechazan **como destino**; que los vigentes de\nla persona lo estén no impide editarla — es justamente para eso que\nexiste esta operación.\n\n**Corregir el documento no libera el anterior.** Es la diferencia\ndeliberada con el correo, que sí queda libre: un documento identifica a\nalguien en el mundo real, y liberarlo permitiría que otra ficha lo\ntomara.\n\n**El nombre de usuario no se puede cambiar**, y enviarlo devuelve `400`\npor propiedad desconocida en lugar de ignorarse en silencio. Lo mismo\ncon el estado, los roles, la membresía y la contraseña: cada uno tiene\nsu operación.\n\nEl correo se normaliza —recorte y minúsculas— **antes** de compararse,\nde modo que reenviar el propio en mayúsculas es un cambio sin efecto y\nno un conflicto consigo mismo.\n\n**El actor sí puede editarse a sí mismo**: corregir el propio apellido\nno concede ningún privilegio.\n",
        "operationId" : "editar",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateUserRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserResponse"
                }
              }
            },
            "description" : "La persona, con los datos actualizados."
          },
          "400" : {
            "description" : "Ningún campo informado (`VAL-001`), campo vaciado que no admite vaciarse (`VAL-002`, `VAL-006` país, `VAL-007` documento, `VAL-008` teléfono), tipo y número de documento sin su pareja (`VAL-007`), correo inválido (`VAL-003`), longitud excedida (`VAL-005`) o campo desconocido —incluidos el nombre de usuario, el estado, los roles y la contraseña"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:update` (`AUTH-002`)"
          },
          "404" : {
            "description" : "No existe, o está eliminada (`EX-002`)"
          },
          "409" : {
            "description" : "El correo ya está en uso (`RN-SP-016`). **El mensaje no dice de quién es**: puede ser de alguien eliminado, y decirlo revelaría una cuenta"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Editar los datos de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/broker-accounts" : {
      "get" : {
        "description" : "Devuelve las cuentas de broker de esa persona: **broker, identificador\nde cuenta, nombre de usuario en el broker y estado**, ordenadas por\nnombre de broker e identificador de cuenta.\n\n**Quién puede consultarlas** (`RN-SP-046`): **su superior comercial\nvigente**, sin ningún permiso, o quien traiga **`broker-accounts:read`**\nsobre cualquiera. Es la primera lectura del sistema cuyo alcance sale\nde la estructura comercial, y **solo alcanza a esta**.\n\n**Quien no es ninguna de las dos cosas recibe `404`, no `403`**, y es\ndeliberado: el mismo `404` que si la persona no existiera. Un `403`\ndejaría a cualquier vendedor recorrer identificadores y averiguar\ncuáles corresponden a personas reales. Los dos cuerpos son idénticos.\n\n**Quien FUE su superior y ya no lo es recibe `404`**: el historial de\nla estructura no concede lectura.\n\n**El titular NO ve aquí sus propias cuentas** salvo que traiga el\npermiso: esta lectura se definió sobre el equipo.\n\n**Una persona sin cuentas devuelve `200` con la colección vacía**, no\n`404`: solo el registro por un enlace de beca obliga a declararlas.\n\n**Hoy el estado es siempre `REGISTER`**, y no es un fallo: quien mueve\nuna cuenta a `FIRST_DEPOSIT` es el webhook del broker, que todavía no\nexiste.\n\n**`brokerUsername` llega en nulo** mientras el broker no lo haya\nconfirmado, y el campo **está presente**: su nulo significa «aún no\nconfirmado», no «sin nombre».\n\n**No se pagina**: una persona tiene unas pocas cuentas. El listado\npaginado es `GET /api/v1/users/me/team/broker-accounts`.\n",
        "operationId" : "cuentasDeBrokerDe",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BrokerAccountsResponse"
                }
              }
            },
            "description" : "Las cuentas de la persona, ordenadas. Vacía si no declaró ninguna."
          },
          "400" : {
            "description" : "Identificador malformado (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "404" : {
            "description" : "La persona no existe, está eliminada, **o el actor no es su superior vigente ni posee `broker-accounts:read`** (`VAL-002`). Los tres casos responden lo mismo, a propósito."
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar las cuentas de broker de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/deletion" : {
      "post" : {
        "description" : "Eliminación **lógica** y con motivo declarado. `POST` sobre un\nsubrecurso y no `DELETE` con cuerpo: RFC 9110 no define semántica para\nel cuerpo de un `DELETE` y un intermediario puede descartarlo,\nconvirtiendo la petición en un rechazo por motivo ausente que el actor\nno puede entender. Y tampoco por *query string*, o el motivo acabaría en\nlos registros de acceso de los proxies.\n\n**El motivo se verifica el primero de todo**, antes incluso de saber si\nla persona existe.\n\nLa operación **retira sus roles y su membresía**, **cierra** su\nasignación de superior —cerrarla, nunca borrarla: es historial de\nmando— y **revoca sus sesiones**, todo en la misma transacción y con la\nmisma marca de tiempo.\n\n**El estado NO se toca**: se conserva como estaba para que el registro\nde eliminación diga en qué situación estaba la persona cuando se la\neliminó.\n\nEl `404` **no distingue** «nunca existió» de «ya estaba eliminada».\n",
        "operationId" : "eliminar",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DeleteUserRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Persona eliminada."
          },
          "400" : {
            "description" : "Motivo ausente o vacío (`VAL-001`), o campo desconocido"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:delete` (`AUTH-002`), o es la cuenta del propio actor (`RN-SP-017`)"
          },
          "404" : {
            "description" : "No existe, o ya estaba eliminada (`EX-004`)"
          },
          "409" : {
            "description" : "Es el último superadministrador activo (`RN-SP-001`), o tiene personas a cargo (`RN-SP-022`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Eliminar a una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/membership" : {
      "delete" : {
        "description" : "**Devuelve al nivel de arranque, y no deja a nadie sin nivel.** Desde el\n05-09-2026 `RN-SP-018` exige que **toda** persona tenga membresía, de\nmodo que esta operación cierra la que tenga y le abre una `BECA`.\n\nExiste para **corregir un nivel concedido por error**. Bajar a alguien a\nun nivel intermedio es la operación de membresía, que admite indicar\ncuál.\n\n**Responde `200` con la membresía resultante**, no `204`: devolver un\ncuerpo vacío diría que no queda nada, y queda el nivel de arranque —\nquien llama necesita saber en qué quedó la persona sin volver a\npreguntar.\n\n**Es idempotente.** Aplicada sobre quien ya está en el suelo no escribe\nni audita, y devuelve lo mismo.\n\n**Ya no rechaza a los consumidores.** Hasta el 05-09-2026 exigía que la\npersona **no** portara ningún rol de consumidor, porque la regla de\nentonces no admitía consumidores sin nivel; retirada `RN-SP-013`, esa\nprecondición no protege nada.\n\n**Conserva el `DELETE`** y no le alcanza la enmienda del retiro de\nroles: esta operación no lleva cuerpo, de modo que el problema que\naquella evitaba no existe aquí.\n",
        "operationId" : "devolverMembresiaAlSuelo",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserMembershipResponse"
                }
              }
            },
            "description" : "La persona queda en el nivel de arranque, que se devuelve."
          },
          "400" : {
            "description" : "Identificador malformado (`VAL-001`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:assign-membership` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Devolver la membresía de una persona al suelo",
        "tags" : [ "Usuarios" ]
      },
      "put" : {
        "description" : "**`PUT` y no `POST`**, al revés que la asignación de roles, y la\ndiferencia no es de gusto: aquí el cuerpo **sí** representa el estado\nfinal. La persona tiene siempre exactamente una, de modo que enviar una\nla deja como la única — y de ahí sale gratis la idempotencia.\n\n`endsAt` es opcional. **Ausente significa indefinida**: enviarlo ausente\nsobre una membresía que tenía fecha la convierte en indefinida, y es un\ncaso normal, no un olvido que haya que interpretar. Presente, la\nmembresía deja de estar vigente **al llegar** ese instante, no después.\n\nRepetir la petición idéntica no escribe ni deja auditoría. Cambiar solo\nla fecha sí es un cambio y sí se registra.\n\nDevuelve `200` incluso la primera vez: `PUT` sobre una ruta fija no crea\nun recurso direccionable nuevo.\n",
        "operationId" : "fijarMembresia",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AssignMembershipRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserMembershipResponse"
                }
              }
            },
            "description" : "La membresía, con su nivel y su vigencia."
          },
          "400" : {
            "description" : "Membresía ausente o malformada (`VAL-001`), o fecha de fin igual o anterior al momento de la asignación (`VAL-005`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:assign-membership` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-004`)"
          },
          "422" : {
            "description" : "La membresía indicada no existe en la cadena (`VAL-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Fijar la membresía de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/password-reset" : {
      "post" : {
        "description" : "Fija una credencial **provisional** sobre la cuenta indicada. `POST`\nsobre un subrecurso y no `PATCH` sobre el usuario: cada petición **crea**\nun restablecimiento, que es un hecho con fecha y con caducidad propia.\n\n**La credencial CADUCA.** Sin plazo, una cuenta restablecida y nunca\nusada conserva indefinidamente una contraseña que otra persona conoce, y\nnadie se entera porque no falla nada. Quien comprueba el plazo es el\ninicio de sesión.\n\nLa cuenta queda marcada para **cambio obligatorio**: la ventana en que\ndos personas conocen la misma contraseña se cierra en el primer inicio\nde sesión.\n\n**No toca el estado ni el bloqueo**: restablecer no es reactivar. Una\ncuenta desactivada sigue desactivada.\n\n**La contraseña asignada no se devuelve**: la conoce quien la escribió, y\nrepetirla la expondría a cualquier registro de la operación. Tampoco la\nfecha de caducidad — quien la necesite consulta el detalle.\n\nRevoca **todas** las sesiones de la persona.\n",
        "operationId" : "restablecerContrasena",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ResetPasswordRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "204" : {
            "description" : "Credencial fijada."
          },
          "400" : {
            "description" : "Contraseña ausente (`VAL-001`) o que no cumple la política (`VAL-002`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:reset-password` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-004`)"
          },
          "409" : {
            "description" : "Es la cuenta del propio actor (`RN-SP-017`). El mensaje indica cuál es la operación correcta: cambiar la propia contraseña, que exige conocer la actual"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Restablecer la contraseña de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/roles" : {
      "post" : {
        "description" : "Agrega roles. **No reemplaza la lista**: por eso es un `POST` sobre un\nsubrecurso y no un `PUT`. Un reemplazo haría retiros implícitos que se\nsaltarían tres reglas cuyo incumplimiento nadie vería.\n\nPedir un rol que la persona ya tiene no es un error: no cambia nada y\nno deja rastro en la auditoría.\n\n**Esta operación no toca la membresía**, y desde el 05-09-2026 tampoco\nla admite: `membershipId` y `membershipEndsAt` se retiraron del cuerpo.\nToda persona tiene nivel desde el alta, de modo que cuando llega esta\npetición ya lo tiene; cambiarlo es la operación de membresía, que tiene\nsu propio permiso.\n\n`supervisorId` es **condicional**: obligatorio exactamente cuando la\noperación cambia el rango comercial de la persona, y no admitido en\ncualquier otro caso. Su admisibilidad depende del estado de la persona\ny no del cuerpo, de modo que su incumplimiento es `422` y nunca `400`.\n\nUn **ascenso** —que cambia el rol vendedor de mayor rango— exige\ndeclarar de nuevo el superior: el anterior puede haber dejado de ser\nadmisible sin que nadie tocara esa fila.\n",
        "operationId" : "asignarRoles",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AssignRolesRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserResponse"
                }
              }
            },
            "description" : "La persona con su estructura actualizada."
          },
          "400" : {
            "description" : "Lista vacía, identificador malformado o más de 100 elementos"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:assign-roles` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-006`)"
          },
          "409" : {
            "description" : "Algún rol concede permisos que el actor no posee (`RN-SEG-010`). El cuerpo enumera cuáles"
          },
          "422" : {
            "description" : "Rol inexistente (`EX-002`), rol inactivo (`EX-003`), vendedor sin superior (`RN-SP-019`), o superior inadmisible (`VAL-007`, `RN-SP-020`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Asignar roles a una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/roles/revocations" : {
      "post" : {
        "description" : "Retira roles y **arrastra UNA cascada**: quedarse sin ningún rol de\nvendedor cierra la asignación de superior comercial —cerrarla, nunca\nborrarla: esa fila dice a quién se atribuía cada resultado—.\n\n**La membresía ya no se arrastra** (05-09-2026). Quien deja de ser\nconsumidor **conserva el nivel que tenía**, incluido uno comprado: toda\npersona debe tener membresía, y bajarla al suelo sería quitarle algo\nque pagó.\n\n**Revoca todas las sesiones de la persona.** Asignar no lo hace;\nretirar sí, porque el refresh token sobrevive al cambio de permisos y\ndejaría vivo hasta siete días el acceso que se acaba de quitar.\n\nEs un `POST` sobre un subrecurso y no un `DELETE`: la lista viaja en el\ncuerpo, y RFC 9110 no define semántica para el cuerpo de un `DELETE` —\nun intermediario puede descartarlo sin avisar, y el retiro llegaría sin\nroles.\n\n**No se pide motivo** y **no se comprueba que los roles existan**:\nretirar un rol eliminado del catálogo es legítimo, porque la asignación\nsigue ahí y debe poder soltarse.\n",
        "operationId" : "retirarRoles",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RevokeRolesRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserResponse"
                }
              }
            },
            "description" : "La persona con su estructura actualizada."
          },
          "400" : {
            "description" : "Lista vacía, identificador malformado o más de 100 elementos"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:assign-roles` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-006`)"
          },
          "409" : {
            "description" : "El retiro dejaría al sistema sin superadministrador activo (`RN-SP-001`), algún rol excede los permisos del actor (`RN-SEG-010`), o la persona tiene equipo a cargo (`RN-SP-022`) — este último informa cuántas personas, nunca quiénes"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Retirar roles de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/status" : {
      "patch" : {
        "description" : "Subrecurso propio y no un campo de la edición: el estado tiene reglas de\nrechazo que la edición no tiene y exige un motivo que la edición no\nadmite.\n\n**Se envía el estado destino y no una acción**, lo que hace la operación\nidempotente por construcción. Pedir el estado que ya se tiene no cambia\nnada y no deja evento.\n\n**Salvo en un caso, y es el que da sentido al requerimiento:** pasar de\nbloqueo **automático** a bloqueo **manual** sí es un cambio aunque el\nestado sea el mismo — `lockedUntil` pasa de informado a nulo, y con ello\nel bloqueo deja de levantarse solo.\n\n**El motivo es condicional en los dos sentidos:** obligatorio al retirar\nel acceso, **rechazado** al devolverlo.\n\n**`PENDIENTE` no se admite**, aunque el esquema lo acepte: ningún\nrequerimiento lo produce y sería el único camino hacia un estado del que\nnadie sabe salir.\n\n**Reactivar nunca falla por regla.** Devolver el acceso no puede dejar a\nnadie sin administración ni a ningún equipo huérfano.\n\nRetirar el acceso **revoca todas las sesiones** de la persona.\n",
        "operationId" : "cambiarEstado",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeUserStatusRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserStatusResponse"
                }
              }
            },
            "description" : "El estado resultante. `lockedUntil` nulo con BLOQUEADO significa manual."
          },
          "400" : {
            "description" : "Estado ausente, fuera del dominio o `PENDIENTE` (`VAL-001`); motivo ausente al retirar (`VAL-005`) o presente al devolver (`VAL-006`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:update` (`AUTH-002`), o es la cuenta del propio actor (`RN-SP-017`) — dos casos distintos con `error_code` distinto"
          },
          "404" : {
            "description" : "No existe, o está eliminada (`EX-005`)"
          },
          "409" : {
            "description" : "Es el último superadministrador activo (`RN-SP-001`), o tiene personas a cargo (`RN-SP-022`) — este último dice **cuántas**, nunca quiénes"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Retirar o devolver el acceso de una persona",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/supervisor" : {
      "patch" : {
        "description" : "`PATCH` sobre el subrecurso y no `PUT`: el cuerpo no representa el\nestado completo —falta el periodo, que lo fija el sistema— y `PUT`\ninvitaría a pensar que se puede enviar.\n\n**No admite fecha de inicio.** La asignación rige desde que se ejecuta,\nsiempre. Declararla obligaría a especificar solapamientos, huecos entre\ntramos y correcciones retroactivas sobre periodos ya liquidados.\n\n**No admite retirar el superior.** El estado «vendedor sin superior» no\nexiste: la única salida es dejar de portar rol comercial retirándolo.\n\nCada cambio **cierra** el tramo vigente con su fecha y **abre** otro:\nnunca se sobrescribe el superior de una fila. La respuesta devuelve el\n**anterior con su fecha de cierre**, que es lo que permite confirmar de\nun vistazo que se cerró el tramo que se creía cerrar.\n\n**El equipo se mueve con el reasignado**: sus subordinados conservan su\nsuperior y solo cambia de quién depende la rama.\n\nRepetir la operación con el mismo superior no cierra ni abre nada y no\ndeja auditoría — pero el motivo se exige igual, antes de saberlo.\n",
        "operationId" : "asignarSuperior",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AssignSupervisorRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommercialStructureResponse"
                }
              }
            },
            "description" : "La estructura, con el superior nuevo y el anterior."
          },
          "400" : {
            "description" : "Identificador malformado, o motivo ausente o vacío (`VAL-001`, `VAL-008`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:assign-supervisor` (`AUTH-002`)"
          },
          "404" : {
            "description" : "Alguna de las dos personas no existe o está eliminada (`VAL-002`)"
          },
          "409" : {
            "description" : "El actor es el propio subordinado (`RN-SP-017`), el subordinado no pertenece a la fuerza comercial (`VAL-003`) o es la cúspide (`VAL-004`), el superior no porta el rol que exige el orden de mando (`RN-SP-020`) —el mensaje dice cuál—, no está activo (`VAL-006`), o sería su propio superior (`VAL-007`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Establecer o cambiar el superior comercial",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v1/users/{id}/team" : {
      "get" : {
        "description" : "Devuelve el superior inmediato y el **equipo directo** de la persona,\npaginado.\n\n**Un solo nivel, nunca el árbol descendente**, y **sin conteo\nindirecto**: `totalElements` cuenta a quienes reportan directamente.\nDevolver la rama completa publicaría de una vez la estructura de la\nempresa por un permiso de lectura de usuarios.\n\n**Cada persona lleva `roles`: TODOS los que porta**, con `id`, `code`\ny `name`, ordenados por código y **presentes aunque la lista vaya\nvacía**. Es el mismo objeto que devuelve `GET /api/v1/users` en cada\nfila. **Sustituye a `roleCode`** (10-09-2026), que traía uno solo y\núnicamente si era de la fuerza comercial: la cartera de clientes\nllegaba con el rol en nulo y era indistinguible de un vendedor sin rol.\n\n**`roles` es además el único filtro**, y acota **el equipo**. Se pasan\n**códigos**, varios admitidos —`?roles=AGENTE,CLIENTE` o repitiendo el\nparámetro—, y entra quien porte **alguno** de ellos. `totalElements`\n**cuenta lo filtrado**. Un código que no existe devuelve el equipo\nvacío con `200`, **no un error**: es el mismo criterio que el filtro\npor rol de `GET /api/v1/users`.\n\n**El filtro NO toca al superior ni a la persona consultada.** Los dos\nse devuelven igual aunque no porten ninguno de los roles pedidos, y eso\nes contrato: `supervisor` va **ausente**, no en nulo, **solo** cuando\nla persona es la cúspide comercial, que es lo que distingue «no depende\nde nadie» de «no se pudo resolver».\n\n**Ningún otro filtro.** Ni búsqueda por nombre, ni estado, ni país: el\nlistado general de usuarios ya los tiene, y replicarlos aquí obligaría\na mantener dos semánticas sincronizadas.\n\n**Sin historial de superiores.**\n\nQuien no pertenece a la fuerza comercial recibe `200` con la estructura\nvacía, no `404` ni `409` — **con sus roles a la vista**: no tener\nestructura comercial no es no tener roles.\n\n**El alcance es global** mientras la decisión D-22 siga abierta: quien\nposea el permiso ve el equipo de cualquiera, no solo el suyo.\n",
        "operationId" : "equipo",
        "parameters" : [ {
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Códigos de rol que acotan el equipo. Varios admitidos, con semántica O.",
          "in" : "query",
          "name" : "roles",
          "required" : false,
          "schema" : {
            "items" : {
              "type" : "string"
            },
            "type" : "array"
          }
        }, {
          "in" : "query",
          "name" : "page",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        }, {
          "in" : "query",
          "name" : "size",
          "required" : false,
          "schema" : {
            "format" : "int32",
            "type" : "integer"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CommercialStructureResponse"
                }
              }
            },
            "description" : "La estructura, con el equipo paginado y los roles de cada persona. El filtro por roles acota el equipo y su total, nunca al superior."
          },
          "400" : {
            "description" : "Identificador malformado o paginación fuera de límites (`VAL-003`)"
          },
          "401" : {
            "description" : "Token ausente o inválido (`AUTH-001`)"
          },
          "403" : {
            "description" : "Autenticado sin `users:read` (`AUTH-002`)"
          },
          "404" : {
            "description" : "La persona no existe o está eliminada (`VAL-002`)"
          },
          "500" : {
            "description" : "Fallo no controlado (`ERR-500`)"
          }
        },
        "summary" : "Consultar el superior y el equipo a cargo",
        "tags" : [ "Usuarios" ]
      }
    }
  },
  "security" : [ {
    "bearerAuth" : [ ]
  } ],
  "servers" : [ {
    "description" : "Este mismo servidor",
    "url" : "/"
  } ],
  "tags" : [ {
    "description" : "Catálogo de documentos de identidad admitidos. Solo lectura: su contenido es la validación de mayoría de edad (RN-SP-036).",
    "name" : "Tipos de documento"
  }, {
    "description" : "Catálogo de venta: upgrades de membresía y bots.",
    "name" : "Productos"
  }, {
    "description" : "Registro y administración de los roles del sistema.",
    "name" : "Roles"
  }, {
    "description" : "El libro de hechos económicos: qué se vendió, a quién y a quién se le atribuye.",
    "name" : "Movimientos"
  }, {
    "description" : "Los cuatro registros de auditoría del sistema. Solo lectura, y cada uno con su permiso.",
    "name" : "Auditoría"
  }, {
    "description" : "Catálogo de países. Inmutable salvo su estado.",
    "name" : "Países"
  }, {
    "description" : "Tasas de comisión personalizadas: la excepción por persona.",
    "name" : "Comisiones"
  }, {
    "description" : "Todas las cuentas de broker del sistema, para quien administra. Las del propio equipo se consultan sin permiso en GET /api/v1/users/me/team/broker-accounts.",
    "name" : "Cuentas de broker"
  }, {
    "description" : "Catálogo de los brokers con los que opera la plataforma. Solo lectura: se puebla por migración (RN-SP-039).",
    "name" : "Brokers"
  }, {
    "description" : "Tasas de comisión por rol: qué paga cada producto.",
    "name" : "Comisiones"
  }, {
    "description" : "Cadena lineal de niveles de consumidor. Inmutable salvo el reordenamiento.",
    "name" : "Membresías"
  }, {
    "description" : "Alta y administración de las personas del sistema.",
    "name" : "Usuarios"
  }, {
    "description" : "Catálogo de permisos del sistema. Solo lectura.",
    "name" : "Permisos"
  }, {
    "description" : "A cuánto se cambia una moneda por otra",
    "name" : "Tasas de cambio"
  }, {
    "description" : "El enlace público que un vendedor reparte",
    "name" : "Hotlinks"
  }, {
    "description" : "Con qué se puede pagar, y en qué países no vale cada medio.",
    "name" : "Métodos de pago"
  }, {
    "description" : "Lo que quien compra dice de un producto.",
    "name" : "Reseñas"
  }, {
    "description" : "Paquetes de productos: varios productos con su descuento.",
    "name" : "Paquetes"
  }, {
    "description" : "Resolución de la comisión efectiva de una persona sobre un producto.",
    "name" : "Comisiones"
  }, {
    "description" : "Catálogo de monedas. Inmutable por API salvo su estado.",
    "name" : "Monedas"
  }, {
    "description" : "Inicio, renovación y cierre de sesión.",
    "name" : "Sesión"
  }, {
    "description" : "Alta de un cliente desde el enlace de un vendedor. Público (RF-SP-045).",
    "name" : "Registro por enlace"
  } ]
}
